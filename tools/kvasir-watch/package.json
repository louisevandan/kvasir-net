{
  "name": "kvasir-watch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Daily report on the three Kvasir tracks: Ring hardening (read through the p4 bridge, never an agent socket), settlement gateway, client app.",
  "scripts": {
    "collect": "node collect.mjs",
    "report": "node report.mjs",
    "daily": "./daily.sh",
    "test": "node --test test/*.test.js",
    "ring-alert": "node ring-alert.mjs"
  },
  "dependencies": {},
  "engines": {
    "node": ">=20"
  }
}
