# P4 development and validation tools

`controller/` contains external client and evidence logic. `scripts/` contains fixture, E2E, benchmark, and launch automation. Tools are not linked into production Rust binaries.
