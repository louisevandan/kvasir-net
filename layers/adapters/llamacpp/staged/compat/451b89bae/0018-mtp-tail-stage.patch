diff --git a/src/llama-context.cpp b/src/llama-context.cpp
index 111cf23ece7aac59d5ecc8c938c3e629d2217d0d..f4ddeebcdf2b5eb6c0724add76e494db45176e9f 100644
--- a/src/llama-context.cpp
+++ b/src/llama-context.cpp
@@ -1398,10 +1398,11 @@ llm_graph_result * llama_context::process_ubatch(const llama_ubatch & ubatch, ll
         //const auto t_start_us = ggml_time_us();
 
         // FIXME this call causes a crash if any model inputs were not used in the graph and were therefore not allocated
+        const bool linkcpp_mtp_graph = gtype == LLM_GRAPH_TYPE_DECODER_MTP;
         res->set_inputs(
             &ubatch,
-            model.linkcpp_layer_begin() > 0,
-            model.linkcpp_layer_end() >= 0
+            !linkcpp_mtp_graph && model.linkcpp_layer_begin() > 0,
+            !linkcpp_mtp_graph && model.linkcpp_layer_end() >= 0
                 && model.linkcpp_layer_end() < (int32_t) model.hparams.n_layer());
         const auto * stage_payloads = &linkcpp_input_payloads;
         std::vector<std::vector<uint8_t>> ordered_payloads;
diff --git a/src/llama-graph.cpp b/src/llama-graph.cpp
index b0da1ae39b0668cd89125b5c5d56a60d7e2c7ac5..6cb9f22dd6db9e8e09bfe864d0ffe7beae0bac1e 100644
--- a/src/llama-graph.cpp
+++ b/src/llama-graph.cpp
@@ -1494,6 +1494,12 @@ bool llm_graph_result::apply_linkcpp_stage(const llm_graph_params & params) {
     const int begin = params.linkcpp_layer_begin;
     const int end   = params.linkcpp_layer_end;
     const int n_layer = params.hparams.n_layer();
+    // The MTP context executes the auxiliary NextN graph entirely on the
+    // tail stage. It is not a slice of the target trunk and therefore has no
+    // inter-stage cut-set to prune or bind.
+    if (params.gtype == LLM_GRAPH_TYPE_DECODER_MTP) {
+        return true;
+    }
     if (begin < 0 || end < 0) {
         return true;
     }
diff --git a/src/llama-model-loader.cpp b/src/llama-model-loader.cpp
index 9a0d4f483e7d39b931ed4b9c9f84d3c3b762d565..9c24601bc384fda876fa6a0b7d8cfb4383f15422 100644
--- a/src/llama-model-loader.cpp
+++ b/src/llama-model-loader.cpp
@@ -1253,7 +1253,11 @@ struct ggml_tensor * llama_model_loader::create_tensor(
                     linkcpp_owned = linkcpp_layer_begin <= tn.bid && tn.bid < linkcpp_layer_end;
                 }
             } else if (info.layer == LLM_TENSOR_LAYER_INPUT) {
-                linkcpp_owned = linkcpp_layer_begin == 0;
+                // The target trunk consumes token embeddings on stage zero.
+                // An MTP tail may also need the stock embedding as a fallback
+                // when the GGUF does not provide a dedicated NextN embedding.
+                linkcpp_owned = linkcpp_layer_begin == 0 ||
+                    (load_mtp && linkcpp_layer_end == (int32_t) hparams.n_layer());
             } else if (info.layer == LLM_TENSOR_LAYER_OUTPUT) {
                 // Auxiliary NextN blocks are part of n_layer_all but not the
                 // normal decode graph. The stage ending at n_layer() owns the
