diff --git a/src/llama-memory-hybrid.h b/src/llama-memory-hybrid.h
index 484eafb74..2c04be3f4 100644
--- a/src/llama-memory-hybrid.h
+++ b/src/llama-memory-hybrid.h
@@ -18,6 +18,8 @@
 
 class llama_memory_hybrid : public llama_memory_i {
 public:
+    static constexpr bool linkcpp_stage_residency_supported = true;
+
     llama_memory_hybrid(
         const llama_model & model,
                             /* attn */
@@ -47,6 +49,8 @@ public:
     // llama_memory_i
     //
 
+    bool supports_linkcpp_stage_residency() const override { return true; }
+
     llama_memory_context_ptr init_batch(
             llama_batch_allocr & balloc,
             uint32_t n_ubatch,
diff --git a/src/llama-memory-recurrent.cpp b/src/llama-memory-recurrent.cpp
index ef82eb976..5d055911e 100644
--- a/src/llama-memory-recurrent.cpp
+++ b/src/llama-memory-recurrent.cpp
@@ -46,6 +46,13 @@ llama_memory_recurrent::llama_memory_recurrent(
     };
     std::map<ggml_backend_buffer_type_t, ggml_context_ptr, ggml_backend_buft_comparator> ctx_map;
 
+    const bool linkcpp_stage = model.linkcpp_layer_begin() >= 0
+        && model.linkcpp_layer_end() >= model.linkcpp_layer_begin();
+    const auto is_stage_resident = [&](int32_t il) {
+        return !linkcpp_stage
+            || (model.linkcpp_layer_begin() <= il && il < model.linkcpp_layer_end());
+    };
+
     // create a context for each buffer type
     auto ctx_for_buft = [&](ggml_backend_buffer_type_t buft) -> ggml_context * {
         auto it = ctx_map.find(buft);
@@ -69,8 +76,21 @@ llama_memory_recurrent::llama_memory_recurrent(
         return it->second.get();
     };
 
+    auto ctx_for_meta = [&]() -> ggml_context * {
+        if (!ctx_meta) {
+            ggml_init_params params = {
+                /*.mem_size   =*/ size_t(2u*n_layer*ggml_tensor_overhead()),
+                /*.mem_buffer =*/ NULL,
+                /*.no_alloc   =*/ true,
+            };
+            ctx_meta.reset(ggml_init(params));
+        }
+        return ctx_meta.get();
+    };
+
     r_l.resize(n_layer);
     s_l.resize(n_layer);
+    resident_l.resize(n_layer, false);
 
     for (int i = 0; i < n_layer; i++) {
         if (filter && !filter(i)) {
@@ -78,11 +98,14 @@ llama_memory_recurrent::llama_memory_recurrent(
             continue;
         }
 
-        const char * dev_name = "CPU";
+        const bool resident = is_stage_resident(i);
+        resident_l[i] = resident;
+
+        const char * dev_name = resident ? "CPU" : "metadata-only";
 
         ggml_backend_buffer_type_t buft = ggml_backend_cpu_buffer_type();
 
-        if (offload) {
+        if (resident && offload) {
             auto * dev = model.dev_layer(i);
             buft = ggml_backend_dev_buffer_type(dev);
 
@@ -91,7 +114,7 @@ llama_memory_recurrent::llama_memory_recurrent(
 
         LLAMA_LOG_DEBUG("%s, layer %3d: dev = %s\n", __func__, i, dev_name);
 
-        ggml_context * ctx = ctx_for_buft(buft);
+        ggml_context * ctx = resident ? ctx_for_buft(buft) : ctx_for_meta();
         if (!ctx) {
             throw std::runtime_error("failed to create ggml context for rs cache");
         }
@@ -709,9 +732,9 @@ size_t llama_memory_recurrent::total_size() const {
 size_t llama_memory_recurrent::size_r_bytes() const {
     size_t size_r_bytes = 0;
 
-    for (const auto & r : r_l) {
-        if (r != nullptr) {
-            size_r_bytes += ggml_nbytes(r);
+    for (size_t il = 0; il < r_l.size(); ++il) {
+        if (resident_l[il] && r_l[il] != nullptr) {
+            size_r_bytes += ggml_nbytes(r_l[il]);
         }
     }
 
@@ -721,9 +744,9 @@ size_t llama_memory_recurrent::size_r_bytes() const {
 size_t llama_memory_recurrent::size_s_bytes() const {
     size_t size_s_bytes = 0;
 
-    for (const auto & s : s_l) {
-        if (s != nullptr) {
-            size_s_bytes += ggml_nbytes(s);
+    for (size_t il = 0; il < s_l.size(); ++il) {
+        if (resident_l[il] && s_l[il] != nullptr) {
+            size_s_bytes += ggml_nbytes(s_l[il]);
         }
     }
 
@@ -874,7 +897,7 @@ void llama_memory_recurrent::state_write_data(llama_io_write_i & io, const std::
     // Get whole range at a time
     for (uint32_t il = 0; il < n_layer; ++il) {
         // skip null layers (read_data will handle this by checking "r_l" and "s_l" for null)
-        if (r_l[il] == nullptr) continue;
+        if (!resident_l[il] || r_l[il] == nullptr) continue;
 
         // Write R tensor type
         const int32_t r_type_i = (int32_t)r_l[il]->type;
@@ -896,7 +919,7 @@ void llama_memory_recurrent::state_write_data(llama_io_write_i & io, const std::
     if (!s_trans) {
         for (uint32_t il = 0; il < n_layer; ++il) {
             // skip null layers (read_data will handle this by checking "r_l" and "s_l" for null)
-            if (s_l[il] == nullptr) continue;
+            if (!resident_l[il] || s_l[il] == nullptr) continue;
 
             // Write S tensor type
             const int32_t s_type_i = (int32_t)s_l[il]->type;
@@ -919,7 +942,7 @@ void llama_memory_recurrent::state_write_data(llama_io_write_i & io, const std::
         const uint32_t mem_size = size;
         for (uint32_t il = 0; il < n_layer; ++il) {
             // skip null layers (read_data will handle this by checking "r_l" and "s_l" for null)
-            if (s_l[il] == nullptr) continue;
+            if (!resident_l[il] || s_l[il] == nullptr) continue;
 
             const uint32_t n_embd_s = hparams.n_embd_s();
 
@@ -1065,7 +1088,7 @@ bool llama_memory_recurrent::state_read_data(llama_io_read_i & io, uint32_t cell
     // For each layer, read the keys for each cell, one row is one cell, read as one contiguous block
     for (uint32_t il = 0; il < n_layer; ++il) {
         // skip null layers
-        if (r_l[il] == nullptr) continue;
+        if (!resident_l[il] || r_l[il] == nullptr) continue;
 
         // Read type of key
         int32_t r_type_i_ref;
@@ -1094,7 +1117,7 @@ bool llama_memory_recurrent::state_read_data(llama_io_read_i & io, uint32_t cell
     if (!s_trans) {
         for (uint32_t il = 0; il < n_layer; ++il) {
             // skip null layers
-            if (s_l[il] == nullptr) continue;
+            if (!resident_l[il] || s_l[il] == nullptr) continue;
 
             // Read type of value
             int32_t s_type_i_ref;
@@ -1124,7 +1147,7 @@ bool llama_memory_recurrent::state_read_data(llama_io_read_i & io, uint32_t cell
         // For each layer, read the values for each cell (transposed)
         for (uint32_t il = 0; il < n_layer; ++il) {
             // skip null layers
-            if (s_l[il] == nullptr) continue;
+            if (!resident_l[il] || s_l[il] == nullptr) continue;
 
             const uint32_t n_embd_s = hparams.n_embd_s();
 
diff --git a/src/llama-memory-recurrent.h b/src/llama-memory-recurrent.h
index b13b7b748..692342266 100644
--- a/src/llama-memory-recurrent.h
+++ b/src/llama-memory-recurrent.h
@@ -16,6 +16,8 @@
 //       see the implementation of llama_kv_cache_context_i for an example how to do it
 class llama_memory_recurrent : public llama_memory_i {
 public:
+    static constexpr bool linkcpp_stage_residency_supported = true;
+
     llama_memory_recurrent(
             const llama_model & model,
                     ggml_type   type_r,
@@ -32,6 +34,8 @@ public:
     // llama_memory_i
     //
 
+    bool supports_linkcpp_stage_residency() const override { return true; }
+
     llama_memory_context_ptr init_batch(
             llama_batch_allocr & balloc,
             uint32_t n_ubatch,
@@ -111,6 +115,7 @@ public:
     // per layer
     std::vector<ggml_tensor *> r_l;
     std::vector<ggml_tensor *> s_l;
+    std::vector<bool> resident_l;
 
 private:
     //const llama_model & model;
@@ -121,6 +126,10 @@ private:
     // ggml contexts for the KV cache along with the allocated backend buffers:
     std::vector<std::pair<ggml_context_ptr, ggml_backend_buffer_ptr>> ctxs_bufs;
 
+    // Nonresident layers still need R/S tensor metadata while the stock full
+    // graph is built. This context is deliberately never backend-allocated.
+    ggml_context_ptr ctx_meta;
+
     size_t total_size() const;
 
     size_t size_r_bytes() const;
