# 2026-09-09 — 포화와 GPU 사용률을 올리려는 실험 10회

종류: 실측 실험. 모델 크기·분할·resident·발행 정책·도착 패턴을 한 번에 하나씩 바꿨다.
응답은 `judge.mjs`가 아니라 **원문을 직접 읽어** 판정했다.
장소는 `m42-server2`(RTX 3090 ×2), 커밋 `6901f236a` 이후의 작업 트리.

숫자는 각 실행의 보존 산출물에서 재계산했다. 재현:

```
node test/benchmarks/p4-4node/measure-run.mjs target/saturation-20260909/<run id>
```

## 실측표

`st`=stage 수, `seq`=resident, `decMn/decMx`=decode batch 행 평균/최대, `fill%`=UBATCH(512) 채움률,
`idle%`=head 유휴가 벽시계에서 차지하는 비율, `ovl2`=stage 2개 이상이 동시에 열린 시간 비율,
`gpu0/1`=stage 실행창 평균 사용률, `zero`=그 창에서 0 %인 표본 수, `W`=평균 전력.

```
run                      scenario                 st seq    TPS   ITL  batch  decMn decMx  fill% idle%  ovl2 depth  gpu0  gpu1  zero0 zero1   W0    W1
20260909T034439Z-4ce8e2b pressure          (2B)    4 256 382.15   692   1367  76.21   160  15.95  59.0  62.2  3.18  21.4  31.9  299/1052  168/1052  98.8 193.8
20260909T055218Z-c9f80e0 pressure_2stage   (2B)    2 256 419.39 590.1   1637  63.44   128  13.32  42.4  53.0  3.81  26.2  32.9   184/959    93/959 111.6 160.0
20260909T060229Z-174791b pressure_35b              2  96 275.35 314.9   2622  39.34    64   8.43  24.7  66.0  2.24  41.5  39.4   28/1469    9/1469 198.4 206.4
20260909T064046Z-7674d59 pressure_35b_4stage_96    4  96 183.00 517.5   2416  42.67    64   9.15  57.0  68.5  2.02  36.8  37.4  452/2207  342/2207 181.2 192.7
20260909T062820Z-57406fe pressure_35b_4stage       4 256 276.62 952.0   1546  67.25   128  14.29  36.0  93.6  3.60  50.7  47.1  169/1461  104/1461 199.9 212.7
20260909T065812Z-e5b6a12 pressure_35b_wide         2 160 289.79 537.3   2430  42.49    64   9.09  21.0  73.0  3.29  44.5  40.9   23/1396    8/1396 198.7 206.9
20260909T071025Z-5cc7fc5   + min-batch-rows 64     2 160 234.58 641.5    879 119.45   160  25.14  55.4   3.0  1.03  27.1  26.6  815/1721  837/1721 175.0 184.4
20260909T072421Z-a198673 pressure_35b_stagger      2 160 223.10 659.9   4526  22.76    52   4.88  16.2  80.8  6.09  30.1  31.0   24/1813    0/1813 160.0 163.0
20260909T073806Z-d565861 pressure_35b_burst 64/2s  2 160 313.80 478.7   2021  51.12    64  10.94  22.9  71.2  2.70  41.8  40.8   16/1289   22/1289 203.4 210.5
20260909T074931Z-8853b3e pressure_35b_burst 128/4s 2 160 302.49 478.4   1929  53.65    96  11.46  22.9  68.0  2.55  43.7  38.7  100/1337   78/1337 197.6 205.4
```

열 개 실행 전부 512 요청 완료·512 해제, `error`·`cleanup_error` 모두 `null`이다.

## 1. 사용률을 정한 것은 모델 크기다

| | 2B | 35B |
| --- | ---: | ---: |
| GPU 창 평균 | 21.4–32.9 % | **36.8–50.7 %** |
| 전력 | 98.8–160 W | **175–212.7 W** |
| 창에서 0 %인 표본 | 9.7–28.4 % | **0–20.5 %** |

2B는 층이 싸서 한 행이 카드를 채우지 못한다. 행 수를 아무리 늘려도 사용률이 따라오지 않는다.
같은 워크로드·같은 카드에서 35B로 바꾸면 사용률이 2.4배(21.4 → 50.7 %)까지 오른다.

## 2. 한 카드에 한 stage. 두 개를 얹으면 33 % 잃는다

resident를 96으로 고정하고 분할만 바꿨다.

| | 2 stage (1/card) | 4 stage (2/card) |
| --- | ---: | ---: |
| 생성 TPS | **275.35** | 183.00 |
| GPU0 / GPU1 | **41.5 / 39.4 %** | 36.8 / 37.4 % |
| head 유휴 | **24.7 %** | 57.0 % |
| stage 시간 | 105.8 / 127.5 ms (20층씩) | 98.3 / 96.7 / 95.9 / 125.1 ms (10층씩) |

10층 stage가 약 97 ms, 20층 stage가 약 117 ms다. **층당 약 2 ms, stage당 고정비 약 77 ms**이며,
네 stage는 한 랩에 그것을 네 번 내고 카드는 둘뿐이라 그중 둘만 겹칠 수 있다.
저장소가 09-04에 적은 "한 카드 한 stage가 35B에서 33 % 빠르다"와 같은 방향이고 더 크다.

따라서 `pressure_35b_4stage`(resident 256)가 사용률 50.7 %로 가장 높았던 것은 **분할 덕분이 아니라
resident 덕분**이다. 4 stage는 stage당 가중치 지분이 절반이라 메모리 계획을 통과할 뿐이다.

## 3. 적재 판정이 최적 구성을 막는다

35B 실행 5회의 계획을 모으면 규칙이 정확히 맞는다. 카드 초기 여유는 22.76 GiB다(단위 GiB).

| arm | n_seq | `CUDA0 RS buffer` | 계획 `free` | free + RS | `required` | 판정 |
| --- | ---: | ---: | ---: | ---: | ---: | :-- |
| 2 stage | 96 | 2,814 MiB = 2.75 | 20.01 | **22.76** | 14.01 | ✓ |
| 2 stage | 256 | 7,504 MiB = 7.33 | 15.43 | **22.76** | 19.72 | ✗ |
| 2 stage(재시도) | 256 | 7,504 MiB = 7.33 | 15.43 | **22.76** | 19.72 | ✗ |
| 4 stage | 96 | 1,407 MiB = 1.37 | 21.38 | **22.75** | 6.96 | ✓ |
| 4 stage | 256 | 3,752 MiB = 3.66 | 19.09 | **22.75** | 10.14 | ✓ |

recurrent 상태 버퍼는 계획을 세우기 **전에 이미 할당**돼 `free`에서 빠져 있는데,
`required`의 `context` 항이 같은 버퍼를 다시 센다. 판정식은
[stage_memory_plan.cpp:107](../../../../../server/src/runtime/stage_memory_plan.cpp)의
`entry.required() <= entry.free`이고 `free`는 컨텍스트 생성 뒤의 `ggml_backend_dev_memory` 값이다.

실효 조건은 `model + compute + 2 × context ≤ 22.76 GiB`가 된다. 이 하이브리드 35B는
**시퀀스당 32.0 MiB**(recurrent, 컨텍스트 길이와 무관. 어텐션 KV는 시퀀스당 5.3 MiB)이므로
2 stage의 resident 상한이 약 160–176으로 깎인다. 실제로 필요한 19.72 GiB는 24 GiB 카드에 들어간다.

**이것은 staged 서버(C++)이며 격리 계약상 다른 계층이라 이 세션에서 고치지 않았다.**

## 4. UBATCH 채움률은 목표 지표가 아니다

같은 구성(2 stage, resident 160)에서 `P4_STAGED_MIN_BATCH_ROWS=64`만 켰다.

| | 기본 | min-batch-rows 64 |
| --- | ---: | ---: |
| **채움률** | 9.09 % | **25.14 %** |
| decode 폭 평균 / 최대 | 42.49 / 64 | 119.45 / 160 |
| 물리 batch | 2,430 | 879 |
| **생성 TPS** | **289.79** | 234.58 |
| **GPU0 / GPU1** | **44.5 / 40.9 %** | 27.1 / 26.6 % |
| GPU p50 | 47 / 39 % | **5 / 4 %** |
| stage 2개 이상 겹침 | 73 % | **3 %** |
| 파이프라인 깊이 | 3.29 | **1.03** |
| `idle_gated` | 0 | 837 |

폭을 모으려고 기다리면 파이프라인이 직렬화된다. 채움률은 2.8배가 됐고 TPS는 19 %,
사용률은 40 % 떨어졌다. **앞으로 "포화"는 채움률이 아니라 카드가 쉬지 않는 정도로 읽는다.**

같은 실행이 고정비를 다시 준다: 20층 stage가 폭 32에서 98 ms, 폭 160에서 278 ms이므로
**행당 1.41 ms, batch당 고정비 약 53 ms**다.

## 5. 폭을 정하는 것은 resident가 아니라 도착 버스트다

resident 96 → 160으로 1.67배 올렸는데 decode 폭 분포는 **32와 64 두 개뿐**이고 최대가 그대로였다.

| decode 폭 | resident 96 | resident 160 |
| ---: | ---: | ---: |
| 32 | 1,996회 | 1,612회 |
| 64 | 594회 | 786회 |
| 96 이상 | **0회** | **0회** |

같은 프롬프트로 함께 도착한 요청은 같은 보조로 decode하고, 다른 버스트는 파이프라인 위상이
어긋나 동시에 발행되지 못한다. 그래서 **동시에 발행 가능한 집합은 버스트 한두 개 크기**다.
도착률을 초당 32건으로 고정하고 버스트 크기만 바꾸면 이것이 직접 보인다.

| 버스트 | decode 폭 평균 / 최대 | 물리 batch | 생성 TPS | 토큰 간격 p50 | GPU0 / GPU1 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 4건 / 125 ms | 22.76 / 52 | 4,526 | 223.10 | 659.9 ms | 30.1 / 31.0 % |
| 32건 / 1 s | 42.49 / 64 | 2,430 | 289.79 | 537.3 ms | 44.5 / 40.9 % |
| **64건 / 2 s** | **51.12 / 64** | **2,021** | **313.80** | **478.7 ms** | 41.8 / 40.8 % |
| 128건 / 4 s | 53.65 / 96 | 1,929 | 302.49 | 478.4 ms | 43.7 / 38.7 % |

**도착을 고르게 펴면 폭이 반으로 잘리고 batch 수가 86 % 늘어 TPS가 23 % 떨어진다.**
버스트를 키우면 기다림 없이 폭이 넓어지고 TPS와 지연이 함께 좋아지지만, 64건에서 포화한다.
resident 160에 128건 버스트는 한 번에 하나밖에 못 들어가 적격 행이 더 늘지 않는다.

`pressure_35b_stagger`가 가장 분명한 증거를 준다: GPU1은 1,813 표본 중 **0 %가 하나도 없고**
tail 점유가 97.2 %인데 사용률은 31 %다. **tail은 쉬지 않지만 GPU 계산에 묶여 있지 않다.**
폭 23행에서는 batch당 고정비가 지배하고 실제 연산이 카드를 채우지 못한다.

## 6. 응답 — 직접 읽고 판정했다

프롬프트는 이 모델 GGUF의 `tokenizer.chat_template`을 읽어 그 템플릿이 `enable_thinking: false`에서
내보내는 문자열과 같게 썼다.

```
<|im_start|>user\n타입스크립트에 대해 한국어로 설명하라<|im_end|>\n<|im_start|>assistant\n<think>\n\n</think>\n\n
```

35B 8개 실행 전부에서:

| 검사 | 결과 |
| --- | --- |
| `<think>` 태그 유출 | **0 / 512** (8회 모두) |
| ChatML 마커 유출 | **0 / 512** (8회 모두) |
| 최다 반복 24자 창 비율 | 0.06–0.11 — 원문을 읽어 마크다운 구조 반복임을 확인, 퇴화 아님 |
| 서로 다른 응답 | 283–417 / 512 |
| 종료 사유 | 512/512 `length` — `max_tokens` 200에서 잘림 |

**싱킹 억제가 실제로 동작했다.** 잘린 응답은 사용자 판단에 따라 정상 응답으로 받아들인다.
한글 비율이 0.45로 2B(0.54)보다 낮은 것은 언어 이탈이 아니라 TypeScript 코드 블록 때문이다
(147/512가 코드 펜스를 포함).

## 이 문서가 주장하지 않는 것

- tail 시간의 내역. 고정비 약 53–77 ms는 stage 시간의 폭 회귀에서 나온 값이며,
  parse·decode·sample·encode 분해는 `P4_STAGED_TRACE_STEP`으로 따로 재야 한다.
- 다른 모델·다른 카드로의 일반화. 특히 시퀀스당 32.0 MiB는 이 하이브리드 모델의 성질이다.
- 다중 물리 컴퓨터 수용. 3090 두 장은 한 호스트다.
- paired A/B의 통계적 유의성. 각 조건 1회씩이며, 반복 없이 인용한 차이는 방향만 읽는다.

## 지금까지의 최선과 다음

**측정된 최선: 35B, 한 카드 한 stage, resident 160, 64건 2초 버스트 —
313.80 생성 TPS, 토큰 간격 478.7 ms, GPU 41.8 / 40.8 %, 전력 203 / 211 W.**

더 올리려면 측정이 가리키는 순서는 다음과 같다.

1. **적재 판정의 중복 계산(3절).** 이것을 고치면 2 stage가 resident 256을 받는다.
   가장 좋은 분할과 가장 넓은 폭을 처음으로 함께 시험할 수 있다.
2. **batch당 고정비(4절).** 폭 32에서 stage 시간의 절반이 고정비다. STEP trace로 그 안을 나눠야
   무엇을 줄일지 정할 수 있다.
3. **적격 시퀀스를 늘리는 발행 규칙.** 기다리지 않고 폭을 넓히는 유일한 남은 길이다.
   `state.rs`의 `phase_within`이 decode에서 `outstanding > 0`을 제외하므로, 파이프라인이 깊을수록
   적격 집합이 줄어든다. 이것을 바꾸려면 요청별 의존성 계약을 먼저 검토해야 한다.
