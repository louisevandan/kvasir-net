# v1.1 통합 진단 — MI250 직렬화와 Hy3 장기 prefill

2026-09-11. 상태: **계획·bounded 선택 구현·두 클러스터 실기 선별 완료**, H5 성능/서비스 승인은 미완료.
§1~5는 최초 분석, §6은 구현, §7은 후속 실기다. 각 절의 당시 검증 범위를 구별한다.
실행 순서는 [로드맵](../../../../../../../docs/distributed-batching-roadmap.md#v11-plan),
판정은 [검증 규약](../../../../../../../docs/distributed-batching-verification.md#v11-gates)이 소유한다.

## 1. 기준과 재현 자료

감사 checkout: `F:/dev/p4`, `11dc7a0ce74460a8b3390d057bb097684cb28c90`, 시작 시 clean.
MI250 agent 기준은 `f3658f1b`, Hy3 adapter/native는 `9ad366f9063e49f2574639d649fe556344992c6e`다.
이 기준들과 감사 HEAD 사이 scheduler/worker/drive/state의 관련 경로 차이를 확인했다.
서로 다른 upstream/backend의 실행 결과를 같은 성능 표본으로 합치지 않는다.

| 자료 | 위치 / SHA256 |
| --- | --- |
| MI250 8-stage 원본 artifact | `F:/dev/p4/target/mi250-deploy/evidence/compare-mi250-02/p4-runs/2026-09-10T10-58-49.735Z-highload-r256-t64-balanced/artifact.json` / `e12d7534ef67f2d03f967d16a4842d7c65dc96b8cc478c560b8ec8bf9278ad67` |
| MI250 16-stage 원본 artifact | `F:/dev/p4/target/mi250-deploy/evidence-16/2026-09-11T00-15-00Z-r16-mixed-p100k-t512/artifact.json` / `0f340cfe686ac3a8c6f3b6e3360353250939411d5df7a47bc2348f58cfcaae38` |
| Hy3 원본 artifact | `F:/dev/p4-fleet-20260910/target/hy3-100k-20260910/hy3-100k-wave-headroom-1789035735558/artifact.json` / `0abc7aa4af40d01389baa2bb20cea0481fd666485f8d7225029db654d674ecc3` |
| 이번 분석 번들 | `F:/dev/p4-releases/v1.1-planning-evidence-20260911.zip` / `922aa9bea4c892ab7b35a379bdb7ebd035ef10385d99af53f41aa2990d37fe19` |
| Hy3 원자료 별도 봉인 | `F:/dev/p4-releases/hy3-100k-20260910-1924.zip` / `5cb312f1109dc500d5fae457a19536ba10064549db66428d2dc37bc6bf047e94` |

이번 번들은 MI250 두 원본 artifact/config/summary, 기존 진단·보고·전체 요약,
`audit/audit.py`, `audit/mi250-audit.json`, Hy3 파생 분석/스크립트를 담는다.
25개 파일을 `MANIFEST.sha256`으로 ZIP 재열람 검증했다. Hy3 원본은 별도 봉인에 있으며 이번 번들에 복제하지 않았다.
`target/` 파일 자체는 영구/다른 머신 증거가 아니므로 두 ZIP을 함께 보존한다.

MI250 native identity: upstream `0eadefebd3f8f92a86d634a0e5b8fffc9dc792c0`,
patch `f37f181c9d38afed04993921b30470dd69d8cfd5d232d05405f384a01439e738`, CPU/ROCm.
Hy3: upstream `434ddbbc0e30522e897670681e503b797c12b7c1`,
patch `ff1468f6187f0e70e2970f73c0d0ca489d7b8bd4465b583ec1b6305cbee56f3b`, CUDA/Metal/CPU.

## 2. MI250: 직렬화 관측은 유지, open-batch 상한 인과는 미확정

모델은 두 실행 모두 Step-3.7-Flash-Q4_K_XL, 45층이다. 아래 TPS는 artifact의
생성 토큰 합계 / `elapsed_ms`이며 적재 성능·정상 응답으로 승인한 유효 TPS가 아니다.

| 조건 | 8-stage / 한 머신 | 16-stage / 두 머신 |
| --- | --- | --- |
| resident / 요청 수 | 256 / 256 | 16 / 16 |
| 요청별 context 상한 | 512 | 100512 |
| 실제 입력 | 총 10160토큰, 요청당 평균 39.7 | 100000토큰 1건 + 61토큰 15건 |
| 출력 상한 / 종료 | 64 / 전부 length | 512 / ignore_eos=true, 전부 length |
| batch / ubatch | 512 / 512 | 512 / 512 |
| 완료 / 해제 | 256 / 256 | 16 / 16 |
| 생성 토큰 / 시간 | 16384 / 54.318s | 8192 / 1072.206s |
| generated TPS | 301.63 | 7.64 |
| 요청 묶음 | 32요청씩 서로 다른 8개 묶음 | 513개 배치는 16요청, 이후 201개는 1요청 |
| idle_gated 합계 | 0 | 0 |

따라서 **301.63 → 7.64를 노드 수 증가의 손실률로 계산할 수 없다.** resident, 입력 길이,
출력 길이, EOS 조건, KV 크기가 함께 바뀌었다. 16건 모두 실제 100k prefill한 실행도 아니다.

16-stage의 전형적 배치는 `497 prefill + 15 decode`(200회)다. 같은 요청의 decode는
이전 토큰 반환 전 발행할 수 없고, 기본 prefill fragment 창도 1이다. 한 배치가 16요청을
모두 점유하면 다음에 발행할 독립 요청이 없어진다. 8-stage의 32요청 묶음 8개와 대조된다.
이는 **요청을 한 묶음에 소진하는 정책과 요청 의존성의 결합**을 우선 검사할 근거다.

원본 `stage-overlap-diagnosis.json`의 평균 0.8618/16, 단일 활성 86.1576%는
호스트별 Unix 시각을 보정 없이 합친 **RPC 구간**이다. GPU 계산 겹침이나 정확한 전역 비행 수가 아니다.
시계를 섞지 않고 머신별 8개 stage를 재집계하면 다음과 같다.

| 머신 | 자체 분석창 | 평균 동시 RPC | 최대 동시 RPC |
| --- | --- | --- | --- |
| MI250-01 | 1071.329s | 0.42535 | 1 |
| MI250-02 | 1065.803s | 0.43877 | 1 |
| 8-stage 비교 실행의 한 머신 | 54.172s | 5.84174 | 8 |

각 머신 내부 직렬화는 이 재계산으로도 유지된다. 다만 현재 `max_open_batches` 기본값은
0(상한 비활성)이며 실제 프로세스의 유효 환경변수는 자료에 봉인되지 않았다.
gate 거절 0과 함께 보면 **open-batch 설정이 차단 원인이라는 선언은 보류**해야 한다.
현재 event adapter에서 `queue.is_running()` 호출도 찾지 못했다. 이름만으로 제거할 대상이 아니다.

머신 간 7→8의 관측 간격 평균은 19.3515ms다. 미보정 전체 batch 지연 1491.438ms와 단순 비교해도
약 1.30%다. 이 값에는 시계 오차·큐·전달이 섞인다. ping RTT나 baseline 차감으로
“실제 네트워크 비용 1% 미만”을 확정하지 않는다. 다른 사용자 부하가 있었던 MI250-01의 GPU 표본도 분리해야 한다.

| 16요청 혼합 arm | batch / ubatch | generated TPS | 100k 요청 TTFT |
| --- | --- | --- | --- |
| 기준 | 512 / 512 | 7.640 | 843.442s |
| 논리 폭 증가 | 2048 / 512 | 6.718 | 972.188s |
| 논리·물리 폭 증가 | 2048 / 2048 | 5.348 | 1269.162s |

각각 단회다. 마지막 비교는 UBATCH 하나만 바꾼 실험이 아니다. 큰 폭이 최선이라는 근거는 없지만
이 세 점으로 최적 폭도 정할 수 없다. 모든 출력이 length이고 사실을 지어낸 응답도 있어 서비스 품질 승인은 없다.
기존 MI250 artifact에는 `output_received_ms`가 없어 실제 토큰 간격 분포도 재구성할 수 없다.

## 3. Hy3: 긴 prefill의 지연 전파와 보관 메모리

Hy3 Q5_K_S 191.884GiB, 80층, 5호스트/6stage다. M42의 3090 두 장, Spark,
Mac Metal, 로컬 3090, Ubuntu 2070에 걸쳤으며 CPU expert 오프로딩을 포함했다.
resident 8, 요청별 context 102400, batch/ubatch 512/256, open-batch 8, fragment 1.
실제 입력 길이 31643/63682/91696, 두 웨이브 총 16건/966748토큰, 출력 상한 8192였다.
6시간 deadline에 16건 전달, 4건 완료·해제, 12건 미완료, OUTPUT 16438건으로 실패했다.
완료 4건도 산술 검사는 통과했지만 서술 품질/분량 조건은 실패해 유효 TPS 승인은 없다.

| 파생 관측 | 결과 / 해석 |
| --- | --- |
| 같은 요청 연속 OUTPUT 간격 | 16430쌍을 decode 입력 위치와 연결, 미연결 0 |
| 초기 prefill 혼합 구간 | 간격 중앙 29.790s; head issue 창 186.65분 |
| decode-only 구간 | 간격 중앙 1.869s; head issue 창 53.60분 |
| 후기 prefill 구간 | 간격 중앙 31.150s; head issue 창 119.65분 |
| decode-only batch인데 20초 이상 출력 공백 | 1963건 모두 이전/현재 head issue 사이에 prefill 포함 batch 존재 |
| 동일 91696토큰 입력의 편차 | 요청 3: prefill 428회, TTFT 186.46분; 요청 6: 240회, TTFT 92.91분 |
| 최초 stage prefill 비용 | native execute 평균 6872.08ms / RPC 평균 6899.91ms; parse 0.734, match 0.484, encode 5.387ms |
| decode 묶음 | 대부분 3요청/5요청 두 묶음. resident 8로 decode 256행 채움은 불가능 |

시간창과 표본 수가 다른 통계를 나눠 TPS를 만들지 않는다. native execute에는 CPU/GPU/전송이
섞여 있어 커널 계산 비용으로 부르지 않는다. prefill은 M42, decode는 Spark의 RPC가 가장 길었다.
tail 한 단계나 배치 행 수만으로 전체 비용을 예측하기 어렵다.

첫 stage가 발행한 payload 합은 10908855372B(10.15966GiB)다. 입·출력 full payload를
두 번 보관하면 약 20.3193GiB이고 Spark agent 최종 RSS는 약 20.546GiB였다.
broker의 full-event receipt 보관과 일치하는 강한 후보이나 heap 귀속을 입증한 값은 아니다.
**활성 flight 제한과 이미 끝난 receipt의 보관 제한은 별도 예산**이어야 한다.

## 4. 코드가 가리키는 수정 경계

| 소유 / 경로(저장소 상대) | 현재 동작과 계획에 주는 제약 |
| --- | --- |
| adapter `src/v2/scheduler.rs::plan_ordinary` | ready decode 각 1행 후 남는 폭을 prefill에 배분. 현재 요청끼리 나누는 것과 누적 대기/서비스 공정성은 다름 |
| adapter `src/v2/node/state.rs::phase_within` | prefill은 fragment 창, decode는 outstanding>0 차단. decode 의존은 유지 |
| adapter `src/v2/node/worker/drive.rs` | issue-row 상한·open/min gate와 issue commit 소유. 작은 행 상한만으로 16 decode를 여러 묶음으로 나누지는 못함 |
| adapter `src/v2/node/worker.rs::stage_request` | native 요청 동기 대기. 동일 context 동시 호출을 허용하는 변경과 actor 응답성을 분리해야 함 |
| native `server/src/server/server_physical.cpp` | 첫 stage physical 실행·캡처 및 후속 physical capsule 처리를 끝내고 전체 결과 인코딩. logical512를 physical256으로 나눈 것만으로 먼저 끝난 절반을 전송하지 않음 |
| adapter frontier/flight/physical_receive | token range·stage prefix·정산·예약 소유. 서로 다른 execution ID 수용은 동일 sequence의 KV 순서 안전성 증명이 아님 |
| `layers/agent/src/event_broker/ledger.rs`, `event_broker/mod.rs` | full Event 보관, 개수 중심 제한. 중립적인 payload byte/receipt 수명 계약으로 수정; core에 KV/llama 타입 금지 |

adapter/native 경로의 공통 접두는 `layers/adapters/llamacpp/staged/`이며 adapter는 그 아래 `adapter/`다.
equal-sequence 경로의 `PREFILL_PATIENCE`를 ordinary 경로의 해결책으로 인용하지 않는다.
backend 호출은 노드당 1개를 유지하고, 다른 stage에 있는 독립 batch의 진행을 별도로 허용한다.
prefill 선행 발행은 입력 토큰을 안다는 이유만으로 안전하지 않다. stage별 KV prefix, 재전송,
recurrent 보조 상태, 취소 중 남은 fragment까지 증명한 뒤 확대한다.

이 진단은 **직렬화·지연·메모리의 공통 수정 근거**다. GPU 포화/최적 batch/처리량 개선율은
선언하지 않는다. 구현과 실기 승인으로 넘어갈 순서는 로드맵 v1.1 계획을 따른다.

## 5. 이번 변경의 검증 범위

문서 5개만 변경했다. 원본 artifact를 다시 읽어 MI250 두 실행의 수치·머신별 RPC 겹침을 재계산했고,
Hy3 파생 분석과 원본 해시를 대조했다. 분석 번들의 25개 member는 manifest로 재열람 검증했다.
`npm run docs-lint`: 92 files clean. `git diff --cached --check`: 통과.
`node tools/scripts/docs-lint.mjs --all`: 214파일 중 122개 오류. 전부
`.cache/llama-pipeline-upstream/0eadefebd3-f37f181c9d38/` 안의 upstream 문서를 안내도 미등록으로 판정한 것이다.
프로젝트 추적 문서 검사와 구분하며 전체 sweep 통과로 보고하지 않는다. 로그는
`F:/dev/p4/target/v1.1-planning-20260911/docs-lint-all.log`에 보존했다.
제품 코드·워크스페이스 시험·원격 배포/실기는 이번 계획 변경에서 실행하지 않았다.

<a id="bounded-implementation"></a>

## 6. 후속 구현 — bounded ordinary 선택 후보 (2026-09-11)

계획 커밋 `974350c27`은 GitHub `main`에 push 확인 후 구현을 시작했다.
아래는 §5의 문서 전용 변경 뒤에 수행한 별도 작업이다. 실제 GPU 배포/성능 승인은 포함하지 않는다.

### 선택 계약과 사용 범위

현재 event worker가 `Scheduler::prepare_plan_with_limits`를 호출한다. 검증된 native issue 수락 뒤에만
policy state를 commit하므로 거부/준비 취소로 공정성 순서를 소비하지 않는다.
기존 KV/flight/원장 승인 경로, decode outstanding≤1, node별 native 호출 직렬성은 유지한다.

| agent 시작 시 환경변수 | 의미 / 기본값 |
| --- | --- |
| `P4_STAGED_DECODE_MEMBERS` | 한 배치의 decode 요청 수 상한 / 0(축 제한 없음) |
| `P4_STAGED_PREFILL_MEMBERS` | 한 배치의 prefill 요청 수 상한 / 0 |
| `P4_STAGED_PREFILL_ROWS` | 한 배치의 prefill 총 행 상한 / 0 |
| `P4_STAGED_PREFILL_ROWS_PER_REQUEST` | 한 요청에서 발행할 prefill 행 상한 / 0 |

모두 0이면 기존 선택 경로를 그대로 사용한다. 하나라도 설정하면 ordinary attention에만 새 선택을 적용한다.
atomic verify/replay가 대기 중이거나 equal-sequence 모델이면 기존 선택을 사용하고 진단에 적용 여부를 남긴다.
값은 선택 상한이며 메모리 예약/credit이 아니다. resident/open/fragment 상한이나 전역 byte 상한을 올리지 않는다.
유효하지 않은 숫자 문자열은 기존 환경설정 방식과 같이 0으로 읽히므로 artifact의 유효값을 확인한다.

예를 들어 resident16의 판별 후보 `(decode_members=4, prefill_members=4, prefill_rows=128,
prefill_rows_per_request=64)`는 16 decode 또는 16 장기 prefill을 한 번에 모두 소비하지 않는다.
이는 최적값/실기 권장값이 아니며, 현재 예산 범위의 실험 후보다. coalescing/open 설정도 함께 봉인한다.

phase별 sequence ID 재개 위치를 보존해 eligible 벡터 길이가 변해도 다음 요청을 건너뛰지 않는다.
prefill에는 선택된 요청 사이의 순환 배분과 총량/요청별 상한을 적용한다. capacity>1이면 대기 prefill에
최소 1행의 기회를 남기고, capacity=1에서는 지속 decode 8회 후 prefill 기회를 부여한다.
이것은 **수락된 선택 기회 기준의 기아 방지**다. 시간 기반 aging·누적 비용 deficit·native 비용 예측은 아직 구현하지 않았다.

### 관측 계약

`BatchObservation.scheduling`은 선택 시점의 유효 설정·적용 여부·open 수·수용 대기 수·outstanding 차단 수·
입력 부재 수·phase별 eligible 요청 수를 보관한다. node 전체 집계이며 요청별 stall trace나 GPU 유휴 원인 확정값이 아니다.
기존 producer의 필드 부재는 `None`이며 0 측정으로 해석하지 않는다. issue가 전혀 나지 않은 구간의 원인은 아직 담지 못한다.
엄격한 구 consumer는 새 필드를 거부할 수 있으므로 producer agent와 event-drive를 같은 수정으로 결속해 배포해야 한다.

`RequestArtifact.output_received_ms`는 run 단조시계 기준 OUTPUT 수신시각이며 terminal을 포함한다.
파싱/승인 전에 시각을 포착하고 모든 검사를 통과한 OUTPUT에만 추가한다. 실패 부분 artifact에서도
outcomes와 같은 길이/순서로 보존한다. 입력 tokenization·수용 대기와 혼동하지 않는다.

### 실제 소비 시험과 판정 범위

`bounded_strategy_*` 6개 중 하나는 실제 `Worker::run` 스레드·event/Frame codec·flight/정산/해제 경로를 사용한다.
native만 독립 대역이다. 3stage/8요청에서 tail 반환 전에 4 issue를 만들고 모든 출력 위치/내용·stage별 KV 해제를 검사한다.
다른 시험은 16 decode의 네 묶음, 16 장기 prefill의 네 묶음(fragment 확대 없이), 혼합 quantum,
eligible 집합 변화, capacity1 기아 방지, 취소된 계획의 정책 불변, atomic/equal 경로 보존을 검사한다.

새 scheduling 필드는 실제 native 호출 전 request state를 독립 수집한 oracle로 전체 값을 대조한다.
과거 골든 JSON은 변경하지 않았다. 기존 cross-run pacing 제외 목록에 새 진단 필드를 분류했으며,
소유권·membership·행·정산·출력 비교는 그대로 유지한다. OUTPUT 수신시각은 실제 driver 소비 경로의
연결 절단/deadline/잘못된 후속 이벤트 세 반례에서 길이·순서·첫/마지막 시각·부분 보존을 검사한다.

### 실행 결과

| 검사 | 최종 결과 |
| --- | --- |
| `cargo test --workspace --no-fail-fast --locked --target-dir target/v11-workspace` | exit 0, summary 58개 합계 **1384 passed / 0 failed / 7 ignored**, filtered 0 |
| staged adapter lib | **521 passed / 0 failed**, 신규 bounded 선택 시험 6개 포함 |
| worker 새 정책 연결 제거 | 소비 경로 1개 실패 |
| bounded selector를 기존 selector로 복원 | 5개 실패 |
| 승인 OUTPUT의 수신시각 보존 제거 | 실제 driver 실패 부분 보존 시험 3개 실패 |
| scheduling 진단 보존 제거 | 실제 run의 pre-native oracle 1개 실패 |
| `npm run docs-lint` | 92 files clean |
| private headers 검사 | 81 files clean; 기존 common source 부채 5개, 이번 변경으로 확대하지 않음 |

변이는 `F:/dev/p4-v11-mutation-20260911` 독립 worktree와 자체 `target/`에서 수행했다.
양성 대조 6/0 및 driver 3/0을 먼저 실행했고, 각 변이의 실제 재컴파일과 기준선과 다른 executable SHA256을 확인했다.
원래 작업 트리에 변이를 적용하거나 실행 중 agent를 종료하지 않았다.

초기 전체 실행은 기존 `target/debug/p4-agent.exe` 잠금으로 빌드 실패해 별도 target으로 전환했다.
이후 새 scheduling 진단을 과거 정적 관측 골든과 비교하던 4개 실패를 독립 상태 oracle로 해결했다.
문서 편집 중 CRLF/LF 혼합으로 docs gate 1개가 실패한 실행도 보존하고, 정규화 뒤 전체를 재실행해 위 최종 집계를 얻었다.
제품 소스 18파일의 해시를 최종 실행 뒤 다시 대조했다. 남은 7 ignored는 실기 시험이며 통과로 계산하지 않는다.

검증 번들: `F:/dev/p4-releases/v11-bounded-strategy-20260911.zip`,
SHA256 `aa65e234b64aa1c5645a03b9e9a6b9298af0164364cc1642413191b30e15e9c2`.
28개 자료를 manifest로 ZIP 재열람 검증했다. 기준 `974350c27`에 적용할 `final-code.patch`,
`final-source-manifest.json`, 전체 실행/실패 로그, 실행 바이너리 해시, 변이 스크립트·결과를 포함한다.
GPU 모델 적재/배포 자료가 아니며 B2/B3·시간 기반 정책·실기 성능 승격의 남은 순서는 로드맵을 따른다.


<a id="dual-cluster-screening"></a>

## 7. 두 클러스터 동시 실기 — 정책 선별 (2026-09-11)

§6 구현 커밋 `d5256af444666daeb2c2f2907e036b6df3af7549`이 GitHub main과 일치함을 확인한 뒤 실행했다.
MI250 두 호스트/16stage와 기존 Hy3 다섯 호스트/6stage를 동시에 가동했다. 두 클러스터의
추론 구간도 겹쳤다. 서로 다른 모델의 TPS를 합치거나 과거 100k 입력의 TPS와 직접 비교하지 않는다.

### 소스·바이너리·고정 조건

- MI250: d5256af44 agent/driver, 기존 f3658f1b native/ROCm, Step-3.7-Flash-Q4_K_XL.
- Hy3: 기존 fleet `1a848a716` 위에 같은 scheduler/worker 선택 변경을 결속한 **`b9deee4ce`**.
  두 소스의 `scheduler.rs`와 `node/worker/drive.rs`는 diff 0이다. physical-wire-v4 호환을 유지했으며
  workspace `cargo test --workspace --no-fail-fast --locked --target-dir F:/dev/p4/target/v11-hy3-tests`
  최종 **1385 passed / 0 failed / 7 ignored**, summary 58개다. 앞선 main의 1384개와 구별한다.
- Hy3 driver와 head(M42)·Spark·로컬·Ubuntu agent는 b9, Mac downstream은 이미 로컬 네트워크 권한이 있는
  기존 **9ad366f9063** agent를 양쪽 arm에서 동일하게 사용했다. SHA256
  `b7bb40ded261dbd6d213156d131cb6912644c75727427198f6d449653f4f3b6c`.
  배치 선택은 head에서 한다. 모든 노드가 새 agent라는 주장이나 main 단독 fleet 호환 승인은 아니다.
- native/DLL은 재빌드·교체하지 않았다. Hy3 backend는 M42 CUDA0/1, Spark CUDA0, Mac MTL0,
  로컬 3090 CUDA0, Ubuntu 2070 CUDA0이며 기존 CPU expert 오프로딩/컷/KV 배치를 유지했다.
- MI250 resident16/context100512/batch512/ubatch512, Hy3 resident8/context102400/batch512/ubatch256.
  MI open 설정0(상한 비활성), Hy3 open8, fragment1. resident·KV·fragment·open 창을 확대하지 않았다.
- 실제 입력은 MI **215토큰 15건+887토큰 1건**, Hy3 **211토큰 7건+883토큰 1건**이다.
  의미 있는 공학 기록/단위 변환/미측정량 구분을 요구했지만, **100k는 context 용량이며 실제 100k prefill이 아니다**.
  동시 1웨이브, 요청당256토큰, temperature0/seed7/ignore_eos, 전부 `length` 조건이다.
  500단어 요구는 이 출력 상한에서 검증하지 않는다. MI 응답의 임의 날짜와 잘린 수식, Hy3의 잘린 보고서도 보존했다.

`arm-invariants.json`은 실행 ID/generation을 제외한 모델·플랜·프롬프트·도착열·sampling·종료조건을 대조한다.
주 비교는 decode cap만 바뀐다. `cap4min4`는 cap4를 고정한 채 기존 conditional min rows를4로 바꾼 진단,
`cpu4`는 native `--threads 4 --threads-batch 4`를 추가한 별도 진단이다. 새 포트는 시작 충돌 방지용이며
모델/워크로드 비교에서 숨기지 않는다. 실제 scheduling snapshot과 agent env를 함께 보관했다.

### 결과

TPS 분모는 `inference.rs::drive`의 **추론 시작부터 전체 완료·해제까지**다. prefill/요청 대기를 포함하고
모델 적재·그 이후 telemetry 대기를 제외한다. 미완 실행은 deadline까지 받은 승인 OUTPUT의 단순 속도이며
완주 TPS가 아니다. ITL은 요청별 연속 OUTPUT 수신시각 차이 전체의 p50/p95다.

| 실행 ID | decode cap/min rows/CPU threads | 승인 출력 / 추론 시간 | generated TPS | 완료/해제 | 실제 ITL p50/p95 |
| --- | --- | --- | --- | --- | --- |
| `v11-mi250-baseline-r1` | 0/0/기본 | 4096 / 143.532s | 28.54 | 16/16 | 0.395/0.529s |
| `v11-mi250-candidate-r2` | 4/0/기본 | 3030 / 900.037s | 미완 (부분 출력 3.37/s) | 0/0 | 4.565/5.045s |
| `v11-mi250-baseline-r2` | 0/0/기본 | 4096 / 137.757s | 29.73 | 16/16 | 0.387/0.466s |
| `v11-mi250-candidate8-r1` | 8/0/기본 | 4096 / 490.437s | 8.35 | 16/16 | 1.829/3.069s |
| `v11-mi250-cap4min4-r2` | 4/4/기본 | 3884 / 900.044s | 미완 (부분 출력 4.32/s) | 0/0 | 3.603/4.085s |
| `v11-mi250-cap4min4cpu4-r2` | 4/4/4 | 4096 / 120.307s | 34.05 | 16/16 | 0.319/0.386s |
| `v11-mi250-baselinecpu4-r2` | 0/4/4 | 4096 / 301.489s | 13.59 (외부 GPU 간섭) | 16/16 | 1.027/1.178s |
| `v11-hy3-baseline-r7` | 0/0/기본 | 2048 / 384.702s | 5.32 | 8/8 | 1.179/1.433s |
| `v11-hy3-candidate-r1` | 2/0/기본 | 2048 / 214.505s | 9.55 | 8/8 | 0.533/0.623s |

완주 행은 해당 요청 전량과 idle UNLOAD가 성공했다. cap4·cap4min4 실패는 deadline 때 여전히 각각
5/4 flight가 있어 UNLOAD가 busy로 거부됐다. 최초 오류·cleanup_error·부분 결과·미관측 수를 보존했으며,
프로세스 강제 정리를 protocol RELEASE/UNLOAD 성공으로 바꾸지 않았다. 실패 요청별 행 수0은 증거 부재 표기다.

Hy3 cap2는 이 한 쌍에서 TPS **5.3236→9.5476(+79.34%)**, ITL p50 **1.179→0.533s**,
p95 **1.433→0.623s**, TTFT p95 **72.555→71.444s**였다. decode 평균 폭7.97→2.00,
발행 전 open 평균0→2.95로 실제 다른 묶음이 진행했다. 각 노드의 최대 동시 RPC는1을 유지했다.
이는 **단회 선별 결과**이며 H5 반복/holdout·정상 EOS 품질·연속 웨이브 승인은 아니다.


**CPU4 대조의 외부 간섭:** 설정상 min rows4·native threads4를 고정하고 decode cap만0→4로 바꿨지만,
후행 기준군(13.5859TPS)에서는 다른 Python GPU 작업이 겹쳤다. 머신A card3/4/6 평균 사용률이
79.34/78.02/84.70%로 뛰고 card3 peak VRAM은 후보의20.71GiB에서49.09GiB로 늘었다.
`gpu-interference.json`에 새 Python PID/첫 관측 시각/메모리 변화와 종료 후 rocm-smi 목록을 결속했다.
따라서 **13.59→34.05를 순수한 배치 개선율로 인용하지 않는다**. per-process GPU 사용률을 당시 수집하지 않아
외부 간섭의 정확한 성능 손실은 미분류다. 후보34.0462TPS의16/16 완료·해제·UNLOAD와 실제 ITL0.319/0.386s는
관측으로 유지한다. 원래 기본 설정의28.54/29.73TPS와도 CPU 설정이 달라 알고리즘 단독 개선율은 아니다.
GPU를 독점할 수 있는 시간대에 CPU 예산을 양쪽에 고정해 재검증해야 한다.

### 사용률과 원인 범위

아래는 각 호스트의 첫~마지막 native RPC 시각 안의 장치 사용률 표본 평균이다.
NVIDIA `utilization.gpu`, Mac `ioreg`의 Device Utilization은 SM 점유율이나 서로 동일한 측정기가 아니다.
로컬의 별도4080과 Mac Renderer/Tiler 지표는 평균에 섞지 않았다. 공유 호스트의 다른 활동도 완전히 제거하지 않았다.

| Hy3 | M42 GPU0 | M42 GPU1 | Spark | Mac Device | 로컬3090 | Ubuntu2070 |
| --- | --- | --- | --- | --- | --- | --- |
| 기준 cap0 | 12.88% | 12.96% | 7.85% | 34.29% | 24.82% | 2.78% |
| 후보 cap2 | 34.50% | 32.98% | 24.11% | 39.43% | 36.49% | 6.23% |

MI250 cap4는 머신별 평균 동시 RPC를 **0.460/0.469→2.486/1.995**로 늘렸지만 deadline에 실패했다.
기본 cap으로 되돌린 A-B-A 대조는28.54→실패→29.73TPS였다. 단순히 겹침을 늘리는 것은 목표 달성이 아니다.

cap4에서 downstream native `decode_us` 평균은 머신A의16행24.08ms→4행309.57ms,
머신B22.61→239.77ms로 늘었다. A는7개 downstream, B는8개이며 head는 별도다.
graph warmup reset은395/396→5364/4971회로 늘었다. 실제 사용 native의 ggml-cuda 구현은
tensor/src의 주소·shape·stride 등이 바뀌면 재준비한다. ROCm도 이 공통 구현의 CUDA 명칭 로그를 쓴다.
그러나 **이것만으로 회귀 원인을 확정할 수 없다**. 폭4를 유지한 cap4min4에서도 reset은324/314회로
줄었지만 여전히 deadline에 실패했다. 그래프 재준비만 제거하면 해결된다는 주장도 기각한다.

cap4min4 실행 말기의 CPU 표본에서 머신A는96 logical CPU에 runnable160~193, user CPU98%,
native당51개 OS thread였다. 이는 CPU 경합 후보이며 그래프 준비·CPU 연산·spin wait를 아직 분해하지 않았다.
CPU4 진단에서는 native당7 thread와 머신A user CPU9~10% 표본을 확인했다. 표본은 짧고 전체 CPU 프로파일은 아니다.
같은 폭4에서 native 평균은 CPU4의20.18/17.89ms로 줄었고 graph reset은352/340회로 cap4min4와 비슷했다.
호스트별 thread 예산과 native 비용을 함께 고려할 근거이며, 보편적인 threads4 최적값을 뜻하지 않는다.

SSH 왕복으로 전·후 clock offset 범위를 보관했다. 호스트별 RPC 겹침과 OUTER 단조시계 ITL은 계산하되,
다른 호스트 Unix 시각을 그대로 합쳐 정확한 전역 겹침/네트워크 홉 비용으로 선언하지 않았다.
GPU 표본 원본·장치별 표본 수/0%/peak 메모리·native 실행 로그와 invalid UTF-8 원바이트를 모두 보존했다.
텍스트 로그의 ASCII 통계만 replacement decode하며 artifact JSON은 엄격한 UTF-8로 읽었다.

### 실패·재현·봉인

Hy3 초기 r1~r6은 방화벽/새 Mac 앱 권한/시작 실패이며 추론 성능 표본에 넣지 않았다.
MI 초기 candidate-r1의 TIME_WAIT 검사 오류, cap4min4-r1·cap4min4cpu4-r1·baselinecpu4-r1의 READY 이전 정지도 보존했다.
이 정지들은 메모리 로그 이후 main futex 대기/보조 stdin pipe 대기와 일치했다. native의 bind 실패 뒤
stdin thread join 경로가 후보지만 당시 bind errno를 수집하지 못했으므로 원인을 확정하지 않았다.
CPU4 후보는 새 포트로, CPU4 기준군은 ephemeral 범위(32768~60999) 밖의22040~22047 포트가 비었음을 검사한 뒤 재시도했다. 실패를 삭제하거나 timeout을 늘려 통과시키지 않았다.

runner·config·prompts·driver/agent/native/DLL SHA256은 번들의 `runtime-binding.json`, `arm.json`,
`config.json`, `MANIFEST.sha256`을 따른다. Hy3 모델6개 shard의 기존 full SHA와 현재 size/mtime 일치도 보관했다.
이것은 이번 실행 중 full hash를 다시 읽었다는 뜻이 아니다. 테스트 중 모델 파일은 수정하지 않았다.
MI 모델은 추론을 모두 종료한 뒤 두 호스트에서122218291488B 전량을 각각 SHA256으로 다시 읽었다.
두 파일 모두 `2703b4e1285647dafa9a155705f0dcc2e6832f5f59db619ea1a3de913675cbd6`로 같고,
hash 도중 size/mtime 변화가 없었다. agent/driver/native/DLL89개 파일도 전·후 SHA256이 같았다.
재현은 번들의 `experiment-v4.mjs`(Hy3), v2/v5/v6(MI cap0/4/8/min4), v8/v10(CPU4)와 각 arm config를 사용한다.
고정 arm 디렉터리를 덮어쓰지 않으며 새 실행 ID/비어 있는 native endpoint를 선택한다.

| artifact | SHA256 |
| --- | --- |
| `v11-mi250-baseline-r1` | `e2c46b38b7a725318e74e8d503df987526faa301c449e7d032ddfa9656cdd27a` |
| `v11-mi250-candidate-r2` | `e1103870ff09d96f4e97373170bb7bd7ea3a7157502edf5cea7ecc4be958bf8d` |
| `v11-mi250-baseline-r2` | `2849c2134e0d4558065aecc614a9fe44c4fb05a7376d865e6437e989d2633f55` |
| `v11-mi250-candidate8-r1` | `48a9146b54825a89af198cd7988f27fea974ddd0b61eba3dbfc2f948747cd574` |
| `v11-mi250-cap4min4-r2` | `546dee55ac38670d4e9391585d6e86ed4aa874a9622dfe95bd1af93050aa4daf` |
| `v11-mi250-cap4min4cpu4-r2` | `f4dd0d5bbc1dc04f9efabcda0a6fd57d74f32890b961b96cc8af1815a2f84a9d` |
| `v11-mi250-baselinecpu4-r2` | `b20b65516b9b58abcc6a347e45f5659dc2b6913f51e410c1f38a79d9b251907b` |
| `v11-hy3-baseline-r7` | `493e10150df4f9ee0f806f6cb92e11d82f1e80f557888f2e805616dd9891b213` |
| `v11-hy3-candidate-r1` | `2c9675ad80c1f3e7ddcde07975bf9552ed667a4ae1ce0914e323aeba71f97e29` |

새 정책 기본값은 계속0이다. 짧은 후보 선별을 실제100k 입력·긴 정상 응답·장기 receipt 메모리 안정,
슬롯 재사용·B2/B3 안전·H5 승인으로 확대하지 않는다. 후속 실행 순서는 로드맵 §0.V1.1을 따른다.
영문256토큰 출력 성공을 과거69~113토큰 한글 UTF-8 오류의 수정·장문 회귀 통과로 해석하지 않는다.

증거 번들: `F:\dev\p4-releases\v11-dual-cluster-batching-20260911.zip`, SHA256 `e358c6997aa5f8744d18caeb3e0b1ddb0f812b977eab3b6ed8757697677c1c49`.
631개 파일을 member별 SHA256과 ZIP 재열람으로 검증했다. 원자료·실패·모델/바이너리 결속·분석 스크립트·Hy3 source tar/bundle·전체 시험 로그를 포함한다.
시험 agent/native/monitor 종료와 원래 Mac binary·M42 방화벽 복원을 확인했다. 원래의 Mac agent와 별도 studio/debug agent는 유지했다.
이번 문서 게이트는 `node tools/scripts/docs-lint.mjs`: 92 files clean, `git diff --check`: 통과다.

<a id="main-integration"></a>

## 8. 단일 main 통합과 공통 실험 구성 (2026-09-11)

사용자는 모델별 브랜치를 장기 운영하지 않고 main 하나를 유지하도록 지시했다.
main `41773651b`와 임시 Hy3 `b9deee4ce`를 merge하여 fleet upstream/메모리/physical-wire-v4 적응을
통합했다. main의 하드웨어 조회, 독립 저장소 경로, Unix facade 링크 수정은 보존했다.
배치 scheduler/worker drive는 `d5256af44`와 동일하다. 충돌은 문서 이력, CMake 링크,
Unix process ID 및 중복 OUTPUT 수신시각 필드를 조정했다. main의 수신 직후 단조시각을 유지한다.
기본 exact-build 검사와 명시적 physical-wire profile은 유지하고, 실제 wire/source 식별이 다른 구성을
호환된 것으로 승인하지 않는다. 과거 fleet 실행 이력은 main의 최신 실행 순서 뒤에 보존했다.

`test/benchmarks/cluster-inference/`의 공통 구성기는 모델 템플릿, 클러스터의 저장된 placement,
runtime source/binary manifest, 배치 정책, 의미 있는 워크로드를 독립 입력으로 받는다.
모델 이름으로 소스 브랜치를 선택하지 않는다. 두 모델 템플릿과 decode2/open8,
decode4/min4/CPU4 정책, 긴 한국어 보고서 8/16요청 워크로드를 제공한다.
CLI는 실제 OUTER config와 prompt SHA256/토큰 결속 manifest를 생성한다.
배포·agent lifecycle·전체 wall timeout은 별도 cluster runner 책임이며, 이 구성기가 배포까지
일반화했다고 주장하지 않는다. 이번 Hy3 긴 실행의 config를 공통 profile로 정확히 재구성했다.

통합 검증:

- `cargo test --workspace --no-fail-fast --locked -j 4`: 58 summary, **1389 passed / 0 failed / 7 ignored**.
- 공통 composer·build script·compat manifest Node 시험: **21/21**. composer 자체는 **4/4**이며 실제 CLI 소비 경로를 포함한다.
- context 초과 거부 및 기존 arm 덮어쓰기 거부를 각각 제거한 독립 Node 소스 복사본: 각 **1개 시험 실패**.
  Node는 매회 해당 소스를 직접 실행했고 소스 SHA256과 로그를 보관했다. Rust/native 변이로 세지 않는다.
- 통합 main C++를 `434ddbbc0`/patch `ff1468f6` prepared source로 CPU Release 재빌드: **CTest 15/15**.
  CUDA/Metal/ROCm 재빌드·실기 승격 증거가 아니며, 선택 모델 파일이 없는 시험의 통과를 실제 모델 추론으로 확대하지 않는다.
- compat manifest/classification: 26 patches valid. private headers: 83 clean.
- docs-lint: **94 files clean**. 작성한 소스/문서의 whitespace 검사 통과.
  가져온 patch의 context 공백과 raw evidence의 CRLF/후행 공백은 byte/hash 보존 대상이므로 전체 diff의 whitespace 경고를 삭제하지 않았다.

검증 원자료는 `target/v11-long-20260911/`의 `main-workspace.log`, `main-native-cpu.log`,
`main-node-tests.log`, `main-composer-final.log`, `suite-mutations.json`, `composition-equivalence.json`에 있다.
이번 진행 중인 GPU arm은 기존 b9deee4ce/9ad366f90 agent와 기존 native를 유지한다.
그 실기를 통합 main 바이너리의 GPU 승인으로 표시하지 않는다. 통합 merge `429e057de`를 push한 뒤
병합된 임시 branch ref를 정리했다. 로컬·GitHub branch는 main 하나이며 기존 worktree는 동일 commit으로
detach하여 보존했다. 측정 commit은 merge ancestry와 증거 번들로 보존한다. 이후 개발·실험 문서는 main에만 반영한다.

<a id="long-output-20260911"></a>

## 9. 긴 입력·긴 출력 동시 실기와 단일 MI250 대조 (2026-09-11)

사용자 승인 시간은 11:21:11~13:21:11 KST의 총 2시간이며, 추론 cutoff는 13:09:11이다.
MI250-A의 별도 Python GPU 작업은 중단하지 않았다. 사용자 지시에 따라 비점유 MI250-B 한 호스트의
8개 GCD/8stage를 Hy3 다섯 물리 호스트/6stage와 병행했다. MI250 두 호스트/16stage 결과로 표시하지 않는다.

### 고정 조건과 재현

- Hy3 Q5_K_S: M42 CUDA0/1 → Spark CUDA0 → Mac MTL0 → 로컬3090 CUDA0 → Ubuntu2070 CUDA0.
  기존 cut/CPU expert 오프로딩/KV 배치를 유지했다. context102400/sequence, resident8, 총819200,
  batch512/UBATCH256, decode cap2/min0/open8/prefill fragment1이다.
  **실제 입력은 7×2870 + 1×42154 = 62244토큰**이다.
- Step3.7 Q4_K_XL: MI250-B의8stage, context100512/sequence, resident16, 총1608192,
  batch/UBATCH512, native threads/threads-batch4, min rows4, fragment1이다.
  후보 decode cap4, 기준 cap0이며 그 밖의 정책·모델·CPU·입력·출력 조건은 같다.
  **실제 입력은 15×2885 + 1×42413 = 85688토큰**이다.
- 두 모델 모두 한국어 합성 정비 기록을 읽고 지정한4개 기록의 전력·에너지·압력 경보를 계산한 뒤
  관측과 가설을 구분하는 보고서를 요청했다. max4096/min1024 출력, ignore_eos=false다.
  요청별 prompt SHA256, 원문, 실제 토큰 수, 응답 전문을 보존했다. **100k는 context 용량이며 실제100k prefill은 미실행**이다.
- MI250 후보 첫 시도 `v11-long-mi250-single8-candidate-r1`은 B에 driver가 없어 exit127로 끝난
  준비 실패다. 추론 표본에 넣지 않았다. A와 동일 SHA256의 driver를 B에 복사한 r2부터 추론했다.
  native/agent를 교체한 것은 아니며 초기89개 파일은 동일하고 B driver1개가 추가됐다.
- MI 소스 `d5256af444666daeb2c2f2907e036b6df3af7549`, Hy3 head/신규 agent 소스
  `b9deee4ce9c9445b21076269053f9598c62c57e2`, 기존 Mac agent `9ad366f9063`을 사용했다.
  Hy3 native upstream434ddbbc0/patchff1468f6, MI native upstream0eadefeb/patchf37f181c는 서로 다르다.
  **통합 main429e057de의 신규 GPU 빌드 승인으로 표시하지 않는다.**

원자료 루트는 `target/v11-long-20260911/`이다. 재현 runner는 `experiment.mjs`(Hy3),
`experiment-single8.mjs`(MI 후보), `experiment-single8-baseline.mjs`(MI 기준)이다.
각각 `run hy3 candidate 1`, `run mi250-single8 candidate 2`, `run mi250-single8 baseline 1` 인자를 사용했다.
고정 실행 ID/endpoint를 덮어쓰지 않고 새 ID·비어 있는 endpoint·새 공통 deadline을 먼저 봉인해야 한다.
공통 composer로 두 클러스터의 실제 config를 정확히 재구성한 `composition*-equivalence.json`도 포함한다.
runner의 baseline arm metadata 중 공통 `purpose/changed_axis` 문구는 후보용 잔존 문구다.
비교 의도와 변경 축의 소유자는 실행 전에 작성한 `paired-single8-plan.json` 및 실제 config/env diff다.

### 완료와 품질은 별도 판정

TPS 분모는 모델 LOAD를 제외한 **추론 시작부터 완료·해제까지**, prefill과 모든 대기를 포함한다.
빈 EOS 종료 frame은 생성 토큰에서 제외한다. 아래 값은 프로토콜 승인 OUTPUT의 처리량이며
내용 품질을 통과한 유효 생성 TPS로 승격하지 않는다.

| 실행 ID | 생성 토큰 / 추론 시간 | generated TPS | 완료/해제 | EOS/length | 독립4개 계산 행 검증 |
| --- | --- | --- | --- | --- | --- |
| `v11-long-hy3-candidate-r1` | 18809 / 3834.099s | 4.9057 | 8/8 | 8/0 | 8요청 통과 /0실패 |
| `v11-long-mi250-single8-candidate-r2` | 51990 / 1315.775s | 39.5128 | 16/16 | 14/2 | 2요청 통과 /14실패 |

두 후보 모두 inference error/cleanup_error/evidence_missing은 null이며 idle UNLOAD까지 통과했다.
Hy3 built-in judge는8/8, MI는14/16이지만 독립 계산 및 전문 검토가 판정을 제한한다.
MI의 length 종료는 요청 index6/7이며, index7은 요구한 결론도 없다.
Hy3 모든 응답은 계산 행을 읽었고 index7은 전문을 확인했다. 일부 응답이 인과 증거 부재를
인과관계 부정으로 확대하고 중국어 단어를 섞었다. 계산8/8을 전체 문장 품질 승인으로 읽지 않는다.
MI16개 첫 계산 표를 모두 읽고 index0/5/15 전문을 확인했다. index15의 R0174는 정답81.536W/570.752Wh를
81.34W/569.38Wh로 적는 등 실제 오답이 있다. 표 통과2개도 본문의 위험/온도 추정 또는 순위 설명에 문제가 있다.
동일 prompt의 monolithic/native 품질 기준선이 없어 오류를 배치 알고리즘 탓으로 확정하거나 분산 경로를 면책하지 않는다.

`arithmetic-review.py`는 원 측정값으로 기대값을 다시 계산한다. 요청은 Markdown 표를 강제하지 않으므로
표와 `R0001:` 형식 계산 행을 모두 읽는다. 표현 형식만 확장했고 기대값·반올림 허용치는 완화하지 않았다.
파싱 불가0이며, 이 검증은 선두4개 계산 행 범위다. 후속 본문/모든260개 기록의 진실성을 보증하지 않는다.
두 후보 artifact는 strict UTF-8이며 응답 U+FFFD는0이다. 과거69~113토큰 오류 입력의 직접 재현·수정 증거는 아니다.

### 대기·배치와 장치 사용률

| 후보 | TTFT p50 / p95 / max | 실제 ITL p50 / p95 | 모든 요청 첫 출력 전 ITL p50 | 이후 ITL p50 |
| --- | --- | --- | --- | --- |
| Hy3 | 723.851 /1701.346 /2227.689s | 0.578 /0.705s | 19.559s | 0.578s |
| MI250 8stage | 190.700 /239.781 /387.024s | 0.204 /0.360s | 2.669s | 0.203s |

ITL은 요청별 평균의 분포가 아니라 실제 연속 OUTPUT 수신시각 차이의 분포다.
모든 요청 첫 출력 전/후 구분은 관측 구간 분리이며 요청이 그동안 항상 eligible했다는 뜻은 아니다.
Hy3 mixed77/prefill167/decode9656 batch, MI mixed77/prefill91/decode13008 batch다.
MI의 mixed batch는 평균511.740행이고 decode는4행, 나머지는 prefill이다.
decode-only 평균3.973행, 발행 전 open 평균2.454/최대3, 단일 호스트 평균 동시 RPC2.182/8·최대4다.
각 노드 동시 RPC 최대1을 유지했다. UBATCH 채움률 또는 RPC 겹침만으로 포화를 선언하지 않는다.

장치별 첫~마지막 native RPC 구간의 표본 평균/0% 표본 비율은 다음과 같다.
NVIDIA utilization.gpu, ROCm GPU use, Mac Device Utilization은 동일 계측기가 아니며 SM 점유율이 아니다.

| Hy3 장치 | 평균 | 0% 표본 비율 | 표본 수 |
| --- | --- | --- | --- |
| M42 GPU0 / GPU1 | 31.67 /29.01% | 28.62 /28.28% | 각297 |
| Spark | 25.05% | 59.12% | 1854 |
| Mac Device | 28.64% | 52.66% | 1842 |
| 로컬3090 | 23.06% | 48.46% | 1787 |
| Ubuntu2070 | 7.78% | 51.29% | 1821 |

MI 후보8장 평균 사용률 범위6.39~10.44%, 0% 표본73.91~82.21%, 장치당506표본이다.
peak VRAM은22.36~28.98GiB다. 실행 전체534개 GPU 프로세스 표본에서 해당 native 외 점유를 찾지 못했다.
이는 표본 사이까지 GPU 독점을 증명하지 않는다. `/usr/bin/rocm-smi`와 `/opt/rocm/bin/rocm-smi`의
card 번호가 달라 PCI/unique ID 대응표를 보관했다. 주 계측은 `/opt/rocm`7.2.4이며 두 번호를 혼합하지 않는다.

MI 동일 호스트의 이전 stage forward 확인→다음 worker 진입 간격을 분석했다.
decode의 양수 간격 합370.209s 중 receiver의 prefill 포함 RPC와 겹친 합303.476s,
decode RPC와 겹친 합34.733s, 미분류32.000s다. **대기 표본을 합한 값이며 전체 wall 시간 비율이 아니다.**
이 구간은 복사·routing·mailbox·스케줄링을 포함하므로 네트워크 지연으로 부르지 않는다.
`physical.rs`의 ingress는 dequeue 뒤 worker 내부이고 head ingress도 demand 선택 뒤다.
따라서 ingress→RPC 시작을 queue 대기로 라벨링하지 않고 preparation으로 정정했다.
노드 간 clock 범위 없이 Hy3 전역 RPC 합집합이나 홉 비용을 확정하지 않았다.

### 메모리 수명과 다음 수정의 근거

MI 후보 agent의 sampled peak RSS는 **30.934GiB**였고, UNLOAD/driver 종료 뒤에도60개 표본에서
**30.645GiB**가 남아 agent 종료 때까지 유지됐다. native 누적 encoded response는29.892GiB다.
수치 규모와 현재 broker 소스는 receipt 보관을 유력한 원인으로 가리키지만, 누적 traffic과 live heap은 다르다.
allocator retained page도 가능하므로 heap 귀속을 확정하지 않는다.
`entrypoints/agent/src/event_runtime/mod.rs`의 duplicate window는262144건,
`event_broker/mod.rs`의 성공 dispatch는 전체 Event를 clone하고 ledger는 Arc<Event>를 건수로만 퇴역시킨다.
adapter PHYSICAL replay의 별도64MiB 한도를 이 broker 한도와 혼동하지 않는다.
agent `/proc`의 VmRSS/HWM은 유효하지만 probe의 main-thread `native_children`은 다른 thread의 자식을 놓친다.
따라서 빈 목록을 UNLOAD/프로세스 회수 근거에서 제외했다. 종료 시 cmdline guard에 의한 observer exit1은
추론 실패가 아니며 실제 artifact와 최종 process audit로 정리를 판정한다.

다음 구현 순서는 로드맵 §0.V1.1을 따른다. 첫째 live/retired receipt byte와 enqueue/dequeue 대기,
head 단조시계 issue→settle·미발행 사유를 계측한다. 둘째 B2/B3 예약과 backend 중립 broker receipt 퇴역을
중복/replay/결과 불명 의미를 보존하며 고친다. 셋째 기존 bounded 정책에서 prefill quantum64/128/256과
decode 서비스 예약·aging을 단일 축으로 비교한다. tail의 min4 묶음 대기는 active 요청 감소와 구분해
bounded coalescing deadline을 평가한다. node 실행1/decode outstanding≤1은 유지한다.
resident/open/fragment 확대는 byte 예산·취소/순서 안전성 검증 뒤다. 같은 prompt의 품질 기준선도 필요하다.
이번 결과는 H5 반복/holdout·서비스 SLO·실제100k 입력·연속 웨이브·장기 메모리 안정 승인이 아니다.

### 동일 MI250-B 8stage 기준 실행과 마감

후보가 전량 완료/해제하고 추론 cutoff까지30분 이상 남으면 같은 조건의 cap0 기준군을 실행한다는
`paired-single8-plan.json`을12:20:16 KST에 작성했다.12:36:21에 조건이 충족되어 시작했다.
모델·prompt·CPU4·min4·context/resident·fragment는 같고 **decode cap4→0만** 바꿨다.
native endpoint는 이전 bind 정지와 구분하기 위해 새 포트22080~22087을 사용했다. 동일 바이너리다.

`v11-long-mi250-single8-baseline-r1`은13:09:11.001 KST의 공통 wall cutoff로 중단됐다.
**47218생성 토큰 /1938.953s 관측, 완료2/16·해제2/16, EOS2·미종료14**다.
부분 수신률24.3523 token/s는 완주 TPS가 아니며, 후보39.5128과 나눠 개선율로 인용하지 않는다.
최초 오류는 `inference observation evidence Missing { requests: 14, stage_executions: 0 }; receive failed: event stream ended mid-frame`,
cleanup_error는 `Broken pipe (os error 32)`다. 예정된 시험 종료로 연결을 닫은 실행이며 자연 발생 timeout 회귀로 분류하지 않는다.
수신한 OUTPUT·완료·해제는 보존했다. 증거 미완 때문에 요청별 row count는0이고 입력을 실행하지 않았다는 뜻이 아니다.
프로세스 회수는 확인했지만 전체 RELEASE/UNLOAD 통과로 바꾸지 않는다.

| 동일 호스트 비교 | 기준 cap0 | 후보 cap4 |
| --- | --- | --- |
| 16요청 모두 첫1024개 생성 토큰을 받기까지 | 940.498s | 591.322s |
| 관측 구간 실제 ITL p50/p95 | 0.533/0.583s | 0.204/0.360s |
| 모든 요청 첫 출력 이후 ITL p50/p95 | 0.532/0.568s | 0.203/0.334s |
| TTFT p95 | 242.109s | 239.781s |
| 호스트 평균 동시 RPC / 최대 | 0.958/1 | 2.182/4 |
| 장치별 평균 사용률 범위 | 2.18~4.84% | 6.39~10.44% |
| 선두 계산 검증 | 1/16 통과,15오류 | 2/16 통과,14오류 |

첫1024 진단은 후보를 본 뒤, 기준군이1024에 도달하기 전12:47:38에 추가 선언했다.
원 워크로드의 min1024를 사용했고 두 arm 모두 이 prefix를 전량 받았다. **37.13% 짧아진 것은 이 공통 출력
prefix 도달 시간**이며 전체 정상 응답 TPS 개선율 또는 사전 H5 주지표가 아니다.
ITL/RPC/사용률 전체 구간은 기준군이 cutoff에서 잘려 길이와 요청 감소 구성이 다르다. 반복/holdout 승인으로 쓰지 않는다.
기준 GPU는 RPC 구간751표본, 전체764표본에서 해당 native 외 GPU process를 찾지 못했다.
두 후보와 기준의 응답 모두 strict UTF-8이며 replacement character0이다.

기준16개 선두 표를 모두 직접 확인했다. 표의 열 순서·Asset 열·ohm 표기가 달라 header와 단위로 파서를 보완했다.
기대값은 입력 측정값에서 Decimal로 재계산했고 precision 허용치는 유지했다. 파싱 불가0이다.
예: index1의2.542W/5.084Wh는2.541W/5.082Wh와 다르고, index10은12W/132Wh를120W/1320Wh로 적었다.
기준도 이미 수신한 본문에 산술 오류가 있어 후보의 품질 문제를 배치 cap만의 원인으로 확정할 수 없다.
기준군은 대부분 본문이 잘렸으며 전체 문장 품질은 검증하지 않았다.

### 최종 결속

agent/driver/native/DLL90개 파일의 전후 SHA256·크기는 **90/90 동일**이다.
MI250-B 모델122218291488B 전량을 추론 종료 뒤 다시 SHA256으로 읽었고,
`2703b4e1285647dafa9a155705f0dcc2e6832f5f59db619ea1a3de913675cbd6`와 일치했다. hash 중 metadata도 같았다.
Hy3 모델6개 shard는 기존 full hash와 현재 size/mtime 결속이며 이번에 전량을 다시 읽었다는 뜻은 아니다.
최종 process audit에서 시험 native/driver/전용 agent 종료를 확인했다. 원래 Mac GUI agent/바이너리,
각 호스트 studio agent를 유지했다. M42 방화벽은 기존 enabled/allow 상태이며 이번에는 변경하지 않았다.
MI250-A의 외부 Python 작업을 종료하지 않았다. MI250-B8장 VRAM은 종료 뒤 각약12.5MiB로 돌아왔다.

| artifact | SHA256 |
| --- | --- |
| `v11-long-hy3-candidate-r1` | `1010986ce77a9e0b6ac0b973c4e00794b2db7212713712248eb7189ab45fd007` |
| `v11-long-mi250-single8-candidate-r2` | `3094d52cd9140f73a7724c110e48ec033233b9ae4d2777a6f880c590fe3dd1bb` |
| `v11-long-mi250-single8-baseline-r1` | `2a506720b2b7ef05bd2059fba1ba19a5c086d029bc814d8aeb8fcc1bf9fb968a` |

증거 번들: `F:\dev\p4-releases\v11-long-cluster-main-20260911.zip`.
SHA256: `a40370c0ad9a40301e235718c55994bcaa82c31135a0edd14c119913367656e5`.
13:17:17 KST에304개 member와 root manifest를 ZIP에서 재열람해 SHA256을 검증했다.
4개 arm별 manifest, 원문·응답·부분 실패·계측·분석/실행 스크립트·runtime/model 결속과 source archive를 포함한다.
main 최종 source archive 기준은 `c559e3d55`이며, 이 봉인 문구를 추가한 뒤의 차이는 문서뿐이다.
Rust1389/0/7·Node21/21·CPU Release CTest15/15는 통합 코드 검증이고 GPU arm의 원 소스 식별을 대체하지 않는다.
최종 docs-lint94 clean 및 이번 문서 diff whitespace 검사를 통과했다. 첫 ZIP의 root manifest CRLF 비교 실패는
별도 실패 ZIP과 `seal-attempts.json`에 보존했으며, 다시 만든 위 번들만 봉인본이다.

## 10. 재개: 수용 입력의 저장 수명 (2026-09-11)

기준 `0cf373d83`, 검증한 code/test index tree `c1f91f3b56ac65aaa3bb89c23662509744fe04b1`.
이 절은 새 작업이며 앞의 두 봉인 ZIP을 변경하지 않는다. 새 원자료는
`F:/dev/p4/target/v11-prefill-20260911/`에 있으며 아직 전체 작업의 최종 증거 번들이 아니다.

`node/request_budget.rs`와 실제 PREFILL handler가 pending/active 입력의 count·capacity footprint·
prompt token·max output token 합계를 예약한다. 실패 시 신원/slot/원장/기존 예약이 보존된다.
공유 provenance가 active map보다 오래 살면 예약도 살아 있으며 마지막 input의 파기 뒤 반환된다.
미래 반환 바이트, native/transport/receiver 저장소, KV, broker receipt는 아직 이 예약 밖이다.
상세 소유 계약은 [배치 경계](../../../../../../../../docs/adapter-batching-layers.md#불변-수용-입력과-가변-진행-후보)가 소유한다.

- `request_storage_refuses_each_aggregate_axis_before_admission_and_preserves_retries`:
  원본 wire를 소비하는 PREFILL 경로에서 네 한도 각각을 초과, side effect 없음과 같은 ID 재시도,
  map 제거 뒤 공유 input이 있을 때의 계속 거부 및 마지막 참조 뒤 예약 반환을 검사한다.
- `request_storage_actual_loop_retires_after_completion_and_slot_reuse`:
  실제 Worker loop 3stage·resident8·completion1에24요청을 제출한다. pending 포함 예약을 확인하고
  완료/해제/슬롯 재사용 뒤 worker가 살아 있는 동안 input 계정 0을 확인한다. native는 fake다.
- `cargo test --workspace --no-fail-fast --locked -j 4`: **1391 passed / 0 failed / 7 ignored**,58 summaries,
  최종 exit0. `workspace-request-budget.log`. ignored native 시험은 이 합계에 실행된 것으로 세지 않는다.
- 독립 detached copy와 별도 build directory에서 `admission-bypass`는1실패,
  `claim-leak`는2실패(exit101). 회차별 실제 재컴파일과 source/binary SHA는
  `request-budget-mutations.json`, 반례 원문은 `mutation-*.log`에 보존했다.
  binary SHA는 각각 `0fdd259d3b7099410927a8aae298d605c69ea6fa820b417c76dccc9e2d422d61`,
  `831601b168a2ed5321b48bdcb966b406446941731fb1f034df2aa968746e8cc8`이다.

최신 upstream `451b89bae`는26패치 clean replay·manifest·분류 통과다. CUDA/ROCm Release 빌드는
이 단위의 검증과 독립 진행 중이며 완료/모델 실행 승인으로 표시하지 않는다. B2/B3 전체, 배치 성능,
100k 입력 실기, 두 클러스터 재시험은 아직 이 단위로 닫히지 않는다.

## 11. 최신 native 및 독립 prefill 묶음 (2026-09-11)

§10 뒤에 완료한 별도 단위다. native source는 `0cf373d83`, upstream은
`451b89bae0c4b1dd612eb503ceace906c01ddcc9`, 26 patch digest는
`7b9423e1917921ef2d9f0556f996a6314371c7fc55739ac241b4035a7318afac`다.
동일 source archive SHA256은 `ad10de776f549a67d49ee24b71a9b94673f19c1b154b315c82fa0b89b726b330`.

| 실제 실행 | 결과 | 증거 파일 (`target/v11-prefill-20260911/`) |
| --- | --- | --- |
| Windows CUDA sm_86 Release | CTest16/16, exit0 | `build-cuda.log`, `native-cuda-binding.json` |
| MI250-B ROCm gfx90a Release | CTest15/15, exit0 | `mi-native-full.log`, `mi-native-runtime.sha256` |
| Qwen2.5-1.5B Q8_0 실제 CUDA0 | 1/1, 26.48s, 출력 ` Hello`, KV 저장/복원·UNLOAD | `small-cuda-runtime.log` |

Windows server SHA256은 `eebee4adfdbf1172bfa161de63dd9e235cebd6ffbb38230360bd82f2d15197d1`,
MI server는 `76b57f7a962eadd6c33d4c8964192400c01711cfef446162e811102681df266e`다.
Windows에서는 재링크된 `bin/*.dll`을 배포 위치에 다시 복사하고 해시를 기록했다.
소형 모델 SHA256은 `7185d306cf45956c8c017cd0d3b05ecc6bc18b3ea8eb5c240dce40e87563db7f`.
이는 최신 호환 계층의 간단한 CUDA 동작이며 정상 장문 응답·분산 성능 승인이 아니다.

### 정책의 근거와 적용 범위

[llama.cpp update_slots](https://github.com/ggml-org/llama.cpp/blob/451b89bae0c4b1dd612eb503ceace906c01ddcc9/tools/server/server-context.cpp)는
생성 슬롯을 먼저 구성하고 continuous batching일 때 남은 행으로 prompt를 처리한다.
[vLLM V1 scheduler](https://github.com/vllm-project/vllm/blob/5c642796e871e41ae75be0b402a8eaa44ee771b1/vllm/v1/core/sched/scheduler.py)는
실제 token budget과 KV 할당을 연결하며 long-prefill 한도를 둔다.
[SGLang PrefillAdder](https://github.com/sgl-project/sglang/blob/a713349faf748b854ec30895abee7fd26789c6d9/python/sglang/srt/managers/schedule_policy.py)는
혼합 decode를 chunk 예산에서 공제하고 KV/page 경계를 검사한다.
[Sarathi-Serve](https://arxiv.org/html/2403.02310v1)는 chunked prefill과 decode 혼합의 처리량/지연
절충을 다룬다. 이 원리가 특정 P4 모델의 최적 폭이나 pipeline window를 증명하지는 않는다.

현재 P4는 upstream server의 슬롯 스케줄러를 호출하지 않는다. 기존 ordinary scheduler의
decode 예약·회전 fairness·prepare/validate/commit 경로 앞에 adapter 소유 `PipelinePolicy`를 붙였다.
phase별 참여 요청 상한을 `ceil(eligible / (window - open))`으로 유도하고, 기존 명시 한도가
더 작으면 그것을 지킨다. 16개 긴 prompt·window8·batch512에서는 fragment1을 유지하며
2요청×256행의 독립8묶음을 준비한다. 모든 요청을32행씩 한 batch에 쓰고 한 바퀴를 기다리는
이전 pure-prefill 반례를 고친 것이다. 전체 행 수와 참여 요청 수를 혼동하지 않는다.

decode 상태의 요청이 하나라도 있으면, 그 토큰이 현재 비행 중이어도 prefill 총 행을
`P4_STAGED_MIXED_PREFILL_ROWS`(실험 시작값128)로 제한한다. 실제 ready decode는 기존 scheduler가
예약한다. 이는 비선점 호출의 정적 작업량 한도이며 비용 예측/실시간 지연 보장이 아니다.
`P4_STAGED_PIPELINE_BATCHING=1`일 때만 적용하고 finite open window·positive quantum·fragment1을
실제 수용 경로에서 요구한다. ordinary attention 대상이며 recurrent/equal·VERIFY/REPLAY는 기존 경로다.
새 정책은 요청 저장 예약 밖의 B3/edge/receiver/broker receipt까지 예약했다고 주장하지 않는다.

### 반례와 검증

code/test index tree `6dfb4a205591e984e21b44270f75571d8d813dd7`.
unit3개는 독립8묶음·비행 중 decode의 quantum·기존 작은 한도 및 invalid window를 검사한다.
소비 시험 `pipeline_policy_actual_loop_fills_independent_prefills_and_bounds_mixed_work`는 실제
8stage Worker loop의 tail을 보류하고, 최초 반환 전에 서로 다른 요청의 full-width4배치가
발행됨을 확인한 뒤 전량 완료/해제한다. 재개 뒤 혼합 prefill quantum도 검사한다.
`pipeline_configuration_refuses_before_request_reservation_or_native_tokenization`은 invalid 설정
거부가 native tokenization·입력 예약·신원/slot 변경 전에 일어나고 올바른 재시도가 됨을 확인한다.
두 소비 시험의 native는 fake이며 GPU 겹침 증거가 아니다.

독립 `pipeline-mutation` worktree와 별도 build에서 수정 제거 두 종류를 실제 재컴파일했다.
`pipeline-mutations.json`과 `pipeline-mutation-*.log`에 결과·source/tree/binary SHA를 보존했다.

| 변이 | 실패 | 재컴파일 binary SHA256 |
| --- | --- | --- |
| 전체 prefill 요청을 다시 한 묶음으로 선택 | 2, exit101 | `bda72c7f7e3c33615c9e965cd1dc0b159475e154e915c967ef1865fe182f2fdc` |
| mixed prefill quantum 제거 | 2, exit101 | `f71fe19d01ea8371b0f631de506d5abfa3ed8811723c6d6d86a294516664d1a8` |

첫 workspace 시도는 다른 작업이 사용 중인 기본 `p4-agent.exe` 파일 잠금으로 링크에 실패했다.
다른 작업은 유지하고 별도 target directory에서 재시도했다. 그 재시도는 문서 mixed EOL
한 건으로 실패했으며 코드 시험 실패는 없었다. 이 실패 로그도 보존한다.
EOL을 정리한 최종 `cargo test --workspace --no-fail-fast --locked -j 4 --target-dir
target/v11-prefill-20260911/workspace-build`는 **1396 passed / 0 failed / 7 ignored**,
58 summaries, exit0 (`workspace-pipeline-final.log`). 공통 composer Node4/4는 실제 CLI에서
새 정책 전달, 잘못된 창/quantum/fragment 설정 거부와 이전 파일 보존까지 확인했다.
Release Windows agent/driver 빌드도 exit0이다. 이 시점에는 새 정책의 GPU 결과를 아직 승인하지 않는다.

<a id="prefill-cohorts-20260911"></a>

## 12. 독립 prefill 묶음의 대조 실기와 다음 설계

### 소스·워크로드·종료 범위

측정 head/driver는 `e4503e10fe93215e0b10db52a99462bb8232a13c`다. §10 수용 입력 예산과
§11 pipeline policy를 구현하고 실제 소비 시험·변이·전체 게이트를 통과한 뒤 봉인했다.
MI250-B 단일 호스트8stage와 Hy3 다섯 호스트6stage를 병행했다. MI250-A는 다른 작업 점유로 제외했다.
MI는 최신451 native/ROCm, Hy3는 기존434 native/CUDA·Metal과 같은 downstream agent를 양 arm에 유지했다.
각 쌍에서 바꾼 값은 `P4_STAGED_PIPELINE_BATCHING=0→1`뿐이다. open8·fragment1·batch/ubatch512,
mixed quantum128·기존 phase cap0을 고정했다. 후보 토글은 독립 묶음과 혼합 quantum을 함께 활성화하므로
전체 TPS 차이를 둘 중 한 변화의 단독 효과로 분리하지 않는다. MI min4/CPU threads4, Hy3 min0/기존 native CPU 기본 설정이다.
**이번 기준은 전체 요청을 선택하는 cap0이다. 과거 tuned MI cap4/Hy3 cap2를 같은 workload에서 이긴 증거는 아니다.**

MI는16요청/108368입력 토큰, Hy3는8요청/53872입력 토큰이다. 각각 짧은8/4건과 긴8/4건을 함께 넣었다.
MI tokenizer 입력은2885/10661토큰이며, context100512/102400 설정과 실제 입력 길이를 구분한다.
**실제100k 입력 시험은 아니다.** 한국어 기술 보고서, 선두4행 계산, 근거와 가설 구분, 약1500–2000단어를
요청했다. 출력 최대4096/min1024/EOS 요구와 원래 judge는 변경하지 않았다. resident16/8은 같은 실행 문맥의
요청/sequence 슬롯이며 지속적인 다중 대화 서비스 승인이 아니다. KV device 우선과 Hy3 CPU expert 배치를 유지했다.

전체 실험 창은2026-09-11 07:21:30.884–09:21:30.886 UTC다. Hy3 기준은 후보 뒤에 실행되어
약18분 LOAD 뒤 실제 추론2417.818s를 수행했다. 공통 cutoff에서 `wall-deadline.json`이 종료 원인을 남겼다.
LOAD 시간은 아래 생성 TPS 분모에 포함되지 않는다. 같은 프리필+공통 생성 prefix의 완료시간은 별도로 비교한다.

### 완료·지연·품질 판정

| 항목 | MI 기준 | MI 후보 | Hy3 기준 | Hy3 후보 |
| --- | --- | --- | --- | --- |
| 완료/해제 | 16/16 | 16/16 | 0/0 (8건 제출) | 8/8 |
| EOS / length | 16/0 | 15/1 | 0/0, 전부 부분 응답 | 8/0 |
| 원래 passed | true | false | false | true |
| 생성 토큰 / 추론초 | 50623/2217.229 | 52128/1336.529 | 3164/2417.818, 부분 출력 | 19168/2545.607 |
| raw generated TPS | 22.8317 | 39.0025 | 완주 TPS 없음 | 7.5298 |
| 모두 첫 출력 도달 | 523.717s | 144.291s | 1871.134s | 960.432s |
| 모두 첫1024 생성 도달 | 1060.464s | 394.926s | 미도달 | 1615.169s |
| 첫 출력이 전부 도착하기 전 ITL p50 | 2.6805s | 1.7150s | 18.842s | 11.207s |
| 그 이후 ITL p50 | 0.5250s | 0.2610s | 1.4895s, 부분 구간 | 0.636s |
| 선두 계산 전량 통과 | 1/16 | 2/16 | 3개 확인,5개 미완 | 7/8 |

ITL은 실제 OUTPUT 수신 시각의 요청별 연속 토큰 간격들을 모은 분포다. 요청별 평균 간격의 분포가 아니다.
MI raw TPS는70.83% 증가했고 모두 첫 출력은72.45%, 모두 첫1024 생성은62.76% 단축됐다.
서로 다른 EOS 출력 길이를 가진 자연 생성 결과이므로 공통 prefix 지연도 함께 제시한다.
Hy3는 모두 첫 출력48.67% 단축이다. 모두 첫256 생성은2255.353→1124.122s(50.16% 단축)다. 기준의 부분 출력률1.3086을7.5298과 나눠 TPS 개선율로 사용하지 않는다.
Hy3 기준의 완료/해제0은 전체 stage 해제를 확인하지 못했다는 뜻이다. cutoff가 연결을 끊었고 artifact는
`evidence_missing={requests:8,stage_executions:0}`, 최초 receive 오류(os10054), 별도 cleanup_error를 보존했다.
뒤의 owned process 회수 성공을 프로토콜 UNLOAD 성공으로 바꾸지 않는다. runner의 중복 close `EBADF`도 남겼다.

MI 후보 req-012는4096에서 문장 중간에 잘리고 결론 marker도 없어 원래 EOS 게이트가 거부했다.
Hy3 후보 index7은825.968Wh를826.968Wh로 적었다. 기준 부분 응답에서도 같은 오류를 이미 관측했다.
기준의 계산 실패1과 미완/미파싱5는 중복되는 플래그다(한 응답이 둘 다 해당). 3+1+5를9요청으로 세지 않는다.
본문에는 인과 증거 부재를 인과관계 부정으로 확대하는 표현 등도 있다. 단어수 요청도 원래 marker judge가
강제하지 않았다. 후보의 whitespace 단위 수는 MI982–1543, Hy3 613–1013이다. 한국어 형태소 단어 수와 같지 않다.
따라서 **정상 응답 goodput·서비스 품질·H5 성능·최적 정책 승인은 모두 미완료**다. 이번에는 기본값을 켜지 않는다.

### 직렬화가 풀린 이유

같은512행도 `16요청×32행`과 `2요청×256행`의 의존성이 다르다. 전자는16요청 전부를 outstanding으로
만들어 다음 독립 batch가 없다. 후자는 다른14요청에서 다음 batch를 만들 수 있다. 행 폭을 유지하면서
참여 요청 수를 제한해야 파이프라인의 여러 stage가 서로 다른 batch를 계산할 수 있다.

| 초기 pure-prefill (첫 decode 이전) | MI 기준→후보 | Hy3 기준→후보 |
| --- | --- | --- |
| 논리 배치 중앙 폭 | 512→512행 | 512→512행 |
| 배치 참여 요청 중앙 | 16→2 | 8→1 |
| 발행 전 open 중앙 | 0→6 | 0→3 |
| head RPC 평균 | 193.10→202.99ms | 5007.49→5490.27ms |
| head RPC 사이 평균 대기 | 1948.47→195.55ms | 11115.30→11.89ms |
| head 발행 주기 평균 | 2141.22→398.27ms | 16112.61→5499.77ms |

개별 native RPC가 빨라진 결과가 아니다. 초기 full-width 계산을 유지하면서 반환을 기다리던 시간을 줄였다.
초기 구간은 같은 phase 경계로 잘랐으므로 전체 행 수는 MI46592/47144, Hy3 23040/22232로 다르다.
전체 동일 입력 완료의 비교는 모두 첫 출력 도달 시간을 사용한다.
MI 전체 같은 호스트 시계의 평균 동시 native RPC는0.9556/8→4.5837/8, 동시2개 이상 시간비율0→82.12%다.
각 노드 안의 RPC 동시 실행은 최대1이다. `mi250-rpc-overlap.png`는 head 누적16384행에서 시작하는
각20초를 같은 규칙으로 그린 실제 trace다. 그림의 구간 평균0.84→5.90을 전체 평균으로 바꾸지 않는다.

MI 진단용240초 decode 구간의 장치별 GPU busy 평균 범위는2.32–4.80%→24.53–52.98%다.
각 구간은 마지막 prefill 이후에 골랐지만 동일 생성 작업량은 아니다. 초기 ROCm 명령 경로 오류로 기준 첫243284ms의
GPU 표본이 없어 초기 pure-prefill GPU 개선율은 계산하지 않는다. RPC는 CPU/GPU/동기화 비용을 포함하며 GPU kernel 시간이 아니다.
Hy3 후보의 호스트별 첫–마지막 RPC 구간에서 Spark GPU53.21%, Ubuntu19.03%, Mac Device31.15%다.
Metal Device/Renderer/Tiler와 NVML GPU busy는 다른 counter다. M42 후보 monitor가 실패해 GPU 표본은0이다.
로컬 GPU에는 다른 작업의 native 두 개와 Python이 공존했다. 해당 작업은 유지했고 per-process 목록을 봉인했다.
이질적 GPU 전체의 포화 또는 외부 간섭 없는 H5 실행으로 승인하지 않는다.

### 이번 정책에 남은 비용

**행 폭, 참여 요청 수, flight 목표는 따로 선택해야 한다.** MI head native 호출은3790→28279회로 늘었다.
decode2행의 head RPC는평균26.24ms, 기준 decode16행은58.43ms다. 작은 묶음이 토큰당 싸다는 뜻이 아니다.
관측 구간의 prefix·동시 실행·요청 감소가 다르므로 이 평균을 고정비 회귀나 정밀한 matched kernel 비교로 사용하지 않는다.
파이프라인을 채울 만큼 독립 flight를 확보한 다음에는 지연 예산 안에서 decode를 더 합치는 편이 나을 수 있다.
현 정책의 `ceil(eligible/(window-open))`은 첫 안전 선택이지 비용 최적화가 아니다.

**정적128행은 지연 보장이 아니다.** Hy3 후보 mixed129행에서 head RPC2692.40ms 중 native decode가2685.29ms,
encode0.72ms, 바깥 미분류는6.16ms다. head trace19215개와 artifact span19215개의 순서·행 수를 모두 확인했다.
native decode는 CPU/GPU/내부 동기화를 포함한다. 이 관측은 다음 귀속이 native 안에서 필요함을 말하며,
1ms 재시도나 RPC wrapper를 없애면2.69초가 사라진다는 근거가 아니다. 부분 기준군의 native471/span470은
전량 대응하지 않아 `head-cost.py`가 상세 비용 대응을 거부했다.
비선점 prefill 여러 개가 decode 앞에 놓이면, 한 chunk를 제한해도 생성 대기가 누적된다.

**같은 layer 수는 같은 서비스 시간이 아니다.** Hy3 후보 pure-prefill 단계별 RPC 평균은 M42 S0/S1각5.38/5.54s,
Spark0.978s, Mac0.331s, 로컬2.910s, Ubuntu1.383s다. M42 두 단계의 전체 RPC 활성비율은92.5/95.6%다.
CPU expert offload와 host CPU/DRAM 공유가 있는 현재 컷에서 빠른 장치에 일을 더 넣으면 느린 단계 앞 queue만
늘 수 있다. CPU/GPU 각각의 병목을 확정하지는 않았으나, 서비스 시간 불균형은 관측됐다.
KV를 먼저 확보한 로컬3090은 KV14.0625GiB와 compute약3.567GiB 때문에 CPU weights약34.8GiB를 사용한다.
최대 context·resident 보장과 실제 KV pool을 구분해 계획해야 한다. 이를 몰래 줄여 GPU weight 비율을 높이지 않는다.

**ready 요청 수로 prefill의 폭을 판단하면 안 된다.** 현재 `drive.rs`의 min gate는 `ready_row_count()`의
요청 수를 사용한다. MI min4에서는 마지막2개의 긴 prompt가512행을 채울 수 있어도 다른 요청 반환을 기다린다.
pure-prefill row 충족과 decode coalescing의 최소 요청 수/대기 만료를 분리하는 반례다.
fragment1은 충분한 독립 요청이 있을 때만 파이프라인을 채운다. 한100k 요청의 선행 fragment는 아직 다루지 않았다.
입력 계정은 고쳤지만 MI agent RSS는 UNLOAD 이후에도 기준35.67GiB/후보30.88GiB 정도 남았다.
이는 heap/receipt 원인이 확정됐다는 뜻이 아니며 B2/B3/반환·broker byte 수명 게이트는 여전히 열린다.

### 산술 실패의 별도 native 대조

최신 native의 public llama API만 쓰는 단일 모델 프로그램으로 같은16개 prompt를 greedy 생성했다.
P4 stage 전달 없이 전체 모델을 같은MI250-B8장에 배치하고 각1024토큰 prefix를 받았다.
정확한 UTF-8 prompt hash와 tokenizer2885/10661을 P4 입력과 확인했다. 선두 계산은2/16통과,
14/16오류, 미파싱0으로 P4 후보와 통과 index0/5가 같다. 따라서 산술 오류는 P4 없이도 재현된다.
이 결과가 분산 수치 차이 전부를 배제하거나 전체 응답/bitwise 동일성을 증명하지는 않는다. native reference는
single sequence와 다른 logical n_batch이므로 TPS 비교가 아니다. 초기 CRLF prompt 시도는 입력 토큰 수가 달라
같은 입력 대조에서 제외하고 원자료를 남겼다. 최종16개는 `native-reference-exact/`, `native-reference-rest/`다.
P4 응답과 유효 reference 출력은 strict UTF-8이며 U+FFFD가 없다. native stderr의 GGUF vocab 표시에서 발견한
비UTF-8 byte는 출력 응답과 별개로 보존했다.

### 다음 구현의 계약과 판별 시험

현재 작업 순서는 로드맵 §0.V1.1이 소유한다. 이번 결과가 정당화하는 후속 설계는 다음과 같다.

1. **자원 수명과 대기 계측:** 실제 actor enqueue→dequeue, head monotonic issue→settle,
   미발행 이유, active/retired receipt bytes와 B2/B3 반환 예약을 결속한다. native CPU/GPU/전달을 분해한다.
   active slot 제거와 payload 최종 퇴역을 구분하고 정상/취소/replay/결과 불명에서 소비 시험·변이를 통과한다.
2. **phase별 폭·묶음·창:** prefill에는 효율적인 row 폭과 독립 요청 묶음을, decode에는 deadline 안의 coalescing을
   적용한다. hard open8은 상한이며 항상8개를 만들라는 목표가 아니다. queue가 없는 경로 지연을 가장 느린 stage의
   서비스 간격으로 나눈 값을 필요한 flight의 초기 추정으로 쓰되 byte/KV/요청 의존성으로 제한한다.
   queue를 포함한 RTT가 길어졌다는 이유로 창을 더 키우는 피드백은 금지한다. min gate도 phase별로 분리한다.
3. **누적 prefill 시간 예산:** 모델/backend/stage/phase/행 수/참여 요청/KV prefix별 비용을 예측한다.
   새 prefill을 추가했을 때 decode가 통과할 각 stage의 기존 대기·비선점 잔여 실행·추가 chunk 비용을 계산한다.
   그 예상 완료시각이 decode의 남은 지연 예산을 넘으면 prefill을 줄이거나 연기한다. P 요청에는 deficit/aging을
   적용해 기아를 막는다. 한 비선점 호출 자체가 목표를 넘으면 그 latency는 스케줄러만으로 보장하지 못한다.
4. **KV 우선의 컷/오프로딩 재계획:** weights+KV+compute+margin과 host RAM/CPU/DRAM 예산 안에서
   가장 느린 stage를 줄이는 컷을 PLAN→실제 최대 메모리→동일 부하로 검증한다. Spark/Mac 여유 활용은 별도 실험 축이다.
   100k×동시수의 수용 약속은 유지한다. 단순 layer 비례 추정은 메모리 승인으로 사용하지 않는다.
5. **실제100k와 적은 요청:** 충분한 요청에서 먼저 정책을 고정한 뒤, fragment1→2→4를 독립 비교한다.
   sequence/token-range/KV prefix 순서와 duplicate/cancel/out-of-order/RELEASE/SETTLE 안전성을 먼저 증명한다.
   node 실행1과 decode outstanding1은 유지한다. 한 긴 요청/여러 긴 요청/생성 중 긴 요청 유입/짧은 신규 요청의
   연속 도착을 분리해 TTFT·TPOT p95·정상 완료/해제·메모리 안정성을 검증한다.

true100k에서는 실제 attention의 KV 조회 범위도 비용 입력으로 계측한다. 최신
[llama-kv-cache.cpp](https://github.com/ggml-org/llama.cpp/blob/451b89bae0c4b1dd612eb503ceace906c01ddcc9/src/llama-kv-cache.cpp)의
unified pool은 하나의 stream이며 `get_n_kv()`는 사용된 위치 범위와 padding에 따라 view 크기를 만든다.
현재 요청의 prefix 길이, 공유 pool의 실제 n_kv, Q/K shape, mask byte, 선택 kernel과 masked-block skip을 함께 기록한다.
최대 context가100k라는 이유로 처음부터 전체 pool을 매번 계산한다고 단정하지 않는다. 최신 HIP의 일부 sparse
flash 경로는 지원하지 않지만, 이번 arm의 실제 선택 kernel/낭비량은 미계측이다. unified를 단순히 끄면
equal-stream 제약과 unequal batch 계약이 달라질 수 있으므로 scheduler knob만으로 바꾸지 않는다.

Sarathi의 chunked prefill, llama.cpp의 decode 우선 잔여 prompt 구성, vLLM/SGLang의 token/KV 예산을
참고하되 P4의 분산 stage에는 독립 요청 묶음과 유한한 비행/반환 권한이 추가로 필요하다(§11 원문 링크).
평가용 고정 출력 길이와 자연 EOS/내용 검증은 구별해 보존한다. 과거 tuned cap4/cap2를 같은 workload에서
대조하고, 후보 선정 뒤 H5 paired/holdout을 수행한다. 이번 한 쌍으로 최적값·기본 정책을 승인하지 않는다.

### 증거 결속과 정리

agent/driver/native/library132개 경로의 SHA256이 실행 전후132/132 동일하다. MI 모델 전량 재해시는
`2703b4e1285647dafa9a155705f0dcc2e6832f5f59db619ea1a3de913675cbd6`다. Hy3 shard는 기존 full hash와
현재 size/mtime 결속이며 이번에191.884GiB 전량을 다시 읽었다는 뜻이 아니다. 모든 owned 시험 native와 agent를
회수했고 Mac GUI agent는 원래 경로로 재시작했다. 기존 studio와 로컬 타 작업은 보존했다.
`processes-after.json`은09:30:47 UTC의 최종 읽기 전용 감사다. MI250-A의 다른 작업은 건드리지 않았다.

| artifact | SHA256 |
| --- | --- |
| `v11-cohorts-hy3-baseline-r1` | `60545f67fc19c892db96829fd2b6fd0d4d919b057530ea4647e658770a2565a7` |
| `v11-cohorts-hy3-candidate-r1` | `8d84f01adb55221224bde51092363c8c02809c17381061c310d6995917b82823` |
| `v11-cohorts-mi250-single8-baseline-r1` | `67ec97561867c0bdce3b8076a951597295d6d11546f9517e4ba279cae27a9987` |
| `v11-cohorts-mi250-single8-candidate-r1` | `82202e83eb814f20479e412d369264b0dcc1ad2a197ae1247daa67eecfb35329` |

원자료·분석 스크립트·실패 시도·source archive·runtime/model/clock 결속·실제 trace 그림은
`target/v11-prefill-20260911/`와 아래 별도 봉인 ZIP에 있다. `evaluation.json`, `mechanism.json`,
`head-cost.json`, `gpu-evaluation.json`, `arithmetic-review.json`, `direct-native-quality.json`,
`comparison.json`으로 위 값을 재계산한다. 비용 대응이 실패한 arm은 자동으로 비용표를 내지 않는다.
문서 최종 기록 전 코드 기준은 e4503e10f이며 이후308d2dfde는 별도 작업의 발표자료2파일만 바꿨다.
이번 마감에서는 runtime 코드를 바꾸지 않았다. §10/11의1396/0/7 및 CUDA16/16·ROCm15/15 검증 범위는 그대로다.

봉인 ZIP은 `F:/dev/p4-releases/v11-prefill-cohorts-20260911.zip`이며 SHA256은
`5b44f2722352d0acb52aefd46dfe3524cde35c069de7ae57cf6ecb8d45262135`다.
2026-09-11 09:39:11 UTC에 ZIP을 다시 열어380개 member, root manifest 및4개 arm/2개 native 대조
디렉터리의 manifest를 모두 검증했다. 크기는107435076B다. 빌드 트리·모델·plot dependency는 포함하지 않는다.
기존 두 증거 ZIP은 변경하지 않았다. ZIP은 측정·분석 시점의 고정 사본이며 이 최종 봉인 문구는 ZIP 밖의
저장소 문서에 남겨 self-reference를 피했다. 최종 docs-lint94 clean과 문서 whitespace 검사를 통과했다.

<a id="receipt-memory-observation"></a>

## 13. Broker exact receipt의 실제 저장 수명 계측

기준 `ea0872d5d`. §12의 봉인 arm과 ZIP은 변경하지 않는다. 목표는 UNLOAD 뒤 RSS를 heap 원인으로
단정하지 않고, broker가 실제 보관하는 exact Event 사본을 실행 중인 INSPECT에서 확인하는 것이다.
원자료는 `target/v11-receipts-20260911/`다. 이번 단위는 CPU 로컬 제어 경로이며 GPU 성능 실기가 아니다.

`EventLedger`는 성공한 dispatch의 독립 사본을 계속 같은 count window에 보관한다. 운영 window는
262144이며 이번에 줄이지 않았다. `Receipt`가 실제 사본의 Event inline/buffer capacity를 계상한다.
count eviction 때 indexed→retired로 옮기고, front ticket의 마지막 Arc가 없어 Event buffer를 파기한
뒤 allocated에서 뺀다. EventNode의 예약 completion 경로도 같은 원장을 사용한다. snapshot은 O(1)이며
원문을 복제/직렬화하지 않는다. 원본 전달 allocation·전체 equality·sequence 판정·eviction 시점은 그대로다.

실제 agent INSPECT의 `broker.receipts`가 현재/최대 바이트·누적 commit/eviction/free와 index capacity를
반환한다. index/Arc/receipt 부기·allocator·목적지 사본·native·RSS는 Event byte 수치 밖이다.
indexed/retired는 요청이나 KV 실행 단계가 아니다. 수치 overflow/미측정은 null이며 실패를0으로 표시하지 않는다.
INSPECT 자신의 수신 receipt는 포함하고 그 응답의 dispatch는 포함하기 전이다. 세부 계약은
[중립 Event 규약](../../../../../../../../docs/event-protocol-v2.md#agent-inspection)이 소유한다.

### 반례·실제 소비·검증

- `inspection_counts_retained_receipts_through_the_actual_control_loop`: 실제 broker→agent control loop→
  OUTER reply를 소비한다. 먼저 목적지에서4096B Event를 꺼내 파기하고 INSPECT를 보낸다. 이전 코드는
  broker 필드가 없어 `Null != 2`로1실패(exit101)했다. 수정 뒤 두 receipt의 실제 비용·payload4098B를 확인한다.
- `receipt_memory_measures_independent_copies_on_both_dispatch_paths`: 일반/예약 dispatch, 네 opaque
  EventClass, spare capacity를 가진 원본 이동, 독립 사본의 정확한 비용, 목적지 변조 후 conflict와 exact duplicate를 검사한다.
- `receipt_memory_retired_bytes_survive_until_the_last_real_front_ticket`: 실제 count eviction 뒤
  두 pinned ticket 중 하나를 소비해도 bytes가 남고 마지막 conflict 판정 뒤에만 줄어드는 것을 확인한다.
- `receipt_memory_refusal_and_cancel_do_not_consume_storage_or_identity`: Full의 원본 allocation 반환,
  예약 취소, 같은 ID 재시도에서 메모리/신원 보존을 검사한다.
- `receipt_memory_tracks_actual_window_rotation_without_retaining_evicted_values`: queue1/window8에서
  64KiB opaque Event128개를 일반/예약 경로로 번갈아 전달한다. 매번 index·보관 byte·최대값·퇴역·해제량을 검산한다.

검증 code/test tree는 `21a6254c9e0d65cc9f68be44dee4efd231081829`이며, 이후 차이는 마감 문서뿐이다.
기존 broker 시험과 새 소비 시험4개, INSPECT/hardware 시험4개를 통과했다. 최종
`cargo test --workspace --no-fail-fast --locked -j 4 --target-dir target/v11-receipts-20260911/build`는
**1401 passed / 0 failed / 7 ignored**,58 summaries, exit0다. `workspace-aggregate.json`으로 최종 종료와
모든 summary를 검산했다. `inspection-before.log`, `receipt-consumers-final.log`, `inspection-after.log`에
최초 실패와 통과 원문을 보존한다. 기존 unused import/dead-code 경고는 남아 있으며 이 결과가 무경고라는 뜻은 아니다.

독립 detached `mutation-copy`와 별도 `mutation-build`에서 실제 core 재컴파일을 확인했다.

| 변이 | 실제 소비 실패 | 재컴파일 binary SHA256 |
| --- | --- | --- |
| count eviction의 retired 전이 제거 | 2, exit101 | `a07707837230f0a3cea345114097ba00c16d876f9b62a532f8bf65cd436eed1b` |
| 마지막 참조 파기 때 allocated 차감 제거 | 2, exit101 | `80c38622c4f9b4f707aa06d6c881f768c53acaf8b01553ebdcdac5a074141884` |

### 실제 로컬 agent TCP 판정

같은 tree의 agent를127.0.0.1 임시 포트55639에서 시작하고 production wire codec으로 접속했다.
모델을 만들지 않고4MiB opaque control payload16건을 보냈다. 해당 명령은 제어 소비자가 unsupported로
거부했고 그 응답을 전량 받았다. 마지막 INSPECT 전에35개 Event의 exact receipt가 남았으며,
payload capacity **67112267B**는 probe가 합한 송신 payload와 수신 응답 길이에 정확히 일치했다.
Event inline/문자열까지 포함한 비용은67130635B, retired0·freed0이었다. 목적지 처리가 끝나도 count window
receipt는 계속 보관됨을 실제 wire 경로에서 확인했다. 이것이 이전 MI250 RSS 전체의 heap 귀속은 아니다.

probe exit0. agent SHA256은 `b1d81b7e3b71a91edf34313853fd4128221a857d17c79dd085c94439ca5f9d6d`,
probe SHA256은 `8b95f4d6e525f1555161581425a475ff7fe9ba35fd7114f8cbb479c229d468ec`다.
전용 agent PID81760은 시험 뒤 명시 종료했고 부재를 확인했다. 종료 코드1은 테스트의 process terminate이며
UNLOAD·graceful drain 성공으로 표시하지 않는다. 기존 다른 작업과 원격 호스트는 변경하지 않았다.

증거 ZIP `F:/dev/p4-releases/v11-receipt-memory-20260911.zip`, SHA256
`b285ad9cccecc647cbfced5953ee2828b21b74ed88a3624b30915381217e7be0`.
10:05:05 UTC에28개 member와 manifest를 ZIP에서 다시 읽어 검증했다. source tree archive·patch·실패/변이/전체
시험·wire probe 소스/결과/바이너리 해시를 포함한다. 최종 봉인 문구는 ZIP 밖이며 기존 실기 번들은 변경하지 않았다.

**남은 범위:** 정확한 Event 사본의 관측이며 B2/B3·receiver/return byte 예약, broker byte 상한,
요청 종료 시 자동 receipt 삭제, native/CPU/GPU 시간 귀속, 실제100k·두 클러스터 재실기 승인은 아니다.
크기 때문에 count-window receipt를 조기 삭제하거나 hash-only equality로 바꾸지 않았다.

<a id="phase-pacing"></a>

## 14. 고정 flight 창에서 phase별 발행과 decode 대기 만료

기준 `0a94f1699`. §12/13의 봉인 실행과 ZIP은 변경하지 않는다. source/로그 위치는
`target/v11-phase-pacing-20260911/`다. main/원격 일치는 시작 시 확인했고 이 단위는 로컬 검증이다.

### 계획과 소비 반례

긴 prefill도 ready 요청 하나로 세는 min gate는 마지막 독립 묶음을 막는다. prefill gate만 고쳐도
소수 ready decode를 기다리는 Worker의 blocking recv는 새 input이 없으면 돌아오지 않는다.
ordinary attention의 opt-in pipeline에서 prefill 계획과 decode-only 대기를 분리하고, 후자에
단조시계2ms의 최대 의도적 coalescing 대기를 붙인다. native/RPC/토큰 간격의2ms 보장은 아니다.
실행 중인 native를 중첩하거나 resident/open/fragment 창을 늘리지 않는다. 반환 byte 예산 전체는 여전히 미완이다.

두 반례는 실제8개 Worker::run·Event/Frame codec·completion capacity1을 통과한다. native 계산만 fake다.
첫 시험은8개 긴 요청을 두 요청씩4행의4개 묶음으로 발행하고 tail 반환을 모두 보류한다. 기존 min4는
마지막 두 요청을 막는다. 둘째는 네 prefill 결과 중 하나만 돌려 두 decode를 ready로 만들고, 다른
결과와 신규 input을 보류한다. timer만으로 새 decode가 발행돼야 한다. 이후 전량 완료·각 stage KV
토큰/위치·정확히 한 번 해제를 기존 독립 소비 oracle로 검사한다. GPU/원격 성능 시험은 아니다.

`before.log`는2실패(exit101), prefill 조건만 바꾼 `prefill-only.log`는1통과/1실패(exit101)다.
후자는 첫 프리필 반례가 사라진 뒤에도 `decode deadline must wake the actual blocking worker`에서
5초 한도에 도달했다. 완성 후보의 소비 경로2개는 통과했다. 중간 시험 진단에 RequestState의 Debug를
요구한 컴파일 실패도 `consumer-final.log`에 보존하고 명시 상태 투영으로 고쳤다. 테스트 기준은 완화하지 않았다.

### 검증 결과

검증 tree는 `3728df7fa8a877599c546b0db675b4195201adb2`다. 이후 마감 변경은 문서뿐이다.
`cargo test --workspace --no-fail-fast --locked -j 4 --target-dir target/v11-phase-pacing-20260911/build`는
**1404 passed / 0 failed / 7 ignored**,58 summaries, exit0이다. 새 시험은 실제 소비2개와
가짜 단조시각을 쓰는 순수 timer1개다. 이전 입력이 대기를 연장하지 않고 load 변경/clear/disarm이
오래된 deadline을 잘못 재사용하지 않는지 검사한다. 실제 소비 시험은 대기 전후 요청 진행·flight·슬롯·
입력 예약·Event ID 권위의 동일성도 대조한다. 대기 전에 native 준비 상태가 없어야 한다.
기존 dead-code/unused 경고는 남아 있다. 최종 docs-lint94 clean, whitespace 검사를 통과했다.

실제 workspace가 실행한 test binary의 SHA256은
`16c409ce394976414fa7daf3cea2d27ce0992d92274c606b0a8af4a97fafb1e6`다. package-only 바이너리와
workspace feature 결합 바이너리를 구분해 실행 로그의 경로로 선택했다. 결과·source9파일 SHA는
`workspace-aggregate.json`에 있다. 이 수정의 remote/native GPU 실기는 수행하지 않았다.

독립 detached `mutation-copy`와 별도 `mutation-build`에서 각 변이의 재컴파일을 확인했다.

| 제거한 수정 | 소비 시험 결과 | 재컴파일 binary SHA256 |
| --- | --- | --- |
| 요청 수로 모든 phase를 막는 기존 조건 복원 | 0통과/2실패, exit101 | `66676bc6e7c496f472946662b263731e0bfb5f2e3e4316945a3f9897668c6181` |
| 실제 Worker 수신 대기에서 deadline 깨우기 제거 | 1통과/1실패, exit101 | `c861840bbb3ef748aad208c58b750ead1c79ee8a65f2c16075dba1bbc19161b7` |

원자료 ZIP은 `F:/dev/p4-releases/v11-phase-pacing-20260911.zip`, SHA256
`23f60cb1ec24fd71b5abb1a0de42e2bf5d617ca8625ddd2c8b4b84004f7a5e0d`다.10:39:05 UTC에24개
member와 manifest를 ZIP에서 다시 읽어 검증했다(2762673B). source archive/patch, 최초 실패,
중간 컴파일 실패, 최종 workspace, 변이 소스 스크립트/결과 및 해시가 포함된다. 최종 문구는 ZIP 밖에 남긴다.

**다음 첫 행동:** 반환 공간의 사전 예약을 broker·EventNode·adapter input/completion·transport의
실제 보관 수명과 연결한다. 기존 raw Event 소비자 앞에 reserved publisher만 켜지 않는다.
누적 prefill 시간 예산·단계 비용/컷 재계획·fragment 확대·실제100k/두 클러스터 성능 승격은 남아 있다.

<a id="retained-broker-node"></a>

## 15. 소유형 broker와 실제 node loop의 국소 반환 수명

기준 `cb71fa6f1`. 기존 GPU 실행/ZIP은 불변이며 이번 source/실패/시험은
`target/v11-owned-broker-20260911/`에 기록한다. production root의 raw 소비와 구분되는 명시
소유형 경계를 구현한다. native 결과 선예약·worker/transport 전체 이관·receipt byte 상한은 이 단위 밖이다.

### 계획과 실제 소비 반례

기존 raw dequeue는 payload가 caller에 살아 있어도 저장 claim을 반환한다. 그 관측 반례를 같은
시험 바이너리에 보존하며 raw API가 바뀌었다고 하지 않는다. 소유형 broker/노드/adapter 입력·완료
소비에서는 목적지의 실제 queue slot과 retained count/bytes를 함께 확보한 뒤 원본 allocation을
이전한다. source claim·실패 반환·held input/output이 중복 원장 퇴역과 독립적으로 살아 있어야 한다.

새 시험10개는 mailbox만의 모델이 아니라 실제 broker, 실제 `RetainedEventNode::run`과
queue1을 소비한다. 실제 llama.cpp worker/native 계산을 실행한 시험은 아니다.

- 실제 node loop에서 입출력 동시 Full·independent completion 진행·terminal refusal의 원본 pointer,
  capacity claim, 원장 commit 수를 검증한다. 같은 `(source, correlation)`은 앞지르지 않는다.
- 실제 node→adapter 입력/adapter 완료→broker 목적지를32회 왕복하고 매회 모든 전달 저장소의
  bytes/count/queue slot이0으로 퇴역하는지 검증한다. exact receipt8개는 별도로 유지한다.
- 목적지 Full/개별 TooLarge/닫힘/등록 변경/중복 conflict는 원본·source claim·원장 권위를 보존한다.
  front ticket은 실제 슬롯·바이트를 함께 선확보하며 틀린 cost는 front를 제거하지 않는다.
- ledger와 등록 잠금에 재진입하는 capacity/reader callback으로 성공 알림의 잠금 밖 실행을 검사한다.
  duplicate pin은 정상 count eviction 뒤에도 전체 Event 비교를 유지한다. claim·slot의 서로 다른
  mailbox 조합과 부족한 바이트 예약은 모두 원 소유자와 함께 반환한다.

`consumer-first.log`의 최초 시험은 helper의 `CompletionMailbox`/`Arc<CompletionMailbox>` 반환 타입
불일치로 컴파일 실패했다. 이 로그를 보존하고 실제 constructor 타입으로 수정했다.
`consumer-second.log`는9통과, 반복 왕복을 추가한 `consumer-final.log`는10통과다.
전체 workspace·독립 변이·마감 해시는 아래에 최종 결과로 기록한다.

### 최종 검증과 봉인

검증 tree는 `d99ff9c9a58e394a72842a11dc48adc429ba6243`이다. 이후 변경은 마감 문서뿐이다.
`cargo test --workspace --no-fail-fast --locked -j4 --target-dir target/v11-owned-broker-20260911/build`는
**1414 passed / 0 failed / 7 ignored**,58 summaries, 최종 exit0다. 공통 코어 중립성 시험을 포함한다.
기존 unused/dead-code 경고는 남아 있다. 실제 workspace가 실행한 core 시험 바이너리 SHA256은
`7eac66e85985c507ac80dddc63f03f38ad4daca609b8a202a3a74c28dd5fa00f`이며 adapter 시험은
`84ed52a285941d21d97ccdf31c1db1b96cd843e36e6ad2ecab2ef5cd03a7bc45`다.
`workspace-aggregate.json`은 최종 종료·모든 summary·실행 binary 경로·수정 source9파일 SHA를 묶는다.

독립 detached `mutation-copy`와 별도 `mutation-build`에서 실제 재컴파일과 다른 binary SHA를 확인했다.

| 제거한 동작 | 소비 시험 통과/실패 | 재컴파일 binary SHA256 |
| --- | --- | --- |
| owned dequeue 이후 claim 유지 | 3/7, exit101 | `beb1b688fddcb15a2682bd1d330f5adc8f34b0be5d81ba4a95c59debdfc3d162` |
| 실제 node loop의 독립 front 전달 | 9/1, exit101 | `93228eab92dff6ef2422118314d6884643976f5f7f1d35c7f3c0dc0a5ac9c1f3` |
| front ticket 소비 때 node 등록 재검사 | 9/1, exit101 | `e8afd8bd2ed7a0278ccc78d5d8d3e707e71a45b0253f2ff34cf53ecb0e84246a` |

원자료 ZIP은 `F:/dev/p4-releases/v11-owned-broker-20260911.zip`, SHA256
`0f79cab589312c042477e433941675d65f197c2eb282622c36e266f2acfd15b2`다.11:11:30 UTC에24개 member와
manifest를 ZIP에서 다시 읽어 검증했다(3239135B). source archive/patch·첫 컴파일 실패·소비 통과·
전체 회귀·변이 소스와 결과·해시를 포함한다. 마감 문구는 ZIP 밖이다. docs-lint94 clean과 whitespace를 확인했다.

이 단위에서 원격/GPU 시험은 시작하지 않았고 기존2시간 실행을 연장하지 않았다. raw 제품 경로는
소유형 타입을 아직 선택하지 않으므로, 제품 RSS 감소·처리량 개선·전 구간 byte 상한으로 인용하지 않는다.

**다음 첫 행동:** 이 소유형 경계를 실제 llama.cpp Worker 입력/완료와 runtime control/connection writer에
연결하고 native 필수 결과 및 독립 receipt의 사전 byte 예약을 닫는다. 그 뒤 누적 prefill 시간 예산과
실제100k/두 클러스터 검증을 진행한다. 새 국소 타입의 존재는 제품 전체 예약 승인이 아니다.

<a id="retained-worker"></a>

## 16. 실제 llama.cpp Worker 입력·완료·중단 소유권 연결

기준 `3bcfc7edb`, 시작 시 main/GitHub 일치·clean을 확인했다. 이번 source/로그는
`target/v11-owned-worker-20260911/`이며 앞 실기·ZIP은 바꾸지 않는다.

### 구현과 소비 시험

소유형 adapter가 별도 처리기를 흉내 내지 않고 기존 Worker의 공통 loop에 입력을 전달한다.
입력 Event는 원본을 빌려 처리하고 std 큐·현재 처리·Full의 비ACK 보류·지연 ACK 오류까지 같은
claim을 유지한다. 완료는 기존 bounded mailbox에 실제 bytes/count를 예약해 수용하고 raw reader가
꺼내지 못하게 한다. 이것은 현재 Event의 수용이며 native 미래 출력/effect 선예약 전체가 아니다.
중단 원인 입력·held 입력·지연 ACK·미처리 receiver·semantic state/effect는 adapter owner에 남는다.
owner Drop 전까지 처리 실패를 원문 퇴역이나 replay 권한으로 바꾸지 않는다.

- `owned_worker_stop_retains_active_obstructing_and_queued_original_inputs`: production constructor와
  실제 Worker를 사용한다. 완료 queue1을 채우고 다음 오류 출력이 Full인 동안 잘못된 ACK·비ACK·
  후속 입력을 정상 offer로 보낸다. 중단 뒤 현재 입력/ACK 원문/held/queued 네 원본 pointer와 claim,
  실패 effect가 살아 있어야 한다. 이미 멈춘 adapter의 offer는 원본을 Closed로 반환한다.
- `owned_worker_two_stage_actual_ring_completes_prefill_decode_release_and_unload`: 실제
  RetainedEventBroker→RetainedEventNode→RetainedLlamaNodeAdapter→Worker::run 공통 loop를
  사용한다. native Frame 계산만 독립 KV oracle로 대체하며 LOAD 뒤 상태만 fixture로 설치한다.
  기존 `ordinary-2` 입력/출력/receipt/관측 골든을 그대로 비교한 뒤 별도 네 요청 웨이브를 처리한다.
  총5요청/25생성 토큰의 위치·내용·각 stage KV 쓰기·정확히 한 번 해제·idle UNLOAD와
  queue1/retained16/4MiB 저장소의 최종 bytes/count/예약 슬롯0을 검사한다. GPU 처리량 시험은 아니다.

### 초기 실패의 분류

`compile-first.log`는 WorkerInput variant 확장에 따른 기존 시험의 exhaustive pattern/내용 참조
컴파일 오류4건이다. raw 입력을 보냈다는 단정과 원 Event 내용 비교는 그대로 두고 타입 경계를 고쳤다.
`consumer-first.log`의1통과/1실패는 수동 actor poll 뒤 OS sleep이 Tokio cooperative task에 yield하지
않아 세션 준비에서 멈춘 시험 구성이다. 기존 actor-ring처럼 async sleep으로 바꿨다.
그 뒤 consumer-second/third/fourth는 추론 경로를 진행했지만 임의 case 이름을 사전 등록된 골든 전용
helper에 전달한 시험 오류다. 새 기대 원문을 생산 결과에서 만들지 않고 기존 `ordinary-2`를 실제로
재실행하며, 추가 웨이브는 입력 workload를 명시하는 동일 prefill 회계 oracle와 토큰/KV oracle로 검사한다.
기존 골든 JSON·수락 기준은 변경하지 않았다. `consumer-final.log`는2통과다.

### 최종 검증과 봉인

검증 source tree는 `0f20c4c341609ca527a9af5547fbb9dd596a609f`다. 이후 변경은 이 기록과 로드맵의
문서 정리뿐이며 모든 Rust 파일이 검증 tree와 같음을 확인했다. 실행 환경은 Windows 로컬이고 GPU/원격 작업은 시작하지 않았다.

| 명령/검사 | 결과 |
| --- | --- |
| `cargo test -p p4-llamacpp-staged-adapter --lib owned_worker_ --locked -j4 --target-dir target/v11-owned-worker-20260911/build` | 2 passed / 0 failed, exit0 |
| `cargo test --workspace --no-fail-fast --locked -j4 --target-dir target/v11-owned-worker-20260911/build` | 1416 passed / 0 failed / 7 ignored, 58 summaries, exit0 |
| `node tools/scripts/docs-lint.mjs` | 94 files clean |
| 소스·바이너리 결속 | workspace 실제 실행 staged-adapter binary SHA256 `fdd4e71a244cf8dd092ca171c8b23a1728d045d7e63400218acf3fd7b7234f91`; package 단독 시험과 feature 통합 binary를 구분 |

독립 detached 복사본에서 매회 staged crate를 실제 재컴파일했다. 세 변이 모두2시험 중1실패/1통과,
exit101이며 반환 소유권 소비 반례가 실패했다. 상한·골든·기대 오류를 완화하지 않았다.

| 제거한 동작 | 변이 실행 바이너리 SHA256 |
| --- | --- |
| Worker 소비 전에 원본 claim을 버리고 raw Event 사본으로 전환 | `e3a8cd93d214a54de91a45c4d998b6ec35830cfa9e988dad4a8fcadc130f0bcd` |
| 완료를 owned 대신 raw 방식으로 발행 | `f82c1d53c3b4020deb82e15f5ff063716a30c88a07611a8ec031b7a723797125` |
| 중단 Worker remainder를 보존하지 않고 폐기 | `21c5d586f2ba5f3d94c5cc35b68c8963c7d10493f62121ff5a6913b9468e279d` |

`workspace-aggregate.json`, `mutations.json`, 원로그·소스 patch·검증 script와 MANIFEST를
`F:/dev/p4-releases/v11-owned-worker-20260911.zip`에 봉인했다. SHA256은
`dce6c455be7cb985b5337f90ea48e5498d4930f5cfdf8f044df69a76b85c6d7a`, 3248548 bytes, 26 members다.
2026-09-11T11:44:30.173255Z에 ZIP 내부 파일 전부를 MANIFEST와 재대조했다.
중단 자료 보존용 필드의 dead-code 경고는 소유권 Drop 수명과 별개며 무경고 빌드라는 뜻이 아니다.

**다음 첫 행동:** runtime composition root와 control reply/connection writer에서 같은 소유권을
연결한다. native 결과/effect/receipt의 독립 선예약과 원격 write 결과 불명 보존도 남아 있다.
그 뒤 누적 prefill 예상 시간 정책·실제100k·두 클러스터 성능 승격을 계속한다.

<a id="long-prefill-design"></a>

## 17. 긴 prefill 다중 요청: 관측에서 도출한 배치 정책

이 절은 §12의 측정과 현재 scheduler/drive를 대조한 설계 결론이다. 새 비용 정책의 구현·실기 통과 기록이 아니다.
실행 순서는 로드맵 §0.V1.1이 소유한다. 비교 실기는 실제 최장10.6k 입력이며100k 검증으로 확대하지 않는다.

### 세 결정을 분리한다

현재 `scheduler/pipeline.rs::PipelinePolicy::select`는 eligible 요청 수를 남은 flight slot 수로 나눈다.
이는 독립 묶음을 만드는 수정이며 GPU 비용·prefix 길이·stage backlog·요청 deadline을 받지 않는다.
`drive.rs`의 phase별 발행/2ms decode coalescer도 시간 기반 prefill 정책 전체를 대신하지 않는다.

| 결정 | 목적과 변경 방향 |
| --- | --- |
| batch의 prefill 행 폭 | 한 native 호출의 효율 확보. pure-prefill은 효율 곡선이 평탄해지는 충분한 폭을 사용하고, mixed는 decode 완료시각 제약 아래 고름 |
| batch 참여 요청 수 | 한 batch가 모든 독립 요청을 outstanding으로 만들지 않도록 분리. 오래 기다린 요청의 서비스 몫을 보존 |
| pipeline의 flight 목표 | 병목 stage에 공급이 끊기지 않을 만큼 확보. 이미 바쁜 stage 앞 queue만 늘리면 추가 발행을 늦춤 |

16요청/fragment1/8flight에서 `16×32=512`는 다음 요청 묶음을 남기지 않지만 `2×256=512`는
서로 다른8개 묶음을 만든다. 행 폭을 줄이지 않고 파이프라인 깊이를 얻을 수 있다. MI의 head RPC가
193→203ms로 빨라지지 않았는데 반환 대기가1948→196ms로 줄어든 현상은 이 의존성 변화와 맞는다.
다만 전체 후보는 mixed quantum도 함께 바꿨으므로 전체 TPS70.83%를 cohort 하나의 효과로 분해하지 않는다.

충분한 independent 요청이 있으면 우선 fragment1을 유지한다. 1–2개 긴 요청만 남으면 최대 독립
flight도 작아진다. 그 경우에만 같은 요청의 다음 prefill fragment를 검토한다. 토큰은 알려져 있어도
각 stage의 이전 prefix KV 완료·token-range 순서가 필요하다. decode는 이전 sampled token이 필요하므로
요청당 outstanding1을 유지한다. 현재 pipeline policy는 multi-fragment를 명시적으로 거부한다.

### 행 제한을 누적 서비스 시간 제한으로 발전시킨다

prefill만 있는 구간은 큰 chunk로 native 호출 횟수를 줄인다. decode와 공존하는 구간에서는 다음
decode가 지날 경로에 이미 들어간 prefill까지 계산해야 한다. ready decode가 잠시0이어도 이전 decode가
비행 중이면 다시 도착할 decode를 고려한다. 한 번 실행한 native 호출은 이 정책에서 선점할 수 없다.

adapter 내부의 비용 입력은 stage/backend/weight 배치/host CPU 예산, prefill·decode 행 수, 참여 요청 수,
요청별 KV prefix, 실제 attention n_kv/QK shape와 kernel이다. 같은128행이라도10k와100k prefix의 비용은
같다고 볼 수 없다. dense causal attention의 유효 query-key 쌍은 대략 `q*L + q*(q+1)/2`지만,
이는 유효 attention 작업의 구조 설명이며 masked block·padding·unified pool·MoE·CPU offload를 포함한
실제 kernel 시간 식이 아니다. 새로운 길이/backend 비용은 직접 측정하고 예측 오차를 남긴다.

동일 크기 batch를 독립 stage가 하나씩 처리하고 비용이 일정하다는 단순 모델에서는
`최대 batch/s ≈ 1/max(stage service time)`이다. queue를 제외한 경로 지연을 이 발행 간격으로 나눈
값은 필요한 flight 수의 초기 추정으로만 쓴다. 실제로는 공유 GPU/CPU/DRAM 및 edge 자원도 제약한다.
queue 때문에 RTT가 늘었다고 flight를 늘리면 혼잡을 더 키우므로 이 방식으로 창을 적응시키지 않는다.

후보 batch b가 stage s에서 끝날 예상시각은 다음 순서 제약을 적용한다.

```text
finish[b,s] = max(arrival[b,s], finish[앞서 예약된 작업,s]) + predicted_service[b,s]
arrival[b,s+1] = finish[b,s] + predicted_transfer[b,s]
```

실행 중 작업의 남은 시간과 이미 발행된 batch를 먼저 반영하고, 후보 prefill을 넣었을 때 각 decode의
예상 완료시각이 지연 목표를 넘는지 검사한다. 넘으면 chunk를 줄이거나 이번 prefill을 연기한다.
이는 head에서 순서를 예측하는 정책이며 노드 간 wall clock을 직접 빼는 계측이 아니다. 입력·KV 순서나
이미 발행한 membership을 뒤집지 않는다. 예측 오차와 최소 native 호출 지연 때문에 hard real-time 보장은 아니다.

동작은 다음과 같이 구성한다.

1. 자원 예약·KV prefix·의존성이 허용하는 요청만 후보로 삼는다.
2. 만료가 가까운 decode 묶음을 고르되 모든 독립 decode를 한 묶음에 모아 직렬화하지 않는다.
3. prefill의 누적 미서비스량과 대기시간으로 요청을 고른다. 공정성은 행 수뿐 아니라 예상 병목 서비스 시간을
   단위로 부과하고 실제 비용으로 보정한다. 거부된 계획은 몫을 소비하지 않는다.
4. discrete chunk 후보를 각 stage의 기존 작업 뒤에 가상 배치해 decode 완료시각과 prefill 처리율을 비교한다.
   native 호출 효율이 좋아지는 충분한 폭 중 지연·메모리 예산을 만족하는 후보를 선택한다.
5. flight·edge/receiver bytes·KV·필수 반환 예약을 함께 확보한 뒤 원장과 membership을 확정한다.
   완료 때 실제 비용·예측 오차를 반영하고 다음 선택을 갱신한다.

decode를 영구 우선하면 prefill이 굶고, prefill의 최소 서비스 몫만 강제하면 과부하에서 decode 목표를
넘을 수 있다. 따라서 bounded aging/서비스 몫과 admission을 함께 사용한다. 지속 도착량이 병목의 처리
용량을 넘는 경우 유한 메모리와 모든 요청의 유한 대기시간을 동시에 보장할 수 없으므로 수용 대기/거부를
명시한다. 긴 입력을 짧게 바꾸거나 resident 약속을 몰래 낮춰 이를 숨기지 않는다.

이 방향은 [Sarathi-Serve](https://arxiv.org/abs/2403.02310)의 chunked prefill·지연을 제한한 혼합 배치와
[vLLM의 chunked prefill](https://docs.vllm.ai/en/latest/configuration/optimization/#chunked-prefill)의
decode 우선/잔여 token budget 원리를 사용한다. 단일 실행기의 모든 decode 선택을 P4에 그대로 옮기지 않고,
독립 요청 묶음·stage 누적 서비스·finite flight를 추가하는 것은 이번 코드/실측에 근거한 설계 판단이다.

### Hy3는 느린 단계의 작업 배치도 함께 바꿔야 한다

Hy3 pure-prefill RPC는 M42 두 stage가5.38/5.54s, Spark0.978s, Mac0.331s다. 느린 stage가 계속 일하는
상태에서 빠른 stage에 같은 작업을 더 공급할 수는 없다. 같은 비용이 반복된다는 단순 모델에서 Mac의
RPC duty는0.331/5.54≈6%여도 전체 pipeline은 병목 용량에 도달할 수 있다. 이는 GPU 사용률 예측이나
이번 전체 실기의 duty 계산이 아니라 불균형의 의미를 설명하는 예시다. 모든 장치100%를 목표로 queue를
늘리면 처리량은 그대로인 채 TTFT·ITL·보관 메모리만 늘 수 있다.

따라서 M42의 CPU 연산/DRAM·GPU/동기화 비용을 나눈 뒤 가장 느린 stage의 비용을 낮추는 합법적 cut과
weight 배치를 비교한다. GPU에100k×보장 동시수의 실제 KV·compute·margin을 먼저 확보하고, 남은 용량에
weights를 배치한다. host RAM에 놓인 experts의 실행 비용과 두 stage의 CPU 자원 경합도 계산한다.
Spark/Mac으로의 이전은 실제 메모리·지원 kernel·네트워크 경계를 통과한 후보만 허용한다. 모든 stage를
동일 layer 수로 만드는 것이 목표가 아니며 offload 허용 자체가 속도 개선을 보장하지 않는다.

### 다음 실험은 정책의 결정을 판별한다

반환 수명 연결 뒤 같은 안전 창에서 행 폭/참여 요청/flight를 독립 축으로 비교한다. 기존 전체 선택과
현재 cohort, 과거 tuned MI cap4/Hy3 cap2를 같은 native·입력·도착열로 대조한다. 이후 비용 정책만 켠다.
각 arm은 아래 입력을 실제 tokenizer로 확인하고 충분한 출력/자연 EOS·내용 검증을 함께 보존한다.

| 입력/도착 조건 | 판별할 것 |
| --- | --- |
| 여러 실제100k 요청이 동시에 도착 | 충분한 행 폭을 유지하며 stage 공급 공백이 줄고 동일 입력의 prefill 완료시간이 단축되는가 |
| decode 중 여러100k 요청 유입 | prefill의 누적 시간 제한이 decode p95/p99 ITL과 긴 정지를 줄이는가; prefill도 진행하는가 |
| 긴 요청 처리 중 짧은 요청 연속 도착 | 짧은 요청 TTFT와 긴 요청 최대 대기시간이 모두 제한되는가 |
| 실제100k 요청1–2개 | fragment1의 공급 한계와 안전하게 증명한 fragment2/4의 효과를 분리할 수 있는가 |
| 지속 웨이브·취소·연결 실패 | KV/반환/receipt 메모리가 안정되고 정상 해제·UNLOAD 및 부분 결과 보존이 유지되는가 |

고정100k context가 아니라 입력100000토큰+출력 예산을 확보한다. shared KV pool의 총 용량과 요청별
최대 context를 구별하고 실제 동시에 수용한 요청 수를 기록한다. GPU kernel/SM·메모리 대역폭 계측,
stage queue/native/transfer, 요청별 TTFT/ITL, 정상 응답 goodput을 각각 사용한다. batch512/512나
open8/8은 관측값이며 성공 조건이 아니다. 반복에서 처리량이 평탄해지면서 대기만 늘면 flight 확대를 멈추고,
native 비용이 늘면 chunk/배치를 재조정하며, 병목 stage가 계속 바쁘면 cut/offload 축으로 이동한다.
