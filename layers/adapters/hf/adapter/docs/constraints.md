# constraints

모델 class/tensor/KV를 Rust에서 해석하지 않는다. 부분 I/O는 uncertain이며 자동 retry하지 않는다. Python 환경은 LOAD readiness까지 준비 완료로 광고하지 않는다.

wire·예산·배포와 오류 의미는 [통합 명세](../../docs/integration/README.md)가 소유한다.
