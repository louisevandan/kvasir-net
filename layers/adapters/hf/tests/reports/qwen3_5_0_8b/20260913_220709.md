> 역사 기록: 독립 저장소 시점의 요구·상태·측정이다. 현재 배치와 사용법은 [HF 안내](../../../README.md)를 따른다. 원본 전체는 이관 시 보존한 Git bundle에 있다.

# Qwen3.5-0.8B 로컬 분할 실행 보고

실행일: 2026-09-13 KST. 시작 HEAD: `16fed3897a0df59ed11a633a6bf15f027b967855`, 시작 dirty 없음.
대상: [시험 계획](../../plans/qwen3_5_0_8b-20260913.md)의 전용 텍스트 스크립트.
이 보고를 포함하는 커밋이 구현 기준이며 아래 source manifest가 실제 실행 Python bytes를 봉인합니다.

## 환경과 판정

Windows, CPython 3.13.15, torch 2.14.0+cu130, transformers 5.17.0, safetensors 0.8.0,
huggingface-hub 1.31.0. PyTorch 장치 순서: cuda:0 RTX 4080 16GB, cuda:1 RTX 3090 24GB.
attention=eager, DeltaNet·causal convolution=PyTorch fallback, TF32 비활성, CPU threads=4.
물리 연산 배치=1, 각 노드는 같은 컴퓨터의 별도 Python 프로세스입니다.

모델: `Qwen/Qwen3.5-0.8B` revision `2fc06364715b967f1860aea9cf38778875588b17`.
공식 전체 safetensors 1,746,942,600 bytes의 SHA256:
`04b1c301231dd422b8860db31311ab2721511346a32cb1e079c4c4e5f1fe4696`.
그 외 파일은 [checkpoint manifest](../../../manifests/qwen3_5_0_8b/artifact/identity.json)에 기록했습니다.
고정 버전·공식 코드 출처·실행 방법은 [모델 문서](../../../docs/models/qwen3_5_0_8b/README.md)가 소유합니다.

첫 실행 전 고정한 기준은 logits `atol=0.125`, `rtol=0.01` 및 매 스텝 greedy token 동일입니다.
reference는 공식 전체 `Qwen3_5ForConditionalGeneration`에 동일 token을 teacher forcing합니다.
분할 실행은 실제 담당 weight만 적재하고 stage-local cache를 사용합니다. logits는 마지막 위치를 비교합니다.

## 최종 고정 소스 결과

명령: `.venv/Scripts/python.exe -B tools/verification/qwen3_5_0_8b/run.py`, exit 0.
아티팩트 루트: `artifacts/qwen/20260913T131913Z-6c3369ba/`.
모든 arm 시작부터 종료까지 production Python source hash가 같았습니다.

| 계획 / 시나리오 | 스텝 비교 | 최대 절대 logits 차이 | 판정 |
| --- | ---: | ---: | --- |
| single_gpu BF16 / short | 4 | 0 | 통과 |
| balanced_two_gpu_fp32 / short | 4 | 0.000014424324035644531 | 통과 |
| uneven_three_stage_fp32 / short | 4 | 0.000019669532775878906 | 통과 |
| attention_boundaries_fp32 / short | 4 | 0.000017642974853515625 | 통과 |
| cpu_gpu FP32 / short | 4 | 0.000019073486328125 | 통과 |
| single_cpu FP32 / short | 4 | 0 | 통과 |
| balanced_two_gpu_fp32 / chunked_prefill | 9 | 0.000011920928955078125 | 통과 |
| balanced_two_gpu_fp32 / interleaved_cancel | 14 | 0.000018358230590820312 | 통과 |

8개 조합·47개 비교 모두 allclose 및 greedy token 일치. 최초 오류·cleanup 오류 없음.
후속 아티팩트 검사에서 32개 stage release, 각 stage cache 층 수, 최종 잔여 상태 0,
production source 일치와 제공 계획 9개의 dependency-free inspect exit 0도 확인했습니다.
short는 산술 `4`, 한국어 `서울`을 EOS로 완료했습니다. 240-token 입력은 32-token chunk로 처리하고
`BLUE`를 EOS로 완료했습니다. 교차 실행은 `Counting`의 2 token에서 취소했고 다른 요청은 `6`, `Jupiter`를
EOS로 완료했습니다. 모든 요청의 release를 확인했으며 각 stage의 최종 active_sessions·remaining_cache_bytes는 0입니다.
GPU allocator의 예약 메모리 0이나 모델 weight 해제를 뜻하지 않습니다. worker는 종료 handshake 후 exit 0으로 회수합니다.

부분 적재 예: FP32 12/12 분할의 node-0은 2,013,343,360 bytes, node-1은 2,013,347,456 bytes의
담당 weight를 갖습니다. 각 stage cache는 12층이며 전역 레이어 번호를 cache-local index로 변환합니다.
별도 stage의 tied embedding/head 복제 비용이 포함됩니다. 단일 CPU FP32는 공유 weight 3,009,572,096 bytes입니다.
tensor별 이름·PID·장치·상태 위치·해제 byte는 각 arm `summary.json`에 있습니다.

reference 없이 사용자 생성 경로도 실행했습니다:
`.venv/Scripts/python.exe -B tools/qwen3_5_0_8b/cli/run.py run --plan plans/qwen3_5_0_8b/balanced_two_gpu_fp32/plan.json --scenario scenarios/qwen3_5_0_8b/short/scenario.json --output artifacts/qwen/final-run-smoke`.
exit 0, ok=true, first_error/cleanup_error=null입니다.

봉인 파일 SHA256:

- `source-manifest.json`: `2a16ef8d5f52ae70571a321d984e89c05c6fd9ea0c3c8387fa9357ebe2883f59`
- `matrix.json`: `c335940b1ba598170223ef0a4d36dcd1c13483fc501fb4e6fff47734f736f260`

## 실패 반례와 독립 변이

`python -B tools/testing/run.py`: 33개 통과, exit 0. 이 중 Qwen 계획·시나리오 8개, 기존 framing 25개입니다.
`.venv/Scripts/python.exe -B tools/verification/qwen_state/run.py`: 4개 통과, exit 0.
상태 시험은 effect-counting fixture로 잘못된 issue/position/shape/dtype/token/context, 중복·해제된 요청,
누적 요청 상한의 거절 시 연산과 상태 생성이 없음을 확인합니다. 실제 모델 증거는 위 matrix와 구분합니다.

독립 복사본 명령: `.venv/Scripts/python.exe -B tools/verification/qwen_mutation/run.py`, exit 0.
루트: `artifacts/qwen-mutation/20260913T131226Z-28e0554e/`. 원본 source hash 보존을 확인했습니다.
실제 checkpoint는 원본의 고정 파일을 읽기 전용으로 사용했습니다.

| 복사본 변경 | 실제 소비 경로·관측 | exit |
| --- | --- | ---: |
| 변경 없음 | 공식 실제 가중치 single_gpu verify 통과 | 0 |
| 연속 coverage 검사 제거 | gap/overlap 계획 거절 시험 실패 | 1 |
| tokenizer return_dict=False 제거 | 실제 tokenizer의 flat token ID 계약 거절; worker 시작 전 실패 | 1 |
| 중복 issue 검사 제거 | 중복 입력이 재실행되어 상태 시험 실패 | 1 |
| cache-local index 변환 제거 | 실제 2단 worker에서 IndexError; 이미 시작한 node-0 회수, cleanup 오류 없음 | 1 |

변이 요약 `summary.json` SHA256:
`c35d7af4cb640392f0c0f0840ace4308c9278ef9001dfe38b0a85bfecc44a125`.
복사본마다 수정 전후 hash, 정확한 명령과 원본 로그를 보존했습니다.
이 변이 및 일반 run smoke 이후 preparation/processes 파일 끝의 빈 줄만 제거했습니다.
네 변이 대상 파일은 불변이며 전체 Python AST 동일성은 `artifacts/qwen/final-format-equivalence.json`에 확인했습니다.
공백 정리 뒤 위 8개 실제 모델 arm 전체를 다시 실행해 최종 production bytes와의 일치를 확인했습니다.

## 보존한 실패와 증거 경계

- `artifacts/qwen/initial-single/`: readiness metadata의 16KiB 제한 초과. 실제 tensor 목록에 맞춰 64KiB로 수정했습니다.
- `artifacts/qwen/single-v2/`: Transformers 5 tokenizer 반환 계약 때문에 Encoding 변환 실패.
  명시적 return_dict=False와 flat ID admission을 추가했습니다. 위 독립 변이가 회귀를 검출합니다.
- `artifacts/qwen/20260913T125105Z-6aed197c/balanced_two_gpu--short/`: 4080+3090 BF16에서
  최대 절대 차이 0.16888427734375, allclose 실패. 첫 greedy token은 같았지만 실패입니다.
- `artifacts/qwen/balanced-same-gpu/`: 같은 4080 내 BF16 2단은 최대 차이 0.
  장치별 수치 경로 차이 가능성을 보여주지만 원인 kernel을 특정한 증거는 아닙니다.
- FP32는 별도 정밀도 arm입니다. BF16의 기준·입력을 사후 변경해 통과로 재분류하지 않았습니다.
  나머지 BF16 3단 실험 계획은 검증하지 않았습니다.
- `artifacts/qwen/20260913T125703Z-37006f69/`: 역할 폴더 분리 도중 single_cpu import 실패가 난 예비 실행입니다.
  최종 통과 근거에서 제외하고 소스 고정 후 전체 8개 arm을 다시 실행했습니다.
- `artifacts/qwen/20260913T130535Z-be183838/`: 8개 arm 통과, 이후 EOF 공백 정리 때문에 최종 소스로 전체 재실행했습니다.

실제 cache tensor 전 원소 비교, 미지의 prompt에 대한 일반 품질, 성능/SLO, 물리 여러 컴퓨터,
압축 weight/kernel, 물리 continuous batching, Rust retained bridge·P4 통합은 검증하지 않았습니다.
상세 로그는 Git 제외 로컬 증거이며 다른 컴퓨터의 수용을 증명하지 않습니다.
P4는 읽기 전용이며 관측 HEAD `6bd01d7e12f2711cb27e93b3b45533703e3caf9b`, dirty 없음입니다.

다음 작업: 이기종 BF16의 연산별 차이 감사 또는 승인된 다음 범위의 cache/bridge 검증.

문서 검사: `python -B tools/verification/documents/run.py`, 문서 23개·로컬 링크 104개·오류 0.
`git diff --check` 통과. 문서·manifest·환경 lock은 UTF-8/LF로 기록했습니다.
