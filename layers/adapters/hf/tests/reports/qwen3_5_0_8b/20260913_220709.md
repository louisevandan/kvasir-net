> Historical record: requirements, status and measurements from the standalone repository era. For current placement and usage, follow the [HF guide](../../../README.md). The full original is in the Git bundle preserved during the migration.

# Qwen3.5-0.8B local split execution report

Run date: 2026-09-13 KST. Starting HEAD: `16fed3897a0df59ed11a633a6bf15f027b967855`, clean at start.
Subject: the dedicated text scripts of the [test plan](../../plans/qwen3_5_0_8b-20260913.md).
The commit that contains this report is the implementation baseline, and the source manifest below seals the Python bytes actually executed.

## Environment and verdict

Windows, CPython 3.13.15, torch 2.14.0+cu130, transformers 5.17.0, safetensors 0.8.0,
huggingface-hub 1.31.0. PyTorch device order: cuda:0 RTX 4080 16GB, cuda:1 RTX 3090 24GB.
attention=eager, DeltaNet and causal convolution=PyTorch fallback, TF32 disabled, CPU threads=4.
Physical compute batch=1; each node is a separate Python process on the same computer.

Model: `Qwen/Qwen3.5-0.8B` revision `2fc06364715b967f1860aea9cf38778875588b17`.
SHA256 of the official full safetensors, 1,746,942,600 bytes:
`04b1c301231dd422b8860db31311ab2721511346a32cb1e079c4c4e5f1fe4696`.
Other files are recorded in the [checkpoint manifest](../../../manifests/qwen3_5_0_8b/artifact/identity.json).
The pinned versions, official code provenance and run instructions are owned by the [model document](../../../docs/models/qwen3_5_0_8b/README.md).

The criteria fixed before the first run are logits `atol=0.125`, `rtol=0.01` and an identical greedy token at every step.
The reference teacher-forces the same tokens into the official full `Qwen3_5ForConditionalGeneration`.
Split execution loads only the weights each stage actually owns and uses a stage-local cache. Logits are compared at the last position.

## Results on the final pinned source

Command: `.venv/Scripts/python.exe -B tools/verification/qwen3_5_0_8b/run.py`, exit 0.
Artifact root: `artifacts/qwen/20260913T131913Z-6c3369ba/`.
The production Python source hash was the same from the start to the end of every arm.

| Plan / scenario | Step comparisons | Max absolute logits difference | Verdict |
| --- | ---: | ---: | --- |
| single_gpu BF16 / short | 4 | 0 | pass |
| balanced_two_gpu_fp32 / short | 4 | 0.000014424324035644531 | pass |
| uneven_three_stage_fp32 / short | 4 | 0.000019669532775878906 | pass |
| attention_boundaries_fp32 / short | 4 | 0.000017642974853515625 | pass |
| cpu_gpu FP32 / short | 4 | 0.000019073486328125 | pass |
| single_cpu FP32 / short | 4 | 0 | pass |
| balanced_two_gpu_fp32 / chunked_prefill | 9 | 0.000011920928955078125 | pass |
| balanced_two_gpu_fp32 / interleaved_cancel | 14 | 0.000018358230590820312 | pass |

All 8 combinations and 47 comparisons are allclose with matching greedy tokens. No first error or cleanup error.
A follow-up artifact check also confirmed 32 stage releases, the layer count of each stage cache, final residual state 0,
matching production source, and dependency-free inspect exit 0 for the 9 provided plans.
short completed the arithmetic `4` and the Korean `서울` (English: "Seoul") with EOS. The 240-token input was processed in 32-token chunks and
completed `BLUE` with EOS. The interleaved run cancelled `Counting` at 2 tokens, and the other requests completed `6` and `Jupiter`
with EOS. Release was confirmed for every request, and each stage's final active_sessions and remaining_cache_bytes are 0.
This does not mean 0 reserved memory in the GPU allocator or that model weights were released. The worker is reclaimed with exit 0 after the shutdown handshake.

Partial-load example: in the FP32 12/12 split, node-0 holds 2,013,343,360 bytes and node-1 holds 2,013,347,456 bytes
of owned weights. Each stage cache has 12 layers and maps global layer numbers to cache-local indices.
This includes the cost of duplicating the tied embedding/head in separate stages. Single-CPU FP32 has 3,009,572,096 bytes of shared weights.
Per-tensor names, PIDs, devices, state locations and released bytes are in each arm's `summary.json`.

The user generation path was also run without a reference:
`.venv/Scripts/python.exe -B tools/qwen3_5_0_8b/cli/run.py run --plan plans/qwen3_5_0_8b/balanced_two_gpu_fp32/plan.json --scenario scenarios/qwen3_5_0_8b/short/scenario.json --output artifacts/qwen/final-run-smoke`.
exit 0, ok=true, first_error/cleanup_error=null.

Sealed file SHA256:

- `source-manifest.json`: `2a16ef8d5f52ae70571a321d984e89c05c6fd9ea0c3c8387fa9357ebe2883f59`
- `matrix.json`: `c335940b1ba598170223ef0a4d36dcd1c13483fc501fb4e6fff47734f736f260`

## Failing counterexamples and independent mutations

`python -B tools/testing/run.py`: 33 passed, exit 0. Of these, 8 are Qwen plan/scenario tests and 25 are existing framing tests.
`.venv/Scripts/python.exe -B tools/verification/qwen_state/run.py`: 4 passed, exit 0.
The state tests use an effect-counting fixture to confirm that rejecting a bad issue/position/shape/dtype/token/context, a duplicate or released request,
or the cumulative request cap performs no computation and creates no state. Real model evidence is kept separate in the matrix above.

Independent copy command: `.venv/Scripts/python.exe -B tools/verification/qwen_mutation/run.py`, exit 0.
Root: `artifacts/qwen-mutation/20260913T131226Z-28e0554e/`. Preservation of the original source hash was confirmed.
The real checkpoint used the original's pinned files read-only.

| Change in the copy | Actual consumption path / observation | exit |
| --- | --- | ---: |
| No change | Official real-weight single_gpu verify passed | 0 |
| Remove the contiguous coverage check | The gap/overlap plan rejection test fails | 1 |
| Remove tokenizer return_dict=False | The real tokenizer's flat token ID contract is rejected; fails before worker start | 1 |
| Remove the duplicate issue check | A duplicate input is re-executed and the state test fails | 1 |
| Remove the cache-local index mapping | IndexError in a real 2-stage worker; the already started node-0 is reclaimed, no cleanup error | 1 |

Mutation summary `summary.json` SHA256:
`c35d7af4cb640392f0c0f0840ace4308c9278ef9001dfe38b0a85bfecc44a125`.
For each copy, the before/after hashes, exact command and raw logs are preserved.
After these mutations and the general run smoke, only the trailing blank lines at the end of the preparation/processes files were removed.
The four mutation target files are unchanged, and full Python AST equivalence is confirmed in `artifacts/qwen/final-format-equivalence.json`.
After the whitespace cleanup, all 8 real model arms above were rerun to confirm a match with the final production bytes.

## Preserved failures and evidence boundaries

- `artifacts/qwen/initial-single/`: exceeded the 16KiB limit on readiness metadata. Changed to 64KiB to fit the actual tensor list.
- `artifacts/qwen/single-v2/`: Encoding conversion failed because of the Transformers 5 tokenizer return contract.
  Added an explicit return_dict=False and flat ID admission. The independent mutation above detects a regression.
- `artifacts/qwen/20260913T125105Z-6aed197c/balanced_two_gpu--short/`: on 4080+3090 BF16,
  max absolute difference 0.16888427734375, allclose failed. The first greedy token matched, but it is a failure.
- `artifacts/qwen/balanced-same-gpu/`: BF16 2-stage within the same 4080 has max difference 0.
  This suggests a possible per-device numeric path difference but is not evidence that pinpoints the responsible kernel.
- FP32 is a separate precision arm. The BF16 criteria and inputs were not changed after the fact to reclassify it as a pass.
  The remaining BF16 3-stage experiment plans were not verified.
- `artifacts/qwen/20260913T125703Z-37006f69/`: a preliminary run in which the single_cpu import failed while splitting role folders.
  It is excluded from the final pass evidence, and all 8 arms were rerun after the source was pinned.
- `artifacts/qwen/20260913T130535Z-be183838/`: 8 arms passed; the whole set was then rerun on the final source because of the EOF whitespace cleanup.

Not verified: element-wise comparison of all actual cache tensors, general quality on unseen prompts, performance/SLO, multiple physical computers,
compressed weights/kernels, physical continuous batching, and the Rust retained bridge / P4 integration.
The detailed logs are local evidence excluded from Git and do not prove acceptance on other computers.
P4 was read-only; observed HEAD `6bd01d7e12f2711cb27e93b3b45533703e3caf9b`, clean.

Next work: an operation-by-operation audit of the heterogeneous BF16 difference, or cache/bridge verification for the next approved scope.

Documentation check: `python -B tools/verification/documents/run.py`, 23 documents, 104 local links, 0 errors.
`git diff --check` passed. Documents, manifests and environment locks were written as UTF-8/LF.
