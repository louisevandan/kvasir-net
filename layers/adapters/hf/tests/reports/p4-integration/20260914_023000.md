> 역사 기록: 독립 저장소 시점의 요구·상태·측정이다. 현재 배치와 사용법은 [HF 안내](../../../README.md)를 따른다. 원본 전체는 이관 시 보존한 Git bundle에 있다.

# P4 HF 통합 실행 보고

2026-09-14 KST. §0 HF-0~3 수용 완료. 소형 모델의 통합 capability 판정이며 성능 승격이 아니다.
[계획](../../plans/p4-integration-20260914.md) · [명령·예산·wire](../../../docs/integration/README.md).

## 소스·환경·소유권

착수 P4 `1553d5aa1fb3ddee8cb4aee5c2224034a5ccd905`, HF `df4f81b7c774e60cba78d71ad868c515e619cb15`, 양쪽 dirty 0.
최종 실행 runtime은 P4 `0bd3734a9354c5b40a0575ce9a78bdd3e1128efb` + HF `6d8144d9cf5d63cca9220ee34a8da85cb72c4d95`다.
후속 `7a4a2dc`/`3149c4f`는 시험·배포 도구와 문서만 바꿨다. HF `python/`, `crates/`, runtime lock과
P4 Rust 소비 소스는 최종 실기 이후 변경하지 않았다. 각 model summary는 실행 중 source SHA256 보존을 검사한다.

- HF 소유: Rust construction/retained/process/ipc/lifecycle, 모델별 Python scheduling/event_worker/event_pipeline/epoch/cache_export,
  환경 lock·bundle·실행·변이·복원 도구. P4는 optional feature/외부 의존성/root lock, 정적 factory 생성·INSPECT·타입 생성 시험을 소유한다.
  공통 core/protocol/trait/기존 llama runtime은 수정하지 않았다.
- Qwen revision `2fc06364715b967f1860aea9cf38778875588b17`, quantization none, CPython 3.13.15,
  torch 2.14.0+cu130, transformers 5.17.0. eager attention 및 torch DeltaNet fallback이다.
- FP32 기본 profile: 24층 [0,12)/[12,24), context2048, 요청 한도8, 출력 cap64, physical batch1,
  outstanding model step1. logits/cache atol=0.125, rtol=0.01과 매 스텝 greedy 일치 기준을 유지했다.
- HF 활성 agent SHA256: `2330db74bd1f5b3a7fb364ee19c3c63ab4ea057c940b292f85985d2344533434`.
  hikaTR PID98832, m42-server2 PID3684가 같은 파일을 실행했다. Python A/B와 llama 실기도 이 파일을 사용했다.
- feature off SHA256: `d84f9390ed7bd1717ae2e5eb6478c6caff03ec21fe0c0d8817f39fe9e976757d`.
  binary/native/GGUF 크기·hash는 P4 `target/hf-acceptance/accepted-binary-model-hashes.json`에 있다.

## 계약 시험

`target/`, `artifacts/`는 이 HF 저장소의 Git 제외 실행 증거다. P4 경로는 별도 표기한다.

| ID | 실행·판정 | 결과·증거 |
| --- | --- | --- |
| HF-PKG | root locked metadata, 두 실제 trait factory, 독립 duplicate-source Cargo fixture | adapter/protocol 각각 0.9.0 package ID 하나. 별도 protocol0.9.1 source의 실제 Cargo graph를 gate가 거부. `artifacts/integration/accepted-package-graph/` |
| HF-REGISTER | 활성 agent의 llama/HF 생성, off INSPECT와 HF CREATE 거부 | 활성 kinds 두 개, off는 llama 하나. `artifacts/integration/accepted-inspect-simultaneous.json`, `accepted-inspect-feature-off.json`, `accepted-disabled-create.json`; 아래 llama 실기 |
| HF-RETAIN / HF-IPC / HF-LIFE | 실제 retained+venv Python pipe 시험9개 | 9/0/0, `target/final-bridge.log`. exact allocation/claim, 4096/4097 byte, Full/Closed, held/peek/front/wake, epoch/unload 거부, EOF/partial/framing/identity/timeout fence·abort |
| HF-LIFE / HF-SWAP | P4 CREATE/LOAD/실패/abort/DELETE fixture12종 | 12 PASS, `artifacts/integration/final-lifecycle/summary.json`. 정상, ready mismatch, death, partial, magic/version/reserved/length/identity/hang, incompatible/hash mismatch |
| HF-MODEL | 기존 v1 matrix 재실행 | 8조합·47 logits PASS, `artifacts/qwen/20260913T174052Z-30110bc0/matrix.json` |
| HF-MODEL / HF-SWAP | 최종 agent P4 event local matrix | 10 case·99 logits·198 stage-cache PASS, `artifacts/integration/final-local/matrix.json` |
| HF-DIST | 두 물리 host의 P4 stage 전달 | 4 case·75 logits·150 stage-cache PASS, `artifacts/integration/accepted-distributed-matrix/matrix.json` |
| HF-DIST 회수 | 응답 frame 도중 TCP 단절→명시 포기→동일 agent 재수용 | PASS, 복구4 logits/8 stage-cache. `artifacts/integration/accepted-distributed-recovery/summary.json` |
| Python 회귀 | testing, qwen_state, qwen_cache runner | 각각33/0,4/0,4/0. `target/accepted-python-tests.log`, `accepted-qwen-state.log`, `cache-epoch-tests.log` |

Rust 명령은 HF root에서 실제 venv를 `HF_TEST_PYTHON`으로 지정한
`cargo test --locked --no-fail-fast -- --test-threads=1`이다. 필수 시험을 ignore하지 않았다.
P4 broker 소비 증거는 lifecycle/model matrix이며 직접 mailbox 시험을 broker 전체 포화 시험으로 부르지 않는다.
모델 input/completion queue 각각1, retained count2, 각 store128MiB, frame32MiB, IPC body scratch128MiB,
stderr64KiB다. metadata/weight/cache와 기존 broker receipt 저장소는 별도다. 전체 RSS 상한 또는 장기 broker 메모리 안정성 증명이 아니다.

Windows venv launcher가 실제 interpreter를 자식으로 만드는 반례를 확인했다. suspended child를 owned Job에
결속한 뒤 실행하고 abort ack 전에 손자 process handle의 종료를 검사한다. startup 회수 오류는 첫 오류와
분리하며 supervisor를 보존한다. 프로세스 전역 종료를 하지 않는다.

## 모델·재수용·교체

| local event case | logits | 전체 stage-cache 비교 |
| --- | ---: | ---: |
| single GPU BF16 | 4 | 4 |
| 균등2 GPU FP32 | 4 | 8 |
| 불균등3 stage FP32 | 4 | 12 |
| attention boundary3 stage FP32 | 4 | 12 |
| CPU/GPU FP32 | 4 | 8 |
| 단일 CPU FP32 | 4 | 4 |
| chunked prefill | 9 | 18 |
| 요청 교차·취소·재사용 | 14 | 28 |
| 같은 LOAD의 8건×3 epoch | 48 | 96 |
| 같은 agent에서 bundle B 교체 | 4 | 8 |

stage-cache 비교는 해당 레이어 attention keys/values·conv/recurrent의 전체 원소와 구성/shape/dtype를 검사한다.
198은 원소 수가 아니라 step×stage 비교 횟수다. 모든 case의 first_error/cleanup_error=null, source_unchanged=true,
정상 UNLOAD/DELETE다. A/B는 실제 entry Python의 build label과 bundle hash가 다르며 readiness에서 확인했다.
실행 디렉터리를 덮어쓰지 않았다.

일반 생성도 고정 기대값으로 별도 판정했다: 산술 `4`, 한국어 `서울`, 문맥 `BLUE`, 교차 정상 요청 `Jupiter`,
재사용 요청 `6`. 정상 요청은 EOS/release 모두 확인했다. 취소 요청 `Counting`은
`cancelled_at_step_boundary`이며 정상 EOS 또는 kernel 강제 중단으로 세지 않는다.
epoch마다 같은 `reused-wave-*` ID8개를 써서24건을 처리했다. cap8을 늘리거나 v1 retired guard를 삭제하지 않았다.
barrier 뒤 이전 epoch step/release/cancel은 거부되며 새 요청을 계속 처리한다.

두 host는 hikaTR `192.168.0.6:51056` CPU [0,12)와 m42-server2 `192.168.0.29:41981` RTX3090 cuda:0 [12,24)다.
마지막 epoch case worker PID109948/10024, weight bytes2,013,343,360/2,013,347,456, remote CUDA allocated2,013,356,544.
OUTER 송수신103,074/1,067,296,994 bytes에는 cache 조회가 포함되므로 생성 bandwidth/TPS 지표가 아니다.
summary에 PID/host/device/담당 tensor, plan/bundle/source hash, job/receipt/source endpoint와 전송 trace가 있다.
controller는 worker pipe를 직접 잡지 않고 Rust retained→P4 broker→다음 node를 소비했다.
원격 고정 Python/환경/checkpoint/bundle은 `C:/Users/42mob/p4-hf-acceptance-20260914/`에 별도로 배치했다.
원래 실패한 generation 재사용과 개별 후속 실행을 최종 합계에 중복 가산하지 않았다.

별도 단절 arm은 두 호스트의 응답994,471 bytes 중 길이 prefix4 bytes만 받은 뒤 실제 TCP 연결을 끊었다.
model result 승인0을 유지하고 같은 OUTER 신원으로 다시 연결해 LOAD 전체를 abort/DELETE했다.
같은 agent에서 새 node/LOAD generation을 적재한 뒤 과거 LOAD 명령을 거부하고 short 정상 답변3건,
4 logits/8 stage-cache, UNLOAD/DELETE를 완료했다. first_error/cleanup_error=null, source_unchanged=true다.
이는 OUTER 반환 연결 단절 검증이며 물리 host 전원 장애의 durable 복구가 아니다.

실제 전체 argv는 각 matrix.json, 원격 구성은 `artifacts/integration/distributed-config/`에 있다.
[통합 명세](../../../docs/integration/README.md)를 따라 재실행 시 새 node generation을 준다.

## 기존 llama.cpp와 전체 Rust

활성/비활성 agent 모두 실제 Qwen2.5-1.5B-Instruct-Q8_0 GGUF를 기존 p4_staged_server.exe의 CPU2stage
[0,14)/[14,28)로 실행했다. context512/batch128/threads4, sampler `{"temperature":0}`.
두 실행 모두 응답 `4`, completed=1/released=1, passed=true/error=null/cleanup_error=null, UNLOAD/DELETE 완료다.

- P4 `target/hf-acceptance/llama-final-result.json`, `llama-final.log`: HF와 동일 agent 바이너리.
- P4 `target/hf-acceptance/llama-accepted-disabled-result.json`, `llama-accepted-disabled.log`: feature off.
- native source/ABI는 각 result의 build, model/binary hash는 위 manifest에 있다.

| P4 전체 명령 | 최종 종료 | 전체 집계 |
| --- | ---: | --- |
| `cargo test --workspace --no-fail-fast --features hf-transformers` | 0 | 58 summary,1450 passed/0 failed/7 ignored |
| `cargo test --workspace --no-fail-fast` | 0 | 58 summary,1450 passed/0 failed/7 ignored |

로그와 exit는 P4 `target/hf-acceptance/accepted-workspace-enabled.*`, `accepted-workspace-disabled.*`다.
외부 HF9개는 workspace 합계에 넣지 않았고 ignored7개는 통과가 아니다.
기존 timer `phase_pacing_actual_loop_expires_decode_wait_without_another_tail_or_input`도 양쪽 PASS였다.
timer 구현은 수정하지 않았으므로 과거 RED의 일반적 해결을 주장하지 않으며 Release A 역사 증거를 보존한다.

## 독립 변이·실패 이력

| 묶음 | baseline / 검출 | 증거 |
| --- | --- | --- |
| Rust epoch/result identity/input bound/held barrier/owned tree | baseline9 PASS, 변이5개 각각 재컴파일 후 assertion 실패(exit101) | `artifacts/integration/final-rust-mutations/summary.json`; 별도 source/target/binary SHA256 |
| cache 전체 원소/live epoch/old epoch | baseline4 PASS, 변이3개 unittest FAILED(exit1) | `artifacts/integration/cache-epoch-mutations/summary.json` |
| 기존 Qwen coverage/tokenizer/duplicate/local cache index | baseline PASS, 변이4개 기대 실패(exit1), source_preserved=true | `artifacts/qwen-mutation/20260913T182343Z-00143cfb/summary.json` |

독립 source 복사본에서 guard를 제거했다. 원본 checkout의 변이·checkout/reset 복원은 하지 않았다.
owned_tree 변이는 abort ack 뒤 손자가 생존한 OS handle 단언으로 검출했다.

1. 최초 UTF-8 파일 cp949 read 실패는 명시 UTF-8로 수정했다. 출력 예약 확대 중 기존4KiB 시험이 막힌
   `target/bridge-tests-03.log`를 보존했다. 상한을 올리지 않고 frame 내 receipt+body 검사와 초과 결과 fence를 수정했다.
2. launcher만 종료해 Python이 남는 반례를 독립 repair worktree에서 고정했다. 최종 실기는 owned Job 포함 바이너리로 다시 실행했다.
3. 최초 P4 `llama-enabled-result.json`은 JSON sampler에 CLI 문자열 `--temp 0`을 준 입력 오류다.
   memory_dirty/cleanup_error를 보존하고 올바른 계약 입력으로 재실행했다. 정상 응답 기대값은 바꾸지 않았다.
4. 최초 remote matrix 두 번째 case는 DELETE된 generation1을 재사용해 StaleNode로 거부됐다.
   `artifacts/integration/final-distributed/`를 보존했다. runner가 case마다 generation을 증가시키도록 수정한
   최종 matrix는5~8로 네 case 전체를 완료했다. P4 stale guard는 바꾸지 않았다.
5. Windows 제외 포트52004 bind, 초기 포트 접근, SSH 종료에 따른 시험 agent 종료는 환경 실패로 보존했다.
   사용 가능한51056과 시험 프로세스 전용 원격 접근으로 검증했다. 다른 fleet 프로세스는 종료하지 않았다.

## 재현·마감·범위

`artifacts/integration/final-source-bundle/sources.json`의 두 runtime commit을 원본과 별도인
`artifacts/integration/final-source-reproduction/`에 복원한
`cargo build --locked -p p4-agent --features hf-transformers`는 exit0이다.
출하 도구는 source zip2개/commit/hash/standalone restore.py를 함께 내보낸다.
원본 checkout path 없이 새 인접 디렉터리에서 빌드하며 debug 경로가 다른 binary의 bit-identical 재현은 주장하지 않는다.
환경/weight/worker bundle은 source archive와 별도다.

최종 INSPECT는 활성/비활성/원격 모두 nodes=0이다: `artifacts/integration/accepted-final-inspect-*.json`.
각 agent의 model/native worker0을 확인한 뒤 시험 agent만 종료했고 원격 임시 방화벽3개를 제거했다.
P4 `target/hf-acceptance/accepted-local-cleanup.json`, `accepted-remote-cleanup.json`에 기록했다.
OS console host는 model worker와 구분했다. 실패 artifact와 준비한 source/환경은 보존했다.
최종 출하 commit과 복원 결과는 `artifacts/integration/delivery-source-bundle/sources.json` 및
`artifacts/integration/delivery-source-reproduction/reproduction.json`을 함께 확인한다.
문서 검사는 P4 `npm run docs-lint` 101 files clean, HF `tools/verification/documents/run.py`
26 documents/111 local links/errors0이다. 로그는 각 저장소 `target`의 `accepted-docs-lint.log`에 있다
(P4는 `target/hf-acceptance/`). 문서 검사는 실행·의미 검증을 대체하지 않는다.

이기종 RTX4080+3090 BF16 분할은 기존 logits FAIL을 유지한다. FP32/단일 GPU BF16 PASS로 대체하지 않았다.
양자화, physical batching, 임의 모델 지원, host 전체 장애의 durable 복구, 초대형 H0–H7/성능·SLO는 범위 밖이다.
다음 P4 작업은 기존 Release A이며 이번 §0 요청으로 자동 착수하지 않는다. 원격 push는 수행하지 않았다.
