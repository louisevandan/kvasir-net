> Historical record: requirements, status and measurements from the time of the standalone repository. For the current layout and usage, follow the [HF guide](../../../README.md). The full original is in the Git bundle preserved during the migration.

# P4 HF integration run report

2026-09-14 KST. §0 HF-0~3 acceptance complete. This is an integration capability verdict for a small model, not a performance promotion.
[Plan](../../plans/p4-integration-20260914.md) · [commands, budgets, wire](../../../docs/integration/README.md).

## Source, environment and ownership

Start: P4 `1553d5aa1fb3ddee8cb4aee5c2224034a5ccd905`, HF `df4f81b7c774e60cba78d71ad868c515e619cb15`, both with 0 dirty files.
The final runtime was P4 `0bd3734a9354c5b40a0575ce9a78bdd3e1128efb` + HF `6d8144d9cf5d63cca9220ee34a8da85cb72c4d95`.
The follow-up commits `7a4a2dc`/`3149c4f` changed only test/deployment tools and documents. HF `python/`, `crates/`, the runtime lock and
the P4 Rust consuming sources were not changed after the final real-hardware run. Each model summary checks that the source SHA256 stayed the same during the run.

- HF owns: Rust construction/retained/process/ipc/lifecycle, the per-model Python scheduling/event_worker/event_pipeline/epoch/cache_export,
  and the environment lock, bundle, run, mutation and restore tools. P4 owns the optional feature, external dependency, root lock, and the static factory construction, INSPECT and type construction tests.
  The shared core/protocol/trait and the existing llama runtime were not modified.
- Qwen revision `2fc06364715b967f1860aea9cf38778875588b17`, quantization none, CPython 3.13.15,
  torch 2.14.0+cu130, transformers 5.17.0. Eager attention and the torch DeltaNet fallback.
- FP32 default profile: 24 layers [0,12)/[12,24), context 2048, request limit 8, output cap 64, physical batch 1,
  outstanding model step 1. The logits/cache criteria atol=0.125, rtol=0.01 and greedy match at every step were kept.
- HF-enabled agent SHA256: `2330db74bd1f5b3a7fb364ee19c3c63ab4ea057c940b292f85985d2344533434`.
  hikaTR PID 98832 and m42-server2 PID 3684 ran the same file. The Python A/B and the llama real-hardware run also used this file.
- feature off SHA256: `d84f9390ed7bd1717ae2e5eb6478c6caff03ec21fe0c0d8817f39fe9e976757d`.
  Binary/native/GGUF sizes and hashes are in P4 `target/hf-acceptance/accepted-binary-model-hashes.json`.

## Contract tests

`target/` and `artifacts/` are run evidence excluded from Git in this HF repository. P4 paths are marked separately.

| ID | Run and verdict | Result and evidence |
| --- | --- | --- |
| HF-PKG | root locked metadata, two real trait factories, independent duplicate-source Cargo fixture | one 0.9.0 package ID each for adapter/protocol. The gate rejects a real Cargo graph with a separate protocol 0.9.1 source. `artifacts/integration/accepted-package-graph/` |
| HF-REGISTER | llama/HF construction on the enabled agent, off INSPECT, and HF CREATE rejection | two kinds when enabled, only llama when off. `artifacts/integration/accepted-inspect-simultaneous.json`, `accepted-inspect-feature-off.json`, `accepted-disabled-create.json`; llama real-hardware run below |
| HF-RETAIN / HF-IPC / HF-LIFE | 9 real retained + venv Python pipe tests | 9/0/0, `target/final-bridge.log`. exact allocation/claim, 4096/4097 byte, Full/Closed, held/peek/front/wake, epoch/unload rejection, EOF/partial/framing/identity/timeout fence and abort |
| HF-LIFE / HF-SWAP | 12 P4 CREATE/LOAD/failure/abort/DELETE fixtures | 12 PASS, `artifacts/integration/final-lifecycle/summary.json`. normal, ready mismatch, death, partial, magic/version/reserved/length/identity/hang, incompatible/hash mismatch |
| HF-MODEL | re-run of the existing v1 matrix | 8 combinations, 47 logits PASS, `artifacts/qwen/20260913T174052Z-30110bc0/matrix.json` |
| HF-MODEL / HF-SWAP | P4 event local matrix on the final agent | 10 cases, 99 logits, 198 stage-cache PASS, `artifacts/integration/final-local/matrix.json` |
| HF-DIST | P4 stage delivery across two physical hosts | 4 cases, 75 logits, 150 stage-cache PASS, `artifacts/integration/accepted-distributed-matrix/matrix.json` |
| HF-DIST reclaim | TCP disconnect mid response frame → explicit abandonment → re-acceptance on the same agent | PASS, recovery 4 logits/8 stage-cache. `artifacts/integration/accepted-distributed-recovery/summary.json` |
| Python regressions | testing, qwen_state, qwen_cache runners | 33/0, 4/0, 4/0 respectively. `target/accepted-python-tests.log`, `accepted-qwen-state.log`, `cache-epoch-tests.log` |

The Rust command, run from the HF root with the real venv set as `HF_TEST_PYTHON`, is
`cargo test --locked --no-fail-fast -- --test-threads=1`. No required tests were ignored.
The P4 broker consumption evidence is the lifecycle/model matrix; the direct mailbox tests are not called a full broker saturation test.
Model input/completion queues are 1 each, retained count 2, each store 128 MiB, frame 32 MiB, IPC body scratch 128 MiB,
stderr 64 KiB. Metadata/weights/cache and the existing broker receipt store are separate. This is not proof of a total RSS cap or of long-term broker memory stability.

A counterexample was confirmed where the Windows venv launcher spawns the real interpreter as a child. The suspended child is bound to an owned Job
before it runs, and termination of the grandchild process handle is checked before the abort ack. Startup reclaim errors are kept separate from the first error,
and the supervisor is preserved. No process-wide kill is performed.

## Model, re-acceptance and swap

| local event case | logits | full stage-cache comparisons |
| --- | ---: | ---: |
| single GPU BF16 | 4 | 4 |
| even 2 GPU FP32 | 4 | 8 |
| uneven 3 stage FP32 | 4 | 12 |
| attention boundary 3 stage FP32 | 4 | 12 |
| CPU/GPU FP32 | 4 | 8 |
| single CPU FP32 | 4 | 4 |
| chunked prefill | 9 | 18 |
| interleaved requests, cancel, reuse | 14 | 28 |
| 8 requests × 3 epochs within one LOAD | 48 | 96 |
| bundle B swap on the same agent | 4 | 8 |

A stage-cache comparison checks every element of that layer's attention keys/values and conv/recurrent state, plus configuration/shape/dtype.
198 is the number of step×stage comparisons, not an element count. Every case has first_error/cleanup_error=null, source_unchanged=true,
and a normal UNLOAD/DELETE. A and B differ in the build label and bundle hash of the actual entry Python, confirmed at readiness.
Run directories were not overwritten.

Ordinary generation was also judged separately against fixed expected values: arithmetic `4`, Korean `서울` (English: "Seoul"), context `BLUE`, interleaved normal request `Jupiter`,
reuse request `6`. Normal requests were confirmed for both EOS and release. The cancelled request `Counting` is
`cancelled_at_step_boundary` and is not counted as a normal EOS or a forced kernel interruption.
Each epoch used the same 8 `reused-wave-*` IDs to process 24 requests. cap8 was not raised and the v1 retired guard was not removed.
After the barrier, step/release/cancel from the previous epoch are rejected while new requests keep being processed.

The two hosts are hikaTR `192.168.0.6:51056` CPU [0,12) and m42-server2 `192.168.0.29:41981` RTX3090 cuda:0 [12,24).
Last epoch case: worker PIDs 109948/10024, weight bytes 2,013,343,360/2,013,347,456, remote CUDA allocated 2,013,356,544.
OUTER send/receive of 103,074/1,067,296,994 bytes includes cache queries, so it is not a generation bandwidth/TPS metric.
The summary contains PID/host/device/assigned tensors, plan/bundle/source hashes, job/receipt/source endpoints and the transfer trace.
The controller did not hold the worker pipes directly; it consumed Rust retained → P4 broker → next node.
The pinned remote Python/environment/checkpoint/bundle were deployed separately to `C:/Users/42mob/p4-hf-acceptance-20260914/`.
The originally failed generation reuse and the individual follow-up runs were not double-counted in the final totals.

A separate disconnect arm cut the real TCP connection after receiving only the 4-byte length prefix of a 994,471-byte response between the two hosts.
It kept model result approvals at 0, reconnected with the same OUTER identity, and aborted/DELETEd the whole LOAD.
After loading a new node/LOAD generation on the same agent, it rejected the old LOAD command and completed 3 normal short answers,
4 logits/8 stage-cache, and UNLOAD/DELETE. first_error/cleanup_error=null, source_unchanged=true.
This verifies an OUTER return-connection disconnect; it is not durable recovery from a physical host power failure.

The full actual argv is in each matrix.json, and the remote configuration is in `artifacts/integration/distributed-config/`.
Following the [integration specification](../../../docs/integration/README.md), a re-run gives a new node generation.

## Existing llama.cpp and full Rust

Both the enabled and disabled agents ran a real Qwen2.5-1.5B-Instruct-Q8_0 GGUF on the existing p4_staged_server.exe as CPU 2-stage
[0,14)/[14,28). context 512/batch 128/threads 4, sampler `{"temperature":0}`.
Both runs gave response `4`, completed=1/released=1, passed=true/error=null/cleanup_error=null, and completed UNLOAD/DELETE.

- P4 `target/hf-acceptance/llama-final-result.json`, `llama-final.log`: the same agent binary as HF.
- P4 `target/hf-acceptance/llama-accepted-disabled-result.json`, `llama-accepted-disabled.log`: feature off.
- The native source/ABI is in each result's build, and the model/binary hashes are in the manifest above.

| P4 full command | Final exit | Full tally |
| --- | ---: | --- |
| `cargo test --workspace --no-fail-fast --features hf-transformers` | 0 | 58 summary,1450 passed/0 failed/7 ignored |
| `cargo test --workspace --no-fail-fast` | 0 | 58 summary,1450 passed/0 failed/7 ignored |

Logs and exit codes are in P4 `target/hf-acceptance/accepted-workspace-enabled.*` and `accepted-workspace-disabled.*`.
The 9 external HF tests are not included in the workspace total, and the 7 ignored are not passes.
The existing timer test `phase_pacing_actual_loop_expires_decode_wait_without_another_tail_or_input` also PASSed on both sides.
The timer implementation was not modified, so no general fix of the past RED is claimed, and the Release A historical evidence is preserved.

## Independent mutations and failure history

| Bundle | baseline / detection | Evidence |
| --- | --- | --- |
| Rust epoch/result identity/input bound/held barrier/owned tree | baseline 9 PASS; each of 5 mutations recompiled and failed an assertion (exit 101) | `artifacts/integration/final-rust-mutations/summary.json`; separate source/target/binary SHA256 |
| cache full elements/live epoch/old epoch | baseline 4 PASS; 3 mutations with unittest FAILED (exit 1) | `artifacts/integration/cache-epoch-mutations/summary.json` |
| existing Qwen coverage/tokenizer/duplicate/local cache index | baseline PASS; 4 mutations with expected failure (exit 1), source_preserved=true | `artifacts/qwen-mutation/20260913T182343Z-00143cfb/summary.json` |

Guards were removed in independent source copies. No mutation of the original checkout and no checkout/reset restore were done.
The owned_tree mutation was detected by the OS handle assertion that the grandchild survived after the abort ack.

1. The initial cp949 read failure on a UTF-8 file was fixed with explicit UTF-8. `target/bridge-tests-03.log`, where an existing 4 KiB test hung
   while the output reservation was being enlarged, is preserved. Instead of raising the limit, the receipt+body check within the frame and the fence for oversized results were fixed.
2. The counterexample where terminating only the launcher leaves Python running was pinned down in an independent repair worktree. The final real-hardware run was repeated with the binary that includes the owned Job.
3. The initial P4 `llama-enabled-result.json` was an input error that passed the CLI string `--temp 0` to the JSON sampler.
   memory_dirty/cleanup_error were preserved and the run was repeated with correct contract input. The expected normal response was not changed.
4. The second case of the initial remote matrix reused the DELETEd generation 1 and was rejected with StaleNode.
   `artifacts/integration/final-distributed/` is preserved. After fixing the runner to increment the generation per case,
   the final matrix completed all four cases with 5~8. The P4 stale guard was not changed.
5. The bind on Windows-excluded port 52004, initial port access, and test agent termination caused by SSH exit are preserved as environment failures.
   Verification used the available port 51056 and remote access dedicated to the test processes. No other fleet processes were terminated.

## Reproduction, closing and scope

`cargo build --locked -p p4-agent --features hf-transformers`, run after restoring the two runtime commits from
`artifacts/integration/final-source-bundle/sources.json` into `artifacts/integration/final-source-reproduction/`, separate from the original,
exited 0.
The delivery tool exports 2 source zips, the commits, hashes and a standalone restore.py together.
It builds in a new sibling directory without the original checkout path, and no bit-identical reproduction of binaries with different debug paths is claimed.
The environment/weights/worker bundle are separate from the source archive.

Final INSPECT shows nodes=0 on the enabled, disabled and remote agents: `artifacts/integration/accepted-final-inspect-*.json`.
After confirming 0 model/native workers on each agent, only the test agents were stopped, and the 3 temporary remote firewall rules were removed.
This is recorded in P4 `target/hf-acceptance/accepted-local-cleanup.json` and `accepted-remote-cleanup.json`.
The OS console host was distinguished from model workers. Failure artifacts and the prepared sources/environments were preserved.
For the final delivery commit and the restore result, check both `artifacts/integration/delivery-source-bundle/sources.json` and
`artifacts/integration/delivery-source-reproduction/reproduction.json`.
Document checks: P4 `npm run docs-lint` 101 files clean; HF `tools/verification/documents/run.py`
26 documents/111 local links/0 errors. Logs are in `accepted-docs-lint.log` under each repository's `target`
(for P4, `target/hf-acceptance/`). Document checks do not replace run or semantic verification.

The heterogeneous RTX4080+3090 BF16 split keeps its existing logits FAIL. It was not replaced by the FP32/single-GPU BF16 PASS.
Quantization, physical batching, arbitrary model support, durable recovery from whole-host failure, and very large H0–H7/performance/SLO are out of scope.
The next P4 work is the existing Release A, and it does not start automatically from this §0 request. No remote push was performed.
