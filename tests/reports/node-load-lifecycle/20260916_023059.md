# LOAD·UNLOAD 노드 수명 M1 검증 보고

## Timestamp

- 2026-09-16 02:30:59 +09:00
- 기준 commit: `921a4dceb5d030c216f935e129a0168694760e73`
- 검증 patch SHA-256: `A2D97B55ABF035CB017CF1BBA13B4D6411B6B123C467D6AA5D181143842390CD`

## Environment

- 개발 checkout: `F:\dev\p4`; 로컬 build·model run 없음
- 원격: Spark `gx10-c044`, `m42@192.168.0.26`
- 검증 checkout: `/home/m42/p4-m1-r2-20260916-0225`
- 도구: Cargo 1.97.1, Node `/home/m42/.nvm/versions/node/v24.19.0/bin/node`
  24.19.0, Python `/usr/bin/python3` 3.12.3
- 상한: `CARGO_BUILD_JOBS=8`, `RUST_TEST_THREADS=8`
- 기존 `:52005` p4-agent와 해당 checkout은 중지·변경하지 않았다.

## Executed Steps

1. 로컬에서 `cargo fmt --all -- --check`, `node tools/scripts/docs-lint.mjs --all`,
   `git diff --check`를 실행했다.
2. 원격 독립 worktree에서 `cargo check --locked --tests -p p4-agent`를 선행했다.
3. `cargo test --locked -p p4-agent -p p4-agent-core -p p4-adapter -p p4-protocol
   node_load_lifecycle -- --nocapture`로 NL01·NL02·NL04·NL07·NL10 묶음을 실행했다.
4. 패키지 전체와 `cargo test --locked --workspace --no-fail-fast`,
   `cargo test --locked --workspace --no-fail-fast --features hf-transformers`를 순차 실행했다.
5. 독립 worktree `/home/m42/p4-m1-mutation-remove-owner-r2-20260916`와 별도 target에서
   UNLOAD owner 제거 조건 한 줄을 무력화하고 실제 TCP 시험을 다시 실행했다.

## Observed Logs

- 표적 묶음: 8 passed, 0 failed, 0 ignored. 실제 TCP fixture는 OS 배정 loopback port를 사용했다.
- 느린 LOAD 중 중복 LOAD는 `Rejected/Unknown`, INSPECT는 `loading`을 반환했다.
- LOAD terminal은 원 OUTER의 correlation·causation·return route로
  `Succeeded/Present`를 반환했고, UNLOAD terminal은 `Succeeded/Absent` 뒤 INSPECT `nodes=[]`였다.
- OUTER가 UNLOAD terminal 전에 닫힌 시험에서 route와 owner는 제거됐고, 원 UNLOAD 입력,
  adapter completion, 최종 reply 세 소유물이 control remainder에 남았다.
- 패키지 전체: p4-adapter 112, p4-agent 39, p4-agent-core 193, p4-protocol 73과
  agent docs gate 1이 통과했다.
- workspace feature off: 1,514 passed, 0 failed, 7 ignored.
- workspace `hf-transformers` feature on: 1,514 passed, 0 failed, 7 ignored.
- ignored 7개는 실제 llama.cpp binary/model을 요구하는 기존 실기 시험이다.
- 제거 변이는 `cargo check --tests`를 통과한 뒤
  `node_load_lifecycle_actual_tcp_creates_fences_reports_and_removes_node`가 exit 101로 실패했다.
  최종 INSPECT에 `lifecycle-node`가 남아 정확히 제거 계약을 검출했다.

## Failures

- 1회차: stale-generation 오류 생성 호출에서 owned metadata를 먼저 이동해 `E0382`가 발생했다.
  진단 문자열을 이동 전에 계산하고 장부 `L013`과 선행 `cargo check --tests` gate를 추가했다.
- 3회차: 코드 시험 417개 통과 뒤 원격 비대화형 PATH에 Node가 없어 docs gate가 중단됐다.
- 4회차: Node 절대 경로는 고정했으나 HF fixture의 기본 `python` 별칭이 PATH에 없어
  `p4-hf-adapter --test retained` target이 실패했다.
- 5회차: Node와 `HF_TEST_PYTHON=/usr/bin/python3`을 모두 봉인해 HF retained 9개와
  workspace off/on이 통과했다. 3·4회차는 `L004`를 Cargo에만 적용한 실행계획 누락이며
  제품 코드나 기대값·timeout은 변경하지 않았다.
- 원격 준비의 중첩 인용 1회와 첫 변이 문자열 match 0은 build 전에 중단됐다. 이후 검토 가능한
  스크립트와 exact source 조각을 사용했으며 기준 소스 시험 횟수에는 포함하지 않았다.

## Reproducibility

- 기준 소스와 patch hash를 위에 고정했다. baseline과 변이는 서로 다른 worktree와 target을 썼다.
- 원격 runner는 Cargo·Node·Python 절대 경로와 작업 수 8을 모두 지정해야 한다.
- 실제 llama.cpp/HF worker가 새 공통 LOAD·UNLOAD terminal을 생성하는 연결은 M2 범위다.
  M1 완료는 neutral 실제 TCP supervisor와 기존 두 adapter 회귀까지이며 실제 모델 수명 이관 완료가 아니다.
