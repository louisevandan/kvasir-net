# LOAD/UNLOAD node lifecycle M1 verification report

## Timestamp

- 2026-09-16 02:30:59 +09:00
- Baseline commit: `921a4dceb5d030c216f935e129a0168694760e73`
- Verified patch SHA-256: `A2D97B55ABF035CB017CF1BBA13B4D6411B6B123C467D6AA5D181143842390CD`

## Environment

- Development checkout: `F:\dev\p4`; no local build or model run
- Remote: Spark `gx10-c044`, `m42@192.168.0.26`
- Verification checkout: `/home/m42/p4-m1-r2-20260916-0225`
- Tools: Cargo 1.97.1, Node `/home/m42/.nvm/versions/node/v24.19.0/bin/node`
  24.19.0, Python `/usr/bin/python3` 3.12.3
- Limits: `CARGO_BUILD_JOBS=8`, `RUST_TEST_THREADS=8`
- The existing p4-agent on `:52005` and its checkout were neither stopped nor changed.

## Executed Steps

1. Ran `cargo fmt --all -- --check`, `node tools/scripts/docs-lint.mjs --all` and
   `git diff --check` locally.
2. Ran `cargo check --locked --tests -p p4-agent` first, in an independent remote worktree.
3. Ran the NL01, NL02, NL04, NL07 and NL10 bundle with `cargo test --locked -p p4-agent -p p4-agent-core -p p4-adapter -p p4-protocol
   node_load_lifecycle -- --nocapture`.
4. Ran the full packages, `cargo test --locked --workspace --no-fail-fast` and
   `cargo test --locked --workspace --no-fail-fast --features hf-transformers` in sequence.
5. In the independent worktree `/home/m42/p4-m1-mutation-remove-owner-r2-20260916` with a separate target,
   disabled the one-line UNLOAD owner-removal condition and re-ran the real TCP test.

## Observed Logs

- Targeted bundle: 8 passed, 0 failed, 0 ignored. The real TCP fixture used an OS-assigned loopback port.
- A duplicate LOAD during a slow LOAD returned `Rejected/Unknown`, and INSPECT returned `loading`.
- The LOAD terminal returned `Succeeded/Present` with the original OUTER's correlation, causation and return route,
  and the UNLOAD terminal returned `Succeeded/Absent`, after which INSPECT showed `nodes=[]`.
- In the test where OUTER closed before the UNLOAD terminal, the route and owner were removed, and three owned items (the original UNLOAD input,
  the adapter completion and the final reply) remained in the control remainder.
- Full packages: p4-adapter 112, p4-agent 39, p4-agent-core 193, p4-protocol 73 and
  the agent docs gate 1 passed.
- workspace feature off: 1,514 passed, 0 failed, 7 ignored.
- workspace `hf-transformers` feature on: 1,514 passed, 0 failed, 7 ignored.
- The 7 ignored tests are existing real-hardware tests that require a real llama.cpp binary/model.
- The removal mutation passed `cargo check --tests`, and then
  `node_load_lifecycle_actual_tcp_creates_fences_reports_and_removes_node` failed with exit 101.
  `lifecycle-node` remained in the final INSPECT, so the test detected exactly the removal contract.

## Failures

- Round 1: the stale-generation error construction call moved the owned metadata first, causing `E0382`.
  The diagnostic string is now computed before the move, and register entry `L013` and a preceding `cargo check --tests` gate were added.
- Round 3: after 417 code tests passed, the docs gate stopped because Node was not on the remote non-interactive PATH.
- Round 4: the absolute Node path was pinned, but the default `python` alias used by the HF fixture was not on PATH, so
  the `p4-hf-adapter --test retained` target failed.
- Round 5: with both Node and `HF_TEST_PYTHON=/usr/bin/python3` sealed, the 9 HF retained tests and
  the workspace off/on runs passed. Rounds 3 and 4 were execution-plan omissions that applied `L004` only to Cargo;
  no product code, expected values or timeouts were changed.
- 1 nested-quoting failure in remote preparation, and 0 matches for the first mutation string, were stopped before the build. After that, reviewable
  scripts and exact source fragments were used; these are not counted among the baseline source test runs.

## Reproducibility

- The baseline source and patch hash are pinned above. The baseline and the mutation used different worktrees and targets.
- The remote runner must specify the absolute Cargo, Node and Python paths and a job count of 8.
- Wiring the real llama.cpp/HF workers to produce the new common LOAD/UNLOAD terminal is M2 scope.
  M1 completion covers the neutral real-TCP supervisor and regression of the two existing adapters; it is not completion of the real-model lifecycle migration.
