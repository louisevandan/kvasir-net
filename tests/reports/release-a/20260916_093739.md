# Release A Qwen122B H0 v2 re-seal

2026-09-16 09:37 KST. H0 v2 is **GREEN**; this is not yet real-model acceptance. The baseline runtime is
`25edd33cf24b46964e432a7cd6d89772417673db`, the sealed specification is
[`benchmark-spec-qwen122b-h0-v2.json`](../../../test/benchmarks/cluster-inference/release-a/benchmark-spec-qwen122b-h0-v2.json),
and its SHA-256 is `b79aff2254c06612eadeb9327e23598d4a3619d886789add28a22606ba3b39bf`.
The verdict is `load_authorized=true`, `runtime_acceptance=false`. The second H1 LOAD starts only after this report, once it passes a separate
preflight.

## Sealed changes

- H1 quality64 is a RELEASE closed-loop with `max_in_flight=1` on the same LOAD. The time-0 marking of the 64 requests is the
  set of submission candidates, not permission to run them concurrently.
- The per-request event deadlines for short32, medium16 and long16 are 600,000, 1,200,000 and 1,800,000ms respectively.
  The overall limit is 67,500,000ms: the sum of per-request limits, 67,200,000ms, plus a reclaim margin of 300,000ms.
- The runtime `config.rs` and `inference.rs`, the H1 materializer and judge, and the H0 validator, tests and single verification runner are
  bound by file hash in the specification. The validator rejects H0 v1, open-loop quality, missing deadlines and
  a 1,800,000ms overall limit.
- The source bundle is 5,126,427 bytes,
  `4de5d0147fe3ac281ab735ad02f3fbfb46c5874a3b434e33237cdfdf1e314cc6`.
  The compatibility patch digest is
  `d8018fa8f7f44d61d23cd68496024fa296d2571c860fda988cef91a28b2572a9`.

## Physical hosts and existing agents

The same source archive was built on all three hosts with `jobs<=8`. The Spark agent is
`86cac7b9e26ac4297c7d46f6ffcb2d098f9fa0fa6969f393db2f74276e8bb233`, and both Mac agents are
`5c2a3301a3b821d6ba990a7947fbbbd5182d8d1fdd64b2833183ba037cad0cdf`. The host snapshots are as follows.

| role | snapshot SHA-256 | Existing `:52005` state | Model occupancy verdict |
| --- | --- | --- | --- |
| spark | `830fb539c5ce41d1e626acf3cc0370bcc3b4e273ec3d610c1d0d914bb30406b4` | Old agent PID1287453, CLOSE-WAIT 310 + LISTEN 1 | native/HF child 0, CUDA compute process 0. INSPECT impossible because the backlog is saturated |
| mac20 | `902d2e08f74721571a1bbf37c792c1d8ac2582da8a2d286a806f3fcbce4550c2` | PID69656, INSPECT `nodes=[]`, CLOSE_WAIT 2 + LISTEN 1 | native/HF child 0 |
| mac21 | `acd1210d52f58b334c4418fd74c6d343f226afc881b88679aa0690f174e7aaef` | PID85992, INSPECT `nodes=[]`, LISTEN 1 | native/HF child 0 |

The node list of the Spark protected agent itself could not be read because of the saturated listener. With no child/GPU occupancy, it was judged that
no model is loaded, but this agent is not used for H1 evidence. The three protected PIDs were not stopped or restarted.
H1 starts a task-owned agent of the new runtime on `:22150` and re-confirms an exact bidirectional INSPECT, nodes 0,
failure 0, child 0 and listener 0.

## Verification

A single command, `verify-h0-v2.py`, passed all of the following.

- H0 spec Node tests 4/4. All 40 mutations covering structure, file substitution and weakening of H1 authority were rejected.
- host inspector Python tests 4/4.
- event runtime preflight Python tests 5/5. It rejects a task agent port with leftover CLOSE_WAIT state, and
  for normal reused ESTABLISHED connections the execution topology separately checks the count and peers.
- H1 materializer self-test 2/2, judge self-test 4/4.
- The H0 file binding CLI returned hosts 3, stages 3, corpus 64 and `runtime_acceptance=false`.

The final H1 inputs were materialized in `target/release-a-h1-v2-20260916-0950`. The config SHA-256 is
`e0888229330de8f3fd1b409b0a2784498fda8bcf4582699b68bef3402faf2b94`, and the deadline vector SHA-256 is
`d7a6d4a778c03f982d6db0ee8ec63f5ee1f595b89e0086fbb5b8bf70bc8ed518`. Feeding the earlier failure artifact
to the new judge is rejected with `artifact_acceptance`, `count_mismatch`, `submission_remainder` and `runtime_error`,
so the earlier 8/64 result cannot be reused as a PASS under the new contract.

## Attempts and lessons

The complete product H0 gate passed from the first run. However, the first call of the auxiliary tests did not reuse the active lesson L054
and passed a file path to `unittest`, failing before collection. The work as a whole is therefore recorded as `first_pass=fail`.
So that this is not left to human memory again, `verify-h0-v2.py` now calls the correct direct tests, self-tests and Node CLI in a fixed way and
asserts each test count. The CLOSE-WAIT buildup on the old protected agent was registered as L065, and the new task agent's
pre- and post-run connection state and INSPECT are used as the execution gate.

The first connection gate before H1 LOAD failed because it required even normal reused ESTABLISHED connections to be 0. No model was loaded.
After checking Spark 2 and 1 per Mac ESTABLISHED connections and their peers against the actual topology, only one difference was fixed: the common gate now requires
CLOSE_WAIT 0, the marker of the old defect. On the second gate, 3/3 bidirectional INSPECT, nodes 0, failure 0,
native/HF 0, CLOSE_WAIT 0 and the expected ESTABLISHED topology passed.

## Next action

Run only the second H1 run. After starting the new task-owned agents and tunnels, with the sealed config unchanged, judge 3/3 LOAD, quality64
closed-loop, per-request deadlines, normal JSON/oracle/EOS/delivered/RELEASE, the final UNLOAD and nodes/child/listener 0.
Do not mix in H2 open-loop concurrent waves before H1 finishes.
