# Release A — current fleet rebuild, adapter regression, 550B cost diagnosis

2026-09-15 KST. A0 preparation and real-hardware record following [the reclaim of the existing fleet](20260915_005456.md).
[Plan](../../plans/release-a-20260914.md). **The 550B instrumentation on and off arms both passed correct answer and cleanup, but the SLO failed. Empty nodes were reconfirmed on all existing agents, and Release A acceptance is incomplete.**

## Source and preflight verification

The executed Rust source is `f80e360ac`. On Linux/Mac, a full Git archive was normalized to LF in a new directory and
sealed, then rebuilt with `cargo build --release --locked -p p4-agent --features hf-transformers`.
All four hosts passed 23 agent tests and 1 docs gate. On three hosts the first docs gate failed because the SSH PATH had
no existing Node; the final full package test with the PATH of the installed Node set is 24/24.
The first archive extraction on Mac failed because the old Python lacks `extractall(filter=...)`. After checking paths
and file types, it was extracted with a compatible API. The first failure and the final result are preserved separately.

The Windows agent is the current binary verified in the HF migration, SHA256
`595cc270a6beef566161c43dcda4b0d5f4715fcd1e7a7b8ff1b4d8989509b524`.
Locally it was copied to the existing development run path `target/debug/p4-agent.exe`, with the original kept separately.
The previously sealed release path was not overwritten. On the two remote Windows hosts it runs as a hidden task in user session 1.
Native reuses the previous current-pin candidate unchanged: upstream 451b89/28 patches, patch SHA256
`a07b782641a008bcab4b4e81e58cd11798387c5bf332fa4a0474a1dce4d700ef`.
The full per-platform agent/native hashes and test exit logs are in `current-posix-build-evidence.json`.

Native PLAN was rerun under the same 550B cut/seq8/ctx819200/batch128/ubatch64/F16/FA conditions and passed **8/8**.
The two stages on `.29` share the same RAM, and the device/host entries on Mac/GB10 are still the same physical pool.
The S: model on Windows was read in the real user session. Individual native PLANs are not counted as acceptance of the public combined resource limit.

## Mac execution environment and return route

The Mac agent first run detached left `No route to host (os error 65)` when sending to a real P4 peer.
On the same Mac, TCP checks with Python, a new Rust standard-library TCP program, and the same Tokio library succeeded.
Re-querying the existing Rust agent and running it as a GUI LaunchAgent also failed the same P4 return check.
No specific privacy or firewall setting was declared the cause or changed.

**After the same binary was run as a foreground process attached to an SSH session, the P4 round trip on both Macs succeeded.**
The data path is TCP over the LAN, not an SSH tunnel or proxy. SSH only keeps the process's session alive.
With Mac20/21 on 41993 and the other five hosts on 41991, all 7 split edges and their returns were verified with real INSPECT.
The failed detached agent and the test GUI LaunchAgent/plist were removed. The existing installed apps and NAS settings were kept.

## Adapter regression

On the same current Windows agent, 12 HF fixture cases (lifecycle/corruption/timeout/identity rejection) passed.
A real Qwen single-CPU HF event passed logits/greedy ×4, cache ×4 and cleanup.
The existing llama.cpp path also passed on two stages on a real RTX3090: response `4` for `2 + 2`, EOS, completion and release 1/1,
and UNLOAD/DELETE. The first error and cleanup error are null on both.

The latest HF Python change from concurrent work is owned by a [separate acceptance report](../../../layers/adapters/hf/tests/reports/loading-planner/20260915_013600.md).
After confirming the binding of the latest source hash, 6 real events / 12 cache checks with cleanup, and full workspace
1460/0/7, it was committed and pushed together as `11194073d`. This change does not alter the Rust/native seal of the running 550B.

## 550B arm

Settings and hashes are sealed in `target/release-a-20260915_005456/550b-case00-config.json` and `550b-seal.json`.
Generation 1789403152032, existing corpus case-00 at 2785 tokens/9273 bytes, output 2048/EOS,
and the JSON correct-answer oracle are unchanged. The `p4-event-drive` SHA256 is
`8fe12cc5f022b3a48f4413858a26d5b5ae9b3009310ec206ed513cf54c3e3f4b`.
The current runner uses a single 3600000 ms timeout for both LOAD and the request, so this arm is not counted as
per-request deadline/SLO acceptance. The normal-response verdict checks the existing exact-json judge plus EOS, release and cleanup.

The instrumented arm completed LOAD and SESSION 8/8 at 02:04:08 KST, about 2244.3 s after driver start.
At 02:17 KST it passed the existing exact-json judge and EOS. The correct answer is `return pressure > threshold;` with
R00002=false/R00017=false/R00031=true/boundary=false. Completed 1 / released 1; first error, cleanup error and
evidence_missing are all null. After UNLOAD/DELETE, real INSPECT 7/7 nodes=[] and 0 native children were confirmed.

| Instrumentation on, case-00 | Measured | Fixed target / verdict |
| --- | --- | --- |
| TTFT | 662.342 s | 60 s / FAIL |
| EOS completion | 770.047 s | 600 s / FAIL |
| ITL p95 | 1.741 s | 250ms / FAIL |
| Generation | 94 tokens | excluding the empty terminal EOS; correct answer PASS |
| Generation TPS | 0.8723 | 93 tokens after the first / last ordinary output − first output |
| Useful-response overall TPS | 0.1221 | 94 correct tokens / submission→EOS 770.047 s |

44 prefill (2785 rows) and 94 decode (94 rows), 138 per stage for a total of 1104 native/Worker executions, were linked
by PID, host and load/session/execution/rows. cost conformance=true; missing, duplicate and unmapped are 0.
The actual n_kv on every stage was 256–3072. The full 819200 reserved capacity is not treated as the actual KV range.

| Stage / host | native prefill s | native decode s |
| --- | --- | --- |
| 0 / .29 CUDA0 + CPU expert | 202.536 | 33.128 |
| 1 / .29 CUDA1 + CPU expert | 195.092 | 28.964 |
| 2 / GB10 | 17.013 | 4.992 |
| 3 / Mac20 | 16.903 | 3.056 |
| 4 / Mac21 | 25.283 | 4.349 |
| 5 / TUF | 56.383 | 12.668 |
| 6 / Ubuntu | 78.744 | 3.464 |
| 7 / local | 57.021 | 14.418 |

Native time and the graph execute time inside it are not added together. Graph time includes backend dispatch, internal
copies and synchronization. Pure kernel time, inter-host network transfer and overlap are not judged. Clock differences
between the remote hosts and local were observed, so inter-host timestamp differences are not reported as network cost.
The 5 NVIDIA hosts collected 1 Hz GPU records from before inference. On TUF, 43 of 730 samples had utilization
`[Unknown Error]`, so valid coverage did not meet 95%. Mac GPU is also unavailable, so this is not overall GPU acceptance.
A separate job on the local 4080 was excluded from measurement and left running. MEMORY_ACTUAL was collected for all 8 stages,
but it is not counted as acceptance of a finite public profile that sums shared RAM.

The actual Windows CPU DLL hash and CMake settings confirm the AVX2 candidate. AVX512/VNNI are disabled.
Prefill on the two middle stages totaled 397.628 s, but other ISA, thread and expert placements were not compared.
This result is not generalized to an absolute lower bound for the hardware or to the optimal kernel.

The first log copy failed on opening the Windows share and on non-UTF-8 bytes on POSIX. The originals were collected by
reading the remote share and copying bytes via base64; the first error is preserved in `550b-log-collection-initial.json`
and the final success in `550b-log-collection.json`. Model execution failures and collection tool errors are kept distinct.

## Instrumentation off control — complete

At 02:19 KST a new arm started with the same model/corpus/plan/policy, changing only the namespace and `P4_STAGED_TRACE_COST=0`.
Generation 1789406353807, owned by `550b-off/config.json` and `seal.json`.
The new LOAD started after confirming the instrumented arm's normal UNLOAD/DELETE and actual native 0.
At 02:54:55 KST it passed LOAD/SESSION 8/8, and at about 03:08 it passed correct answer, EOS, completed 1 / released 1 and cleanup.
First error, cleanup error and evidence_missing are all null, and all 94 generated token IDs are identical to the on arm.

| Metric | on | off | Fixed target / verdict |
| --- | --- | --- | --- |
| Until LOAD/SESSION complete | 2244.264 s | 2141.610 s | from driver start; separate from SLO request time |
| TTFT | 662.342 s | 656.619 s | 60 s / FAIL on both |
| Submission→EOS | 770.047 s | 763.552 s | 600 s / FAIL on both |
| ITL p95 | 1.741 s | 1.754 s | 250ms / FAIL on both |
| Generation TPS after first token | 0.8723 | 0.8781 | 93 tokens / last ordinary output − first output |
| Useful-response overall TPS | 0.1221 | 0.1231 | 94 correct tokens / submission→EOS |

Settings normalization, seal hashes and stage binary identity all matched. The off arm's Worker executions are
8 stages × 138 = 1104, with 0 missing. The toggle was confirmed consumed by 0 cost logs across the 8 native PIDs owned by the off arm.
The off driver's actual exit code is 0. For the on arm, the artifact and `GATE_RESULT passed=true` were confirmed, but
the separate observer's exit file was empty, so the OS exit code was not collected.

The observed on/off differences are TTFT +0.872%, E2E +0.851% and ITL p95 −0.741%.
This is a single pair in fixed order, so no causal overhead or confidence interval free of cache, thermal or run-order effects is claimed.
Both arms have a null request deadline. The driver's overall timeout is not read as a per-request deadline or cancellation implementation.

## Final cleanup and the existing control path

For both arms, after UNLOAD/DELETE, candidate INSPECT 7/7 nodes=[] and 0 native children were confirmed.
The 7 test agents and the owned GPU samplers were stopped, and the temporary Windows tasks and firewall rules were removed.
Local `target/debug/p4-agent.exe` was restored and checked against the original SHA256
`d84f9390ed7bd1717ae2e5eb6478c6caff03ec21fe0c0d8817f39fe9e976757d`.
The first restore failed on a file lock right after shutdown; the successful re-restore after the lock cleared is preserved separately.
On remote Windows, the first zero-children guard rejected because only a system conhost remained. After checking that PID,
path and parent, the candidate agent was stopped and the conhost disappeared too. This was not a forced kill that ignored model children.

There were 0 P4 native processes across the 9 physical machines. Of the existing registered agents, 5 returned INSPECT nodes=[] immediately,
but the old agents on Spark/Ubuntu timed out again. Their two TCP listeners showed Recv-Q 129/Send-Q 128 and
many CLOSE_WAIT sockets. The source of the requests and a permanent fix are not determined.
After confirming 0 model children, only those two agents were restarted, preserving the same original binary, argv, cwd and environment.
Spark PID 1103448→1287453, Ubuntu PID 3787675→307384. Afterwards, real registered INSPECT 7/7 nodes=[] and
native 0 on the restarted hosts were reconfirmed. The existing installed apps and NAS settings were kept.

This is a cold recovery of control responses, not a permanent transport fix or acceptance of past request/KV settlement.
TCP half-close is also used for normal delayed output, so a fix that drops the route immediately on EOF was not applied.
A fix that verifies the lifetime and ownership preservation of connection slots and return routes remains.

## Reproduction and remaining gates

The raw evidence root is `target/release-a-20260915_005456/`. The run passed two sealed config/artifact paths to the current driver.
Cost was verified with the existing `analyze-native-cost.mjs` and `correlate-cost.mjs`, and the correct answer with the existing
`corpusCase("short",0,32)` and `judge` in `corpus.mjs`. The analysis commands are `analyze-550b.mjs` and `analyze-pair.mjs`
in the raw root, which produced `550b-summary.json`, `550b-correlated-cost.json`,
`550b-phase-cost.json` and `550b-pair-summary.json`. Cleanup evidence is `final-fleet-audit.json`,
`final-local-processes.json`, `final-original-control-recovery.json`, `final-recovered-host-audit.json`,
and the final `550b-original-inspect-*.json` and `550b-*-cleanup*.json`.

Refer to the raw evidence `*-prepare-start.json`, `current-fleet-plan-results.json`, `current-fleet-return-path-final.json`,
`current-hf-lifecycle/`, `current-hf-qwen/`, `current-llama-artifact.json`, `550b-*.json` and the logs.
Acceptance of A0 as a whole, the finite deployment profile / per-request deadline / all 8 waves, and A1–A5 and H0–H7 is incomplete.
The SLO failure of the current placement is not turned into a pass by relaxing inputs, answers or targets.

The first next action is to reproduce the lifetime of the recurring control connection slot / return route on the real
TCP half-close consumption path and fix it with ownership preservation, regression and independent mutation. Then verify
CPU expert/ISA/placement candidate costs and a finite profile with a per-request deadline runner. A single short correct-answer pair is not promoted to all-8-wave acceptance.
