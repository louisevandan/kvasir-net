# Release A — 기존 fleet 노드 회수

2026-09-15 00:54–01:10 KST, P4 `f80e360ac`. 사용자가 기존 모든 에이전트의 노드 상태 확인과
언로드를 명시했다. [계획](../../plans/release-a-20260914.md)의 다음 실기 전 점유 회수 기록이다.
정상 UNLOAD 수용 또는 Release A 전체 완료로 계산하지 않는다.

## 확인과 종료

Studio `GET /api/snapshot`의 등록 7개 에이전트를 P4E3 INSPECT로 직접 조회하고, SSH의 프로세스
목록으로 MI250 두 호스트와 비등록 `.29:42003`까지 조사했다. 로컬 포함 물리 9대다.
SSH 파일 접근 결과로 설치 앱의 S: 접근을 판정하지 않았다.

| 대상 | 최초 상태 | 처리와 최종 상태 |
| --- | --- | --- |
| 로컬 `.6:51054`, `.29:52005`, Mac21 | nodes=[] | 기존 에이전트 유지 |
| Mac20 | 빈 MiniMax node 1개, retained=0 | 정상 DELETE ok, nodes=[] |
| TUF `.17:52005` | Nemotron failed/stopped 1개, loaded 1개 | loaded에 UNLOAD 요청, native 연결 종료 오류10054. 기존 task로 cold restart, nodes=[] |
| 비등록 `.29:42003` | MiniMax loaded 2개, native 없음 | 두 UNLOAD 모두 오류10054. 보조 에이전트 종료, native 없음 |
| Spark `.26:52005` | Qwen122B native 1개, INSPECT 접속 timeout | agent/native의 PID·시작 시각·부모 결속 확인 후 SIGTERM. 강제 SIGKILL 없이 종료, 재기동 후 nodes=[] |
| Ubuntu `.19:52005` | Qwen122B native 1개, INSPECT 접속 timeout | 같은 cold recovery. 첫 재기동은 AddrInUse, port 해제 후 재기동 성공, nodes=[] |
| MI250 A/B | P4 agent/native 없음 | 다른 GPU 작업 유지 |

Spark/Ubuntu는 외부뿐 아니라 SSH를 통한 loopback 접속도30초 timeout이었다. listener backlog는
129/128, 기존 로그의 마지막 수락은 connection255였다. EOF 뒤 writer route를 유지하는 현재 코드와
부합하지만 원인 수정/회귀 검증은 하지 않았다. 이 사실을 일반적 연결 수명 문제의 해결로 표현하지 않는다.

TUF의 정지 노드는 input_retained=1, effects_fenced=true, active_owners/frontiers=8,
receive uncertain=1과 기존 unload_failed를 원문 보존했다. cold recovery는 프로세스 메모리와 모델을
회수한 것이며 과거 요청의 정산·KV 완료를 소급 증명하지 않는다. 비등록 보조 agent의 SSH 자식 재기동은
SSH 종료 후 지속되지 않았으며 이 보조 agent는 최종 종료 상태다. 등록 에이전트의 대체로 세지 않는다.

## 최종 검증과 다음 행동

최종 실제 INSPECT **등록7/7 nodes=[]**, 물리9대의 P4 native **0개**다. Ubuntu VRAM은7281→4MiB,
TUF는0MiB, Spark 가용 RAM은125,911,699,456bytes였다. 설치 앱과 NAS 설정은 유지했다.
원자료는 `target/release-a-20260915_005456/`의 `registered-before.json`, `deployments-before.json`,
`process-audit-before.json`, `loopback-inspect.json`, `m42-extra-inspect.json`, `*-unload.json`,
`*-cold-recovery.json`, `ubuntu-restart-retry.json`, `process-audit-after.json`, `registered-final-empty.json`이다.
재현은 Studio 등록 주소 수집→P4E3 INSPECT→프로세스/부모/시작 시각 대조→해당 load_generation의
UNLOAD→오류 보존→필요한 cold recovery→전체 INSPECT/프로세스 재조회 순서다.

회수 후 현재 HEAD와 직전 `bfc8bcc45`의 agent/core/protocol/llamacpp/event-drive 소스를 대조했다.
차이는 entrypoint Cargo의 HF 의존 경로다. 현재 HEAD의 LF 소스 archive를 새 디렉터리에 봉인해
Linux/Mac의 HF 포함 agent 재빌드를 시작했다. 다음은 현재 바이너리·가용량을 결속한 동일550B
7host/8stage LOAD·정상 응답·비용 계측·회수다. A0 전체와 A1–A5는 미완료다.
