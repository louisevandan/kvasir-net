# Release A — Reclaiming existing fleet nodes

2026-09-15 00:54–01:10 KST, P4 `f80e360ac`. The user explicitly asked to check the node state of every existing agent and
to unload the nodes. This records the occupancy reclaim before the next real-hardware run in the [plan](../../plans/release-a-20260914.md).
It is not counted as normal UNLOAD acceptance or as completion of Release A as a whole.

## Check and termination

The 7 agents registered in Studio `GET /api/snapshot` were queried directly with P4E3 INSPECT, and SSH process
lists were used to survey the two MI250 hosts and the unregistered `.29:42003` as well. That is 9 physical machines, including the local one.
The installed app's S: access was not judged from SSH file access results.

| Target | Initial state | Handling and final state |
| --- | --- | --- |
| Local `.6:51054`, `.29:52005`, Mac21 | nodes=[] | Existing agents kept |
| Mac20 | 1 empty MiniMax node, retained=0 | Normal DELETE ok, nodes=[] |
| TUF `.17:52005` | 1 Nemotron failed/stopped, 1 loaded | UNLOAD requested on the loaded node; native connection-closed error 10054. Cold restart through the existing task, nodes=[] |
| Unregistered `.29:42003` | 2 MiniMax loaded, no native | Both UNLOADs failed with error 10054. Auxiliary agent terminated, no native |
| Spark `.26:52005` | 1 Qwen122B native, INSPECT connection timeout | SIGTERM after confirming the agent/native PIDs, start times and parent binding. Exited without a forced SIGKILL; nodes=[] after restart |
| Ubuntu `.19:52005` | 1 Qwen122B native, INSPECT connection timeout | Same cold recovery. The first restart hit AddrInUse; restart succeeded after the port was released, nodes=[] |
| MI250 A/B | No P4 agent/native | Other GPU work left running |

On Spark/Ubuntu, not only external connections but also loopback connections over SSH timed out after 30 seconds. The listener backlog was
129/128, and the last accept in the existing log was connection255. This is consistent with the current code keeping the writer route after EOF,
but no root-cause fix or regression verification was done. This is not described as a resolution of the general connection lifetime problem.

TUF's stopped node preserved, verbatim, input_retained=1, effects_fenced=true, active_owners/frontiers=8,
receive uncertain=1 and the existing unload_failed. Cold recovery reclaimed the process memory and the model;
it does not retroactively prove settlement or KV completion for past requests. The SSH-child restart of the unregistered auxiliary agent
did not persist after SSH ended, and this auxiliary agent is in its final terminated state. It is not counted as a replacement for a registered agent.

## Final verification and next action

Final real INSPECT: **registered 7/7 nodes=[]**; P4 native processes on the 9 physical machines: **0**. Ubuntu VRAM went from 7281 to 4MiB,
TUF was at 0MiB, and Spark available RAM was 125,911,699,456 bytes. The installed app and NAS settings were kept.
The raw data is `registered-before.json`, `deployments-before.json`,
`process-audit-before.json`, `loopback-inspect.json`, `m42-extra-inspect.json`, `*-unload.json`,
`*-cold-recovery.json`, `ubuntu-restart-retry.json`, `process-audit-after.json` and `registered-final-empty.json` in `target/release-a-20260915_005456/`.
Reproduction order: collect the Studio registered addresses → P4E3 INSPECT → match processes, parents and start times → UNLOAD for the relevant load_generation →
preserve errors → cold recovery where needed → re-query INSPECT and processes everywhere.

After the reclaim, the agent/core/protocol/llamacpp/event-drive sources at the current HEAD were compared with the previous `bfc8bcc45`.
The difference is the HF dependency path in the entrypoint Cargo. An LF source archive of the current HEAD was sealed in a new directory,
and a rebuild of the HF-enabled agent on Linux/Mac was started. Next is the same 550B
7-host/8-stage LOAD, normal response, cost instrumentation and reclaim, bound to the current binaries and capacity. A0 as a whole and A1–A5 are incomplete.
