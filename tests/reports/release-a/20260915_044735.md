# Release A connection reclaim — WIP stopped after 3 failures

2026-09-15 04:47 KST. Baseline `530985320`. Local Windows verification; no remote deployment or model runs.
[Plan](../../plans/release-a-transport-20260915.md). **The user's stop condition of 3 failures per phase was reached.**
The overall roadmap, A0 and connection-reclaim acceptance were not completed. No automatic retest or advance to the next phase follows.

## Changes and observations

Confirmed a path where the return sender stays alive after a read EOF and keeps the slot. A normal half-close is the same EOF, so
the route is not dropped unconditionally. The candidate adds connection-owned FINISH (length0) → live route detach → drain of pending output →
length0 ACK. The new socket binding and the failure tombstone are kept, and a late OUTER result leaves
the original allocation with the failure owner. The ACK is not request/KV/ledger settlement.

Explicit-close consumption was wired into the Rust driver and the HF Python verification caller, but final acceptance failed.
Studio was only read; no changes were made under the cancelled monitoring UI instruction. Existing callers do not yet send FINISH,
so replacing only the candidate agent does not permanently fix saturation of the existing fleet.

## Commands, results and failure count

Commands were run by appending a filter to the shared prefixes below. Each file preserves stdout/stderr and the actual exit.
`cargo test -p p4-agent <filter> -- --nocapture`, `cargo test -p p4-event-drive -- --nocapture`.

| Record | Scope / result | Cumulative implementation verification failures |
| --- | --- | --- |
| baseline.log / exit101 | new real INSPECT shutdown test: ACK timeout. 1 baseline counterexample | intended RED, counted separately |
| round1-agent.log / exit0 | event_runtime 17 PASS / 7 filtered | 0 |
| round1-ownership.log / exit101 | event_runtime 18 PASS / 1 FAIL / 7 filtered | 1 |
| round2-ownership.log / exit0 | event_runtime 19 PASS / 7 filtered | 1 |
| round2-driver.log / exit101 | compile failure from a missing Instant import; no tests run | 2 |
| round3-driver.log / exit101 | driver unit 94 PASS / 1 FAIL | **3 — STOP** |

Failure 1 was a test error: the retained event had transferred its cost to the OUTER store through the broker, but the test expected it to remain
in the input store. The fix that keeps original payload pointer identity and checks input store 0 and OUTER store 557 bytes
passed. The existing half-close, partial write failure, new binding protection and pending output drain also passed.

Failure 2 was the missing Instant import in `tools/event-drive/src/run/mod.rs`. After adding the import, the next run
compiled, but failure 3 occurred. Failure 3 is
`run::wire::tests::explicit_finish_rejects_eof_and_preserves_unexpected_output`.
It failed by comparing the consumer buffer, which had read the first 64 bytes of an unexpected frame and returned an error, with the full 721-byte frame.
After the failure, no expected values were relaxed, no inputs removed, and no further fixes or retests were made. Which owner should
preserve the remaining bytes, and the contract for exposing them in the failure artifact, are not yet verified.

## Not run and next conditions

Independent removal mutations, the full-workspace final tally, HF Python change tests, actual construction/reclaim on both adapters with the new binaries,
caller migration, and remote installation/sustained repetition were not run. The later integration suite of p4-event-drive
also did not proceed in this command because of the unit failure. The earlier 1460/0/7 and the real-hardware PASS of both adapters are not carried over to this WIP.

The first action after the user lifts the stop is to settle preservation of the entire unexpected FINISH output and the failure artifact contract,
then verify the fix, the consumption path and independent mutations while keeping the current 64/721-byte counterexample. A0 cost/profile/deadline,
A1–A5 and H0–H7 are still incomplete. This commit is not a deployable GREEN.

Raw data: `target/release-a-transport-20260915/`. `source-manifest.json` holds the SHA256 of the 8 modified source files.

Follow-up status: at the user's instruction, work resumed after a deterministic preflight review, and the local FINISH acceptance result is
recorded in [20260915_142237](20260915_142237.md). The 3 failures and the stop recorded in this document stand.
Feature sources were not changed after the last feature test. Only this report, the plan, contract comments and the index were added.
