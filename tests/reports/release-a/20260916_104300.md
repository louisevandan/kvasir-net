# Release A Qwen122B H1 second run INVALID and H0 v3 judge hardening

2026-09-16 10:43 KST. The second H1 run is **INVALID**. The Qwen3.5-122B-A10B 3-stage deployment was actually
loaded and computing, but an audit during the run found that the H0 v2 judge does not judge per-request E2E deadlines or per-class TTFT/ITL p95.
This run is not used as grounds for H1 GREEN or for a product performance RED.
There is also no terminal artifact, so latency and normal responses are not reconstructed from partial logs.

## Run identity and grounds for stopping

| Item | Value |
| --- | --- |
| runtime source | 25edd33cf24b46964e432a7cd6d89772417673db |
| H0 v2 spec | SHA-256 b79aff2254c06612eadeb9327e23598d4a3619d886789add28a22606ba3b39bf |
| Run root | Spark /home/m42/p4-h1-v2-run-20260916-094333 |
| event-drive | PID 2753825, SHA-256 a11f92865ba9e060fa653c2d76cfdd7ee09e6c03f6efafc4f08afbfca150923c |
| task agents | Spark 2749529, Mac20 32481, Mac21 31400 |
| generation | 202609161101 |
| schedule | quality64, RELEASE closed-loop, max_in_flight=1 |
| source config | short32/medium16/long16, 600/1200/1800 s per request, 67,500 s overall |
| Run time | event-drive started around 09:52:03; elapsed 41 min 13 s at the 10:33:16 snapshot |
| artifact | no h1-quality.artifact.json |
| stdout/stderr | both files 0 bytes |

A bidirectional INSPECT just before the stop returned three nodes and 0 transport failures. The head held 1 request of
234,683 bytes, and 3 completion groups were using the 268,435,456-byte reservation, so it was in
native_completion_reservation_full:waiting. On Mac20/21, the nodes of the same generation were loaded.
The sealed native logs show 1,021/1,023/1,023 head/intermediate/tail linkcpp graph stage entries respectively.
This is only evidence of actual compute progress, not evidence of request completion, TTFT, ITL, E2E or response quality.

Even if the run had continued, the judge at the time did not consume the following figures.

- Per request, completed_ms-arrival_ms <= request_timeout_ms
- Per-class short/medium/long TTFT nearest-rank p95
- Per-class ITL nearest-rank p95 computed from the actual output_received_ms intervals of generated tokens
- A fail-closed verdict for classes with no samples

So even if the existing judge had later returned PASS, it could not have proven the H1 contract of the verification conventions. Under the rule that the measurement source and
judge authority are not changed during a run, event-drive was stopped with TERM after checking its exact PID and argv.
event-drive has no path that produces a partial artifact/FINISH on a signal, so no artifact was created.

## Normal reclaim and the remaining transport ledger

After event-drive exited, NODE_UNLOAD was sent exactly once to each of the three nodes. All three results were
status=succeeded, resource_state=absent, and the adapter result was llamacpp.unloaded-v3.
A follow-up INSPECT showed nodes=[] on all three hosts.

On the Spark task agent, 2,390 transport failures, 4,505,782 bytes, that could not be sent to the vanished OUTER return route
remained as not_started. They were not deleted or merged into the H1 result. After confirming that nodes were empty,
the 3 task agents, the Spark proxy and the 4 local reverse tunnels were checked by exact PID, executable, cmdline and listener
and stopped with TERM. Native PIDs 2753848/32734/31630 were already gone, reclaimed normally by UNLOAD. Task ports
22150–22153/23150–23152 and the Spark GPU compute processes are at 0. Protected agent PIDs
1287453/69656/85992 and each :52005 listener are all alive.

The ignored raw data is preserved locally in
target/release-a-h1-v2-run-20260916-094333/abort-evidence/ and in the Spark run root.
The normal reclaim is a lifecycle GREEN, but it does not produce an H1 acceptance result.

## H0 v3 hardening

Commit a6bbc47b7 hardened the judge authority without changing the existing runtime or the model, topology, workload or SLO figures.
The new
[H0 v3 spec](../../../test/benchmarks/cluster-inference/release-a/benchmark-spec-qwen122b-h0-v3.json) has
SHA-256 d069fc04d01bd16ef6bebc44f5bfb8c23659e9b0d31685503dbf2cfacd07d8aa.

- The materializer records per-request timeouts and per-class SLOs in the schema 3 seal.
- The judge numerically enforces the per-request E2E deadline, per-class TTFT p95 and ITL p95 from actual token intervals.
- It rejects missing timestamps, reversed ordering, receipt count mismatches and classes with no samples.
- The sealed paths are pinned to LF so that a fresh Windows worktree gets the same raw-byte hashes.

H0 v3 passed on the first run for the final candidate in the independent worktree and in a fresh detached checkout. After integrating into main,
the first verification failed with artifact changed: corpus because of the CRLF corpus in a worktree that was already open.
An exhaustive working/index/blob comparison showed 6 sealed files differing only in line endings; after re-materializing from the exact HEAD blobs,
the next fixed gate passed.

    {"passed":true,"checks":6,"inspector_tests":4,"preflight_tests":5,
     "materializer_tests":4,"judge_tests":10,"spec_tests":4}

The functional runtime did not change, so the full workspace Rust tests and the real adapter regressions were not recounted at this stage.
H0 v3 is load_authorized=true, runtime_acceptance=false.

## Attempts and next gate

- H1 first run: **RED** — H0 v1 mixed quality with a concurrent wave, and only 8/64 completed.
- H1 second run: **INVALID** — closed-loop real computation progressed, but the judge contract was incomplete and there is no terminal artifact.
- H1 third run: not started yet.

Do not immediately repeat a third full quality64. First seal an **A-COST feasibility gate** that runs LOAD→generation→RELEASE→UNLOAD of a
single normal short request on the same Qwen122B 3-host topology with the H0 v3 judge.
This gate does not count as an H1 pass; it obtains actual TTFT, token intervals, E2E and normal JSON/EOS to judge the short SLO and
the feasibility of the cost of 64 sequential requests. If even a single request exceeds the short SLO, the full H1 is not run and
the runtime cost boundary is fixed instead. Only if it passes is the third H1 opened. Deadlines, inputs and SLOs are not relaxed.
