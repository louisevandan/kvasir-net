# Release A Qwen122B H1 2차 INVALID와 H0 v3 판정 보강

2026-09-16 10:43 KST. H1 2차는 **INVALID**다. Qwen3.5-122B-A10B 3-stage가 실제로
적재되어 계산했지만, 실행 중 감사에서 H0 v2 judge가 요청별 E2E deadline과 class별 TTFT/ITL p95를
판정하지 않는다는 사실을 확인했다. 이 실행은 H1 GREEN이나 제품 성능 RED의 근거로 사용하지 않는다.
terminal artifact도 없으므로 부분 로그에서 latency나 정상 응답을 복원하지 않는다.

## 실행 identity와 중단 근거

| 항목 | 값 |
| --- | --- |
| runtime source | 25edd33cf24b46964e432a7cd6d89772417673db |
| H0 v2 spec | SHA-256 b79aff2254c06612eadeb9327e23598d4a3619d886789add28a22606ba3b39bf |
| 실행 root | Spark /home/m42/p4-h1-v2-run-20260916-094333 |
| event-drive | PID 2753825, SHA-256 a11f92865ba9e060fa653c2d76cfdd7ee09e6c03f6efafc4f08afbfca150923c |
| task agents | Spark 2749529, Mac20 32481, Mac21 31400 |
| generation | 202609161101 |
| schedule | quality64, RELEASE closed-loop, max_in_flight=1 |
| source config | short32/medium16/long16, 요청별 600/1200/1800초, 전체 67,500초 |
| 실행 시각 | event-drive 09:52:03경 시작, 10:33:16 snapshot에서 elapsed 41분13초 |
| artifact | h1-quality.artifact.json 없음 |
| stdout/stderr | 두 파일 모두 0 bytes |

중단 직전 양방향 INSPECT는 세 node와 transport failure 0을 반환했다. head는 요청 1건
234,683 bytes를 보유했고 completion group 3건이 268,435,456-byte 예약을 사용해
native_completion_reservation_full:waiting이었다. Mac20/21은 같은 generation의 node가 loaded였다.
봉인 native 로그에는 head/intermediate/tail linkcpp graph stage가 각각 1,021/1,023/1,023회 남았다.
이는 실제 계산 진행 증거일 뿐, 요청 완료·TTFT·ITL·E2E·응답 품질 증거가 아니다.

실행을 계속해도 당시 judge는 다음 수치를 소비하지 않았다.

- 요청별 completed_ms-arrival_ms <= request_timeout_ms
- short/medium/long class별 TTFT nearest-rank p95
- 생성 token의 실제 output_received_ms 간격으로 계산한 class별 ITL nearest-rank p95
- 표본이 없는 class의 fail-closed 판정

따라서 기존 judge가 나중에 PASS를 내더라도 검증 규약의 H1 계약을 증명할 수 없었다. 측정 source와
판정 권위를 실행 중 바꾸지 않는 규칙에 따라 event-drive의 정확 PID·argv를 대조한 뒤 TERM했다.
event-drive에는 signal 시 partial artifact/FINISH를 만드는 경로가 없어 artifact가 생성되지 않았다.

## 정상 회수와 남은 transport ledger

event-drive 종료 뒤 세 node에 NODE_UNLOAD를 각각 정확히 한 번 보냈다. 세 결과 모두
status=succeeded, resource_state=absent였고 adapter 결과는 llamacpp.unloaded-v3였다.
후속 INSPECT에서 세 host 모두 nodes=[]였다.

Spark task agent에는 사라진 OUTER 반환 경로로 보내지 못한 transport failure 2,390건,
4,505,782 bytes가 not_started로 남았다. 이를 삭제하거나 H1 결과에 합치지 않았다. nodes가 빈 것을
확인한 뒤 task agent 3개, Spark proxy, 로컬 reverse tunnel 4개를 정확한 PID·executable·cmdline·listener로
대조해 TERM했다. native PID 2753848/32734/31630은 UNLOAD가 정상 회수해 이미 없었다. 작업 port
22150–22153/23150–23152와 Spark GPU compute process는 0이다. 보호 agent PID
1287453/69656/85992와 각 :52005 listener는 모두 생존한다.

ignored 원자료는 로컬
target/release-a-h1-v2-run-20260916-094333/abort-evidence/와 Spark 실행 root에 보존했다.
정상 회수는 생명주기 GREEN이지만 H1 수용 결과를 만들지는 않는다.

## H0 v3 보강

커밋 a6bbc47b7은 기존 runtime과 모델·topology·workload·SLO 수치를 바꾸지 않고 판정 권위를
보강했다. 새
[H0 v3 spec](../../../test/benchmarks/cluster-inference/release-a/benchmark-spec-qwen122b-h0-v3.json)의
SHA-256은 d069fc04d01bd16ef6bebc44f5bfb8c23659e9b0d31685503dbf2cfacd07d8aa다.

- materializer가 요청별 timeout과 class별 SLO를 schema 3 seal에 기록한다.
- judge가 요청별 E2E deadline, class별 TTFT p95, 실제 token 간격 ITL p95를 수치로 강제한다.
- 누락 timestamp, 순서 역전, receipt 수 불일치, 표본 없는 class를 거부한다.
- Windows fresh worktree도 raw-byte hash가 같도록 봉인 경로를 LF로 고정했다.

독립 worktree의 최종 후보와 fresh detached checkout에서 H0 v3가 첫 실행에 통과했다. main 통합 뒤
첫 검증은 이미 열려 있던 worktree의 CRLF corpus 때문에 artifact changed: corpus로 실패했다.
working/index/blob 전수 비교에서 봉인 파일 6개가 줄바꿈만 달랐고, 정확한 HEAD blob으로 재물질화한 뒤
다음 고정 gate가 통과했다.

    {"passed":true,"checks":6,"inspector_tests":4,"preflight_tests":5,
     "materializer_tests":4,"judge_tests":10,"spec_tests":4}

기능 runtime은 바뀌지 않았으므로 이 단계에서 workspace Rust 전체나 실제 adapter 회귀를 재집계하지
않았다. H0 v3는 load_authorized=true, runtime_acceptance=false다.

## 회차와 다음 gate

- H1 1차: **RED** — H0 v1이 quality와 동시 wave를 혼합해 8/64만 완료.
- H1 2차: **INVALID** — closed-loop 실제 계산은 진행됐지만 judge 계약이 불완전하고 terminal artifact 없음.
- H1 3차: 아직 시작하지 않는다.

세 번째 전체 quality64를 바로 반복하지 않는다. 먼저 같은 Qwen122B 3-host topology와 H0 v3 판정기로
단일 short 정상 요청의 LOAD→생성→RELEASE→UNLOAD를 수행하는 **A-COST feasibility gate**를 봉인한다.
이 gate는 H1 통과로 세지 않으며, 실제 TTFT·token 간격·E2E와 정상 JSON/EOS를 얻어 short SLO와
64건 순차 실행 비용의 가능성을 판정한다. 단일 요청조차 short SLO를 넘으면 H1 전체를 실행하지 않고
runtime 비용 경계를 수정한다. 통과할 때만 세 번째 H1을 연다. deadline·입력·SLO를 완화하지 않는다.
