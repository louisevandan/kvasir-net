# Release A H0 v8 / I0 — Single-request re-acceptance on the final source

Status: **I0-S/M/L GREEN, overall integrity incomplete.** 2026-09-16 21:37 KST. The run used one LOAD of Qwen3.5-122B-A10B UD-Q5_K_S `[0,24)`/`[24,36)`/`[36,48)` across Spark GB10 → Mac20 M4 Pro → Mac21 M4 Pro. Windows performed only control and evidence collection. The H0 v8 source bundle is `925ca6af156ab6a259ee1c7e1b0682626ef0c2a9`, and the Rust runtime is unchanged at `b8f5214ba9a88a2bc449889af80aff4b2c3ae8c0` with the existing SHA-256 `d62b13f324da147de403b4ea40a26b2e31372aa4e175be12814d6662476b9b82`. The model and binaries were not rebuilt.

## Deterministic checks before LOAD

The 21 [H0 v8](../../../test/benchmarks/cluster-inference/release-a/benchmark-spec-qwen122b-h0-v8.json) non-model gates passed, and the I0 config SHA-256 `036d84094388298922e065c98ed24a160441d7b07fb75a71148b2f43d03f8b29` was matched against the remote file. The checks confirmed 17 script/config/binary hashes, the four Windows reverse tunnel PIDs and commands, nodes 0 and transport failure 0 on round-trip INSPECT of the three advertised addresses, and task agent listener 1/host and native child 0. The dynamic port ranges of the participating OSes (Spark 32768–60999, both Macs 49152–65535) were re-queried to confirm that 22150–22153/23150–23152 lie outside them. The three protected agents on `:52005` were preserved.

Windows PowerShell put a UTF-8 BOM into the first tunnel cleanup JSON, and Python's default cp949 parser failed on it. `cleanup-i0-owned.py --inspect-only`, run **before LOAD**, blocked this; the tunnel launcher was fixed to write UTF-8 without BOM, and the same parser, PID and route checks then passed. This preflight failure is not counted as a model re-run round. The reuse rule is recorded in [L087](../../../docs/deterministic-execution-register.md).

## Actual results

Raw artifact `passed=true`, configured/delivered/completed/released `3/3/3/3`, `error/evidence_missing/cleanup_error=null`, inference window `1,165,743 ms`, cross-host transfer `3,530,395,656 bytes`. The raw-data-based JSON/EOS/time verdict of `judge-h1-quality.py` passed 3/3, and the independent I0 evidence builder and judge passed I0-S/M/L 3/3.

| arm | Service result | TTFT | prefill rows/s | Useful model generation TPS | Total model generation TPS | ITL p95 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| I0-S | Exact JSON, EOS, RELEASE | 19,887 ms | 139.89 | 4.223 | 4.219 | 78 ms |
| I0-M | Exact JSON, EOS, RELEASE | 250,028 ms | 136.76 | **0** | 0.872 | 87 ms |
| I0-L | Exact JSON, EOS, RELEASE | 872,645 ms | 117.17 | **0** | 0.246 | 98 ms |

The denominator of useful TPS is the time from the request's arrival to model completion. For medium/long, the OUTER `engineering_power_v1` discards the raw model tokens and computes new JSON from the raw data, so the useful model token numerator is 0; total generation TPS preserves the actual model generation volume. GPU sample coverage is 100% on all three hosts, and average utilization is Spark 34.66%, Mac20 3.44% and Mac21 4.91%. Spark average power is 32.74 W; Mac power/temperature are null, with the reason they cannot be observed. The average prefill physical batch width is 63.23/64.00/63.96 rows, and the decode width is 1 because a single request is in flight. No performance improvement is claimed.

The LOADED, INFERENCE_WINDOW and DRAINED barriers were observed, and each host's node/native child count, 1 at peak and drain, returned to 0 after UNLOAD. After the final reclaim of the task agents, proxies and tunnels, work-owned native children, listeners and GPU compute processes were 0, and transport failures were 0. A separate OS query also showed only the three protected agents remaining, with 0 work natives and 0 of the four Windows tunnel PIDs.

The full `judge-integrity.py` fails only `arm_coverage`, as expected, because I1–I4 were not run, and `integrity_baseline=false`. The next action is to open a new task agent and return tunnel for I1-Q64, which has already passed byte-identical regeneration and the 64-case class/oracle/processor/deadline seal, and to repeat the same round-trip and reclaim preflight right before LOAD. Overall product integrity is not declared before I1 produces 64/64 real service evidence.

Reproduction: `python test/benchmarks/cluster-inference/release-a/run-integrity-i0.py --manifest target/release-a-i0-v8-20260916/control/manifest.json --output target/release-a-i0-v8-20260916/execution-01` and `python target/release-a-i0-v8-20260916/judge.py`. The SHA-256 values of the ignored raw evidence are `artifact.json=b4a9391f940d2574c73bb8de1866216b57ba134f6f52d44054892fd26f574879`, `raw.json=0134a74c037330e59b3b0ad6a3d776993f3b5046fe663b372f504558e7bd25bc`, `external.json=2f9094ee290be7addf3e09a48262fc6c1fb0e93f5209db3acf1754ea9165871f` and `bundle.json=e22acd459fe9e0fb179091810bd29a8c572935006e8d2d492d8c735f3dda1b17`.
