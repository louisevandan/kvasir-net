# Release A I0 v6 real-hardware acceptance — Qwen122B single request 3/3

**Correction, 2026-09-16 20:43 KST:** The original judge in this report wrongly counted the medium/long model tokens that OUTER discards as useful generation TPS. The service correct answers 3/3 and the reclaim evidence stand, but the performance scorecard is not final evidence. The [H0 v7 real-hardware rerun](20260916_204300.md) passed 3/3 again with a new judge source and fixed the useful model generation TPS for medium and long at 0 each.

## Time and execution environment

2026-09-16 19:55 KST. Qwen3.5-122B-A10B UD-Q5_K_S was loaded distributed across Spark GB10 → Mac20 M4 Pro → Mac21 M4 Pro as `[0,24)`/`[24,36)`/`[36,48)`. The Windows PC did only control and evidence assembly. The H0 v6 runtime source is `b8f5214ba9a88a2bc449889af80aff4b2c3ae8c0`, and the runner SHA-256 is `d62b13f324da147de403b4ea40a26b2e31372aa4e175be12814d6662476b9b82`. The remote workspace for the same source was **1536 passed / 0 failed / 7 ignored**, and a mutation in an independent copy that reverted to trusting the model's arithmetic was detected by the source test and by the real OUTER consumer test. The 17 check items of the H0 v6 validator also passed before and after the run.

The config `target/release-a-i0-v6-20260916/run-01/integrity-i0.json` has SHA-256 `4891ae1dd7d7ea6c0e417b0dc9a5862c572fc29b29901e3b9d70d33385043207` and sealed the I0-S/M/L order, `max_in_flight=1`, and a single LOAD/UNLOAD. The OUTER `engineering_power_v1` raw-data computation applied only to medium/long. The raw model responses in the run artifact are preserved separately as is.

## Run stages and observation logs

The first control preflight failed. The four SSH reverse tunnels started from a transient Windows shell exited right after the start check, and the Mac20 agent preserved `Connection refused` from return peer `tcp://localhost:22151` as three `not_started` entries. Reopening the tunnels alone did not clear the failed peer cache, so the round-trip INSPECT timed out. No model LOAD had happened, and a direct INSPECT recorded nodes 0 on all three hosts and transport failure 3 on Mac20. `target/release-a-i0-v6-20260916/preflight-failure-direct-inspect.jsonl` and the per-host `preflight-*-agent.err` were preserved. The blocking check and removal mutation for [execution register L084](../../../docs/deterministic-execution-register.md) are in a separate `f3e84016f` checkpoint. That checkpoint was not merged into the source of this sealed run.

Only the task-dedicated agents were started as new broker instances, and the four PIDs/commands of the persistent SSH sessions and the Mac reverse listeners were confirmed. Then a **round-trip INSPECT per actual advertised address** confirmed nodes 0 and transport failure 0 on Spark, Mac20 and Mac21. The 15 execution binding SHA-256 values and, per host, native child 0 and task listener 1 also matched before LOAD. The three existing protected agents (`:52005`) were not touched.

The first real model run of the new source succeeded. `P4_EVENT_GATE_LOADED`, `P4_EVENT_GATE_INFERENCE_WINDOW` and `P4_EVENT_GATE_DRAINED` each fired once, and within one LOAD the three requests reached EOS, completion and RELEASE in sequence. The raw artifact has `passed=true`, `error/evidence_missing/cleanup_error=null` and `elapsed_ms=1,152,385`. Actual inter-host transfer was **3,530,395,650 bytes**. The independent `build-integrity-i0-evidence.py` and `judge-integrity-i0.py` also passed all three I0-S/M/L arms.

| Case | Valid JSON | Service TTFT | prefill rows/s | Re-judged useful model TPS | Total model generation TPS | decode rows/s | ITL p95 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| I0-S | Pass | 19,755 ms | 140.83 | 4.302 | 4.301 | 15.38 | 76 ms |
| I0-M | Pass | 246,664 ms | 138.37 | **0** | 0.884 | 14.16 | 74 ms |
| I0-L | Pass | 863,142 ms | 118.39 | **0** | 0.249 | 11.83 | 87 ms |

The denominator of the re-judged useful TPS is each request's `completed_ms - arrival_ms` and includes prefill. OUTER discards the medium/long model tokens when it builds the new JSON, so the numerator is 0. Total model generation TPS is a separate figure: raw generated tokens divided by the time to RELEASE. The re-judgement is a raw-data diagnosis and is not applied retroactively as final scorecard approval for the v6 run source. Service TTFT reflects the time at which medium/long responses are published after verification. The average physical prefill batch width was 63.23/64.00/63.96 rows, and the full-ubatch ratio was 97.7%/100%/99.9%. Because this is a single in-flight run, decode width is 1 throughout and there are 0 mixed batches. These figures do not prove a performance improvement.

In the same 1,153-second inference window, GPU sample coverage was 100% on all three hosts. Average util was Spark 32.1%, Mac20 3.3% and Mac21 4.5%. An average Spark power of 33.1 W was observed, and Mac power/temperature was marked as unsupported by host telemetry. The 7,977 per-stage compute spans, runnable/blocked and per-phase batches are preserved in the raw artifact. The cause of the low GPU utilization and any improvement rate are not concluded from this single I0 request alone.

The raw model's medium/long outputs contained code fences and wrong power and alarm values. OUTER verified the request's record ID and revision and computed power, energy and alarms **from the figures in the input raw data** to produce exact JSON. The raw output is preserved in `model_response`. This result is therefore acceptance of the specified `engineering_power_v1` service contract, not acceptance of the model's general arithmetic ability or of its correct-answer rate on arbitrary natural-language questions.

The peak and drain snapshots showed node 1, native child 1 and the model resident on all three hosts; after UNLOAD, nodes/native child 0 and agent listener 1. After cleaning up the task-dedicated agents/proxies/tunnels, nodes, task native children, task listeners, GPU compute processes and transport failures are all 0, and the three protected agents are alive. `cleanup.json` and `route-snapshots.json` are preserved together.

## Verdict and reproduction

**I0 v6 proved service correct answers 3/3 at the time, but because of the performance scorecard error it is not the current seal.** The full `judge-integrity.py` recorded exit 1 on `arm_coverage` and `integrity_baseline=false` because the I1–I4 arms do not exist yet. I1–I4 and the performance P stages are not accepted. The new H0 v7 re-verified I0 with the tunnel preflight and the useful-TPS provenance check combined. This v6 3/3 is not reused as run evidence for the new source.

The ignored reproduction raw data is in `target/release-a-i0-v6-20260916/execution-r2/`. `artifact.json` SHA-256 is `b6246ea7817dcf21fa1c33fee0d0c97ce9667e7292d832bbbe9094b290a27813`, `raw.json` is `e024ee4198e928b0833c872b1b2c667bd474fffc6f2f0cfeabc3f3fbb373c0e8`, `external.json` is `285b6be6f719fd86a6005d8df81444d86e13643dd5b2013a972440a5e9539d1a`, and `bundle.json` is `c59d755664f99294f676fa0529b355f13c04d95b9b881d25532c2b736bca5b3c`. The run command was `python test/benchmarks/cluster-inference/release-a/run-integrity-i0.py --manifest target/release-a-i0-v6-20260916/control/manifest.json --output target/release-a-i0-v6-20260916/execution-r2`, and the artifact/seal/raw in the same directory were fed in turn to `build-integrity-i0-evidence.py`, `judge-integrity-i0.py` and `judge-integrity.py`.
