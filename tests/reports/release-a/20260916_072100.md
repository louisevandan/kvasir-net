# Release A Qwen3.5-122B H0 실행 명세 봉인 보고

2026-09-16 07:21 KST. 기준 runtime source는 `c6a28b58269f9ff06c21c8cf9aee73a7ec0b21fa`다.

## 판정

H0는 **GREEN**이다. [봉인 benchmark-spec](../../../test/benchmarks/cluster-inference/release-a/benchmark-spec-qwen122b-h0-v1.json)이
모델·3물리 host·실행물·라이브러리·배치/자원 상한·workload·SLO·A/B·telemetry·증거 형식을 모두
구체적인 값으로 결속했다. 검사 결과는 `load_authorized=true`, `runtime_acceptance=false`다. 이 판정은
다음 H1의 실제 `NODE_LOAD`를 허용할 뿐, 정상 응답이나 H1–H7 수용을 소급 승인하지 않는다.

H0 중 모델을 적재하거나 추론하지 않았다. 이 Windows PC에서는 Rust/native build와 모델 실행을 하지 않았다.
기존 Spark/Mac20/Mac21 `:52005` agent를 종료하지 않았고, 최종 host snapshot에 task-owned
`p4_staged_server`와 HF worker가 없다.

## 봉인 대상

| 항목 | 값 |
| --- | --- |
| 명세 | `p4.release-a.benchmark-spec.v1`, 52,098bytes, SHA-256 `2ba8d0636a6df18130ffecb85922734e7eda25259f2c4b0dd2f5979a28689bda` |
| 모델 | Qwen3.5-122B-A10B, UD-Q5_K_S 3-shard, 88,310,156,320 artifact bytes |
| GGUF 산술 | tensor 899개, 총 124,635,206,144 parameters, expert 118,380,036,096, token당 활성 9,954,546,176 |
| 모델 구조 | qwen35moe block49, trunk48, MTP1은 비활성, model context262,144 |
| 실행 shape | resident8, sequence102,400, total context819,200, batch128/ubatch64, unified F16 KV, flash attention |
| sampling | temperature0, seed20260916, output cap2,048, speculative 없음 |
| scheduler/judge/summary | `6b92a85d…` / `d37f43fa…` / `c0a54b17…` |
| P4 실행물 | Spark agent `86caac7b…`, event-drive `7d6af638…`; 두 Mac agent `5c2aa330…` |
| native | Spark `4a14c867…`; 두 Mac `8e20e202…`; upstream `451b89bae…`, patch `a07b7826…` |
| 생명주기 | Agent-target `application/vnd.p4.node.load-v1` / `node.unload-v1`; 별도 CREATE/DELETE 금지 |

GGUF header를 읽기 전용으로 파싱해 nonexpert + expert×8/256 산식을 적용했다. 파일명의 `122B-A10B`를
계산값 대신 사용하지 않았다. 세 shard 전체 hash는 기존 봉인값을 유지하고, H1 LOAD 직전 공유 저장소의
동일 파일을 한 host에서 다시 검증한다.

## 물리 host와 배치

| stage | host/device | cut | 계획 bytes | 예비 후 여유 | 최대 physical result | 봉인 port |
| --- | --- | --- | ---: | ---: | ---: | --- |
| spark-0 | gx10-c044 / NVIDIA GB10 CUDA0 | `[0,24)` | 54,259,452,320 | 62,921,352,800 | 103,843,468 | agent22150/native23150 |
| mac20-1 | Macmini-2 / Apple M4 Pro MTL0 | `[24,36)` | 28,477,144,128 | 10,569,843,648 | 103,843,468 | agent22150/native23151 |
| mac21-2 | Macmini / Apple M4 Pro MTL0 | `[36,48)` | 29,394,628,160 | 17,677,750,720 | 3,696,012 | agent22150/native23152 |

Spark GPU UUID는 `GPU-583855c0-b56c-03c9-4f46-177eb3efb800`이다. Metal은 UUID를 노출하지 않아
각각 registry ID `4294968303`/`4294968441`을 이유와 함께 기록했다. GB10의 power limit는
`nvidia-smi`가 N/A이고 Metal은 configurable device cap이 없어 모두 `unavailable+reason`으로 보존했다.
값 0으로 대체하지 않는다. Spark native dependency 19개, 각 Mac 배포 dylib 27개의 개별 hash가 host
snapshot에 있다.

모든 경로는 1,000Mbps full-duplex다. 5회 ping의 평균 RTT는 Spark→Mac20/21
0.643/0.669ms, Mac20→Spark/Mac21 0.954/0.752ms, Mac21→Spark/Mac20 0.962/0.719ms이고
packet loss는 모두 0%다. Linux dynamic port `32768–60999`, macOS `49152–65535`를 피해
22150/23150–23152를 배정했다. 실제 시작 전 listener 부재를 다시 검사한다.

## 자원·workload 계약

- active resident8 외 pending64건, 128MiB, 6,553,600 input tokens를 둔다. active까지 포함한 admission은
  72건, 144MiB, 7,372,800 input tokens, 147,456 output tokens다.
- completion/edge store는 각각 count65,536/256MiB, receipt는 count65,536/64MiB,
  hop outstanding은256이다. 최대 result 기준 동시 retained group은 앞 두 stage2개, tail72개다.
- H1 quality64, H2 cold8·sustained8×8·같은 load recovery3×8, H3 overload80,
  H7 32wave×8=256건/최소93분을 고정했다.
- cancel·slow edge·disconnect·중간 stage restart·late return을 별도 fault arm으로 고정했다.
- TTFT p95 short/medium/long 60/300/900초, ITL p95 250ms, send slip1초, clock skew10ms,
  표본1초·coverage95%·warmup60초다.
- A/B는 기본 안전 정책과 bounded mixed pipeline policy만 비교한다. paired8쌍과 holdout4쌍을 균형
  순서로 고정했고, useful TPS 중앙 개선5%·95% CI 하한 양수·TTFT/ITL 비회귀 기준을 유지한다.

Mac의 무권한 `powermetrics`는 password 요구로 사용할 수 없고, `ioreg` AGX
`PerformanceStatistics` fallback은 두 host 모두 읽기 성공했다. H4는 이를 실제 1초 sampler로 정규화해야
하며 지원하지 않는 power/temperature 항목은 이유 있는 null로 남긴다.

## 결정론적 실행과 시험

| gate | 결과 |
| --- | --- |
| 현재 source 원격 release build | Spark agent+event-drive 및 Mac agent2대 모두 첫 build 통과, jobs8/host |
| host inventory | 3회차 통과. 1회차는 power 표현과 dynamic/telemetry 누락, 2회차는 Mac `@rpath` dylib hash 누락을 거부하고 3회차에 필수 필드 전부 봉인 |
| Python inspector | 4/4 통과. GGUF exact parameter 산술과 NVIDIA N/A power 판정 포함 |
| H0 Node gate | 10/10 통과. 새 H0 4개와 기존 manifest/corpus 6개 |
| 구조 변이 | source/lifecycle/원격 shell/host/device/link/cut/budget/workload/SLO/A-B/telemetry 등 25종 거부 |
| 파일 교체 변이 | hash 형식은 유효하지만 실제 bytes가 다른 inspector 교체 1종 거부 |
| 실제 CLI | `valid=true`, `h0_status=sealed`, `load_authorized=true`, `runtime_acceptance=false` |

Python 첫 시험 호출은 `python -m unittest <hyphen path>`가 Windows에서 dotted module로 오해해 수집 전
실패했다. 명시적 discovery root/pattern으로 바꾸고 L054에 고정했다. H0 완성 후보의 Node/Python/CLI
제품 gate는 첫 실행에 통과했다. 뒤이은 보조 EOL 검사식이 혼재 없는 LF 파일도 거부한 오류는 L055로
고정하고 mixed EOL만 판정하도록 수정했다. 원격 `$HOME`이 로컬 PowerShell에서 확장된 반복 실수도 L053으로
승격했고, spec은 원격 argv의 `$`·pipe·redirect·command substitution을 거부한다.

최종 host evidence SHA-256은 Spark `51ee4502…`, Mac20 `eee3b513…`, Mac21 `4c7fd8b5…`,
GGUF metadata `5cca37b2…`다. 검사 runner는 `d4fbd906…`, GGUF parser는 `7324ce77…`다.

## 다음 첫 행동

H1은 새 `:22150` task agent namespace에서 시작한다. 먼저 세 host의 현재 task-owned node/listener/worker를
INSPECT하고 소유 항목만 UNLOAD한다. 공유 모델 shard hash, source/binary/library, dynamic port, 가용 pool을
spec과 다시 대조한 뒤 `NODE_LOAD` 3/3을 한 번 수행한다. READY의 placement·allocation·result bound가
모두 일치해야 64개 quality corpus를 실행한다. 기존 `:52005` agent는 건드리지 않는다.
