# Release A Qwen3.5-122B H0 execution specification seal report

2026-09-16 07:21 KST. The baseline runtime source is `c6a28b58269f9ff06c21c8cf9aee73a7ec0b21fa`.

## Verdict

H0 is **GREEN**. The [sealed benchmark-spec](../../../test/benchmarks/cluster-inference/release-a/benchmark-spec-qwen122b-h0-v1.json)
binds the model, 3 physical hosts, executables, libraries, placement/resource caps, workload, SLO, A/B, telemetry and evidence format, all
to concrete values. The check result is `load_authorized=true`, `runtime_acceptance=false`. This verdict
only permits the real `NODE_LOAD` of the next step, H1; it does not retroactively accept normal responses or H1–H7.

No model was loaded and no inference ran during H0. No Rust/native build or model execution happened on this Windows PC.
The existing Spark/Mac20/Mac21 `:52005` agents were not stopped, and the final host snapshots contain no task-owned
`p4_staged_server` or HF worker.

## Sealed items

| Item | Value |
| --- | --- |
| Specification | `p4.release-a.benchmark-spec.v1`, 52,098 bytes, SHA-256 `2ba8d0636a6df18130ffecb85922734e7eda25259f2c4b0dd2f5979a28689bda` |
| Model | Qwen3.5-122B-A10B, UD-Q5_K_S 3-shard, 88,310,156,320 artifact bytes |
| GGUF arithmetic | 899 tensors, 124,635,206,144 parameters in total, 118,380,036,096 expert, 9,954,546,176 active per token |
| Model structure | qwen35moe block 49, trunk 48, MTP 1 disabled, model context 262,144 |
| Execution shape | resident 8, sequence 102,400, total context 819,200, batch 128/ubatch 64, unified F16 KV, flash attention |
| sampling | temperature 0, seed 20260916, output cap 2,048, no speculative |
| scheduler/judge/summary | `6b92a85d…` / `d37f43fa…` / `c0a54b17…` |
| P4 executables | Spark agent `86caac7b…`, event-drive `7d6af638…`; both Mac agents `5c2aa330…` |
| native | Spark `4a14c867…`; both Macs `8e20e202…`; upstream `451b89bae…`, patch `a07b7826…` |
| Lifecycle | Agent-target `application/vnd.p4.node.load-v1` / `node.unload-v1`; separate CREATE/DELETE forbidden |

The GGUF header was parsed read-only and the nonexpert + expert×8/256 formula was applied. The `122B-A10B` in the file name
was not used in place of the computed value. The full hashes of the three shards keep their existing sealed values, and the same
files on shared storage are verified again from one host right before the H1 LOAD.

## Physical hosts and placement

| stage | host/device | cut | Planned bytes | Headroom after reserve | Max physical result | Sealed port |
| --- | --- | --- | ---: | ---: | ---: | --- |
| spark-0 | gx10-c044 / NVIDIA GB10 CUDA0 | `[0,24)` | 54,259,452,320 | 62,921,352,800 | 103,843,468 | agent 22150/native 23150 |
| mac20-1 | Macmini-2 / Apple M4 Pro MTL0 | `[24,36)` | 28,477,144,128 | 10,569,843,648 | 103,843,468 | agent 22150/native 23151 |
| mac21-2 | Macmini / Apple M4 Pro MTL0 | `[36,48)` | 29,394,628,160 | 17,677,750,720 | 3,696,012 | agent 22150/native 23152 |

The Spark GPU UUID is `GPU-583855c0-b56c-03c9-4f46-177eb3efb800`. Metal does not expose a UUID, so
registry IDs `4294968303`/`4294968441` were recorded with the reason. For GB10 the power limit is N/A in
`nvidia-smi`, and Metal has no configurable device cap, so both are preserved as `unavailable+reason`.
They are not replaced with 0. Individual hashes of the 19 Spark native dependencies and the 27 dylibs deployed on each Mac are in the host
snapshots.

All links are 1,000Mbps full-duplex. The mean RTT over 5 pings is Spark→Mac20/21
0.643/0.669ms, Mac20→Spark/Mac21 0.954/0.752ms, Mac21→Spark/Mac20 0.962/0.719ms, and
packet loss is 0% on all. Avoiding the Linux dynamic ports `32768–60999` and macOS `49152–65535`,
22150/23150–23152 were assigned. The absence of listeners is checked again right before the actual start.

## Resource and workload contract

- Beyond active resident 8, there are 64 pending requests, 128MiB and 6,553,600 input tokens. Admission including active is
  72 requests, 144MiB, 7,372,800 input tokens and 147,456 output tokens.
- The completion and edge stores are count 65,536/256MiB each, receipts are count 65,536/64MiB,
  and hop outstanding is 256. At the maximum result size, concurrent retained groups are 2 for each of the first two stages and 72 for the tail.
- Fixed: H1 quality 64, H2 cold 8, sustained 8×8 and same-load recovery 3×8, H3 overload 80,
  H7 32 waves × 8 = 256 requests / at least 93 minutes.
- Cancel, slow edge, disconnect, mid-stage restart and late return are fixed as separate fault arms.
- TTFT p95 short/medium/long 60/300/900 s, ITL p95 250ms, send slip 1 s, clock skew 10ms,
  sampling at 1 s, coverage 95%, warmup 60 s.
- A/B compares only the default safe policy and the bounded mixed pipeline policy. 8 paired runs and 4 holdout pairs are fixed in balanced
  order, and the criteria of a 5% median useful TPS improvement, a positive 95% CI lower bound and TTFT/ITL non-regression are kept.

Unprivileged `powermetrics` on the Macs cannot be used because it requires a password, but the `ioreg` AGX
`PerformanceStatistics` fallback was read successfully on both hosts. H4 must normalize this into a real 1 s sampler,
and unsupported power/temperature items stay null with a reason.

## Deterministic execution and tests

| gate | Result |
| --- | --- |
| Remote release build of the current source | Spark agent+event-drive and both Mac agents passed on the first build, jobs 8 per host |
| host inventory | Passed on round 3. Round 1 rejected the power representation and missing dynamic/telemetry, round 2 rejected missing Mac `@rpath` dylib hashes, and round 3 sealed all required fields |
| Python inspector | 4/4 passed, including exact GGUF parameter arithmetic and the NVIDIA N/A power verdict |
| H0 Node gate | 10/10 passed: 4 new H0 tests and 6 existing manifest/corpus tests |
| Structural mutations | 25 kinds rejected, including source/lifecycle/remote shell/host/device/link/cut/budget/workload/SLO/A-B/telemetry |
| File replacement mutation | 1 kind rejected: an inspector replacement with a valid hash format but different actual bytes |
| Real CLI | `valid=true`, `h0_status=sealed`, `load_authorized=true`, `runtime_acceptance=false` |

The first Python test call failed before collection because Windows misread `python -m unittest <hyphen path>` as a dotted module.
It was changed to an explicit discovery root/pattern and pinned in L054. The Node/Python/CLI
product gates of the H0 completion candidate passed on the first run. An error in a subsequent auxiliary EOL check that also rejected LF files with no mixing was
pinned as L055 and fixed to judge only mixed EOL. The repeated mistake of the remote `$HOME` being expanded in local PowerShell was
promoted to L053, and the spec rejects `$`, pipes, redirects and command substitution in remote argv.

The final host evidence SHA-256 values are Spark `51ee4502…`, Mac20 `eee3b513…`, Mac21 `4c7fd8b5…`, and
GGUF metadata `5cca37b2…`. The check runner is `d4fbd906…`, and the GGUF parser is `7324ce77…`.

## First next action

H1 starts in a new `:22150` task agent namespace. First INSPECT the current task-owned nodes/listeners/workers on the three hosts
and UNLOAD only owned items. After rechecking the shared model shard hashes, source/binary/library, dynamic ports and available pool
against the spec, perform `NODE_LOAD` 3/3 once. The 64-item quality corpus runs only if the placement, allocation and result bound in READY
all match. The existing `:52005` agents are not touched.
