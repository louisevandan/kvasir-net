# Release A H0 v7 / I0 — Qwen122B single request 3/3 re-acceptance

## Environment and seal

2026-09-16 20:43 KST. Qwen3.5-122B-A10B UD-Q5_K_S was loaded once across Spark GB10 → Mac20 M4 Pro → Mac21 M4 Pro as `[0,24)`/`[24,36)`/`[36,48)`. Windows did only control and evidence collection. The actual Rust runner for H0 v7 is the same runtime source as before, `b8f5214ba9a88a2bc449889af80aff4b2c3ae8c0`, with SHA-256 `d62b13f324da147de403b4ea40a26b2e31372aa4e175be12814d6662476b9b82`. The new judge and tunnel preflight are bound to branch `47b571c37`, and the 17 check items of the H0 v7 non-model verification passed before and after the run. The Rust runtime binary was not rebuilt or changed.

`target/release-a-i0-v7-20260916/run-01/integrity-i0.json` has SHA-256 `0af42f47449e93dfaf4c9d8248a92de3aac3b4100394782e82c6dba2b9055aad`. It sealed the I0-S/M/L order, `max_in_flight=1`, one LOAD/UNLOAD and three 15-second observation barriers. The PIDs and commands of the four Windows reverse tunnels and the hashes of 17 remote/local executables were checked before LOAD. In a round-trip INSPECT over the actual advertised addresses, all three hosts had nodes/native child 0, task agent listener 1 and transport failure 0. The three protected agents on `:52005` were kept.

## Results

`P4_EVENT_GATE_LOADED`, `P4_EVENT_GATE_INFERENCE_WINDOW` and `P4_EVENT_GATE_DRAINED` each fired once. The raw artifact has `passed=true`, requests/completions/RELEASE `3/3/3`, `error/evidence_missing/cleanup_error=null`, and an inference window of `1,153,810 ms`. Distributed transfer was `3,530,395,645 bytes`. The raw-data and service response verdict of `judge-h1-quality.py` is 3/3, and the I0-S/M/L verdict of the independent `build-integrity-i0-evidence.py` and `judge-integrity-i0.py` is also 3/3.

| arm | Service JSON/EOS/RELEASE | Service TTFT | prefill rows/s | Useful model generation TPS | Total model generation TPS | ITL p95 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| I0-S | Pass | 19,463 ms | 142.94 | 4.329 | 4.327 | 73 ms |
| I0-M | Pass | 247,143 ms | 138.11 | **0** | 0.882 | 75 ms |
| I0-L | Pass | 864,291 ms | 118.24 | **0** | 0.249 | 87 ms |

Useful model generation TPS counts in the numerator only model tokens the service actually delivered. For medium and long, OUTER `engineering_power_v1` checks the ID, revision and format of the model output and then computes a new JSON from the raw data, so the 218/215 raw model tokens are preserved but the useful model generation numerator is 0. The denominator runs from request arrival to model completion, and total model generation TPS includes the time up to RELEASE. The raw model's wrong arithmetic values were not accepted as service answers. This is evidence of correctness for the specified raw-data computation service, not evidence of the correct-answer rate on arbitrary natural-language questions.

GPU sample coverage in the inference window was 100% on all three hosts, and average utilization was Spark 35.08%, Mac20 4.77% and Mac21 3.56%. Spark average power was 33.03 W, and Mac power/temperature is null with an unobservable reason. The average physical prefill batch width was 63.23/64.00/63.96 rows, and the full-ubatch ratio was 97.7%/100%/99.9%. With a single in-flight request, decode width is 1 and mixed batches are 0. This run does not prove a performance improvement rate.

At peak and drain, each host had node/native child 1, and 0 after UNLOAD. After the final task agent/proxy/tunnel reclaim, task-owned native children, listeners and GPU compute processes were 0, transport failures were 0, and the three protected agents were confirmed alive. The owned resources and round-trip routes are preserved together in `cleanup.json` and `route-snapshots.json`.

## Verdict and next stage

**The H0 v7 I0 single-request service is GREEN**. The full `judge-integrity.py`, as expected, gives `arm_coverage` exit 1 and `integrity_baseline=false` because the I1–I4 arms do not exist yet. No overall service integrity or performance improvement is declared. [Deterministic register L084](../../../docs/deterministic-execution-register.md) is blocked by the return tunnel liveness check, and L085 by removing discarded model tokens from the numerator. The medium/long `0.884/0.249` used as useful TPS in the H0 v6 report were wrong; this rerun's `0/0` is the current reference.

The next stage, I1, must prove the full 64-request closed loop on the same load. The static corpus 64/64, 2,201,802 total input tokens and 7,159,035 prompt bytes were checked against the request budget. A contract defect, the I1 generator having no observation barrier, was found before any model LOAD and is being fixed in a separate checkout. If the final I1 source/seal differs, a new H0 and I0 are verified again per the roadmap rules.

The reproduction command is `python test/benchmarks/cluster-inference/release-a/run-integrity-i0.py --manifest target/release-a-i0-v7-20260916/control/manifest.json --output target/release-a-i0-v7-20260916/execution-01`. In that directory, `artifact.json` SHA-256 is `22d4f3cfbfc118a6e2be05546a6bb6ec3cec8014bf06e8edf5809eead032f8ea`, `raw.json` is `bd37a498cc1498c9eb294492af15e072267b9e28eb390ca503b2793ba2a97b79`, `external.json` is `cffd9148e1976c0416bcb6f6358e95a46f7d836e3a7233c19d0db9cfba6494a7`, and `bundle.json` is `27d9f20ee34bd8292750efcffbdf7d8da8b2b2b972ea18de9464b43587719f42`. The raw data is preserved in the ignored `target/`.
