# Release A H0 v7 / I0 — Qwen122B 단일 요청 3/3 재수용

## 환경과 봉인

2026-09-16 20:43 KST. Spark GB10 → Mac20 M4 Pro → Mac21 M4 Pro에서 Qwen3.5-122B-A10B UD-Q5_K_S를 `[0,24)`/`[24,36)`/`[36,48)`로 한 번 적재했다. Windows는 제어와 증거 수집만 수행했다. H0 v7의 실제 Rust 실행기는 이전과 같은 runtime source `b8f5214ba9a88a2bc449889af80aff4b2c3ae8c0` 및 SHA-256 `d62b13f324da147de403b4ea40a26b2e31372aa4e175be12814d6662476b9b82`다. 새 판정기와 터널 사전검사는 `47b571c37` 브랜치에 결속했고, H0 v7 비모델 검증 17개 확인 항목을 실행 전후 통과했다. Rust runtime 바이너리를 다시 빌드하거나 변경하지 않았다.

`target/release-a-i0-v7-20260916/run-01/integrity-i0.json`의 SHA-256은 `0af42f47449e93dfaf4c9d8248a92de3aac3b4100394782e82c6dba2b9055aad`다. I0-S/M/L 순서, `max_in_flight=1`, 한 LOAD/UNLOAD, 15초 관측 장벽 세 개를 봉인했다. 네 Windows 역방향 터널 PID·명령과 17개 원격/로컬 실행 파일 해시를 LOAD 전에 대조했다. 실제 advertised 주소 왕복 INSPECT에서 세 host 모두 nodes/native child 0, task agent listener 1, transport failure 0이었다. 보호 agent 세 개 `:52005`는 유지했다.

## 결과

`P4_EVENT_GATE_LOADED`, `P4_EVENT_GATE_INFERENCE_WINDOW`, `P4_EVENT_GATE_DRAINED`가 각각 한 번 발생했다. 원시 artifact는 `passed=true`, 요청/완료/RELEASE `3/3/3`, `error/evidence_missing/cleanup_error=null`, 추론창 `1,153,810 ms`다. 분산 전송은 `3,530,395,645 bytes`였다. `judge-h1-quality.py`의 원자료·서비스 응답 판정은 3/3, 독립 `build-integrity-i0-evidence.py`와 `judge-integrity-i0.py`의 I0-S/M/L 판정도 3/3이다.

| arm | 서비스 JSON/EOS/RELEASE | 서비스 TTFT | prefill rows/s | 유효 모델 생성 TPS | 총 모델 생성 TPS | ITL p95 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| I0-S | 통과 | 19,463 ms | 142.94 | 4.329 | 4.327 | 73 ms |
| I0-M | 통과 | 247,143 ms | 138.11 | **0** | 0.882 | 75 ms |
| I0-L | 통과 | 864,291 ms | 118.24 | **0** | 0.249 | 87 ms |

유효 모델 생성 TPS는 서비스가 실제 전달한 모델 token만 분자로 센다. 중·장문은 OUTER `engineering_power_v1`이 모델 출력의 ID·revision·형식을 검사한 뒤 원자료에서 새 JSON을 계산하므로, 원시 모델 token 218/215개는 보존되지만 유효 모델 생성 분자는 0이다. 분모는 요청 도착부터 모델 완료까지이며 총 모델 생성 TPS는 RELEASE까지 포함한다. 원시 모델의 잘못된 산술값을 서비스 정답으로 인정하지 않았다. 이는 지정된 원자료 계산 서비스의 정확성 증거이며 임의 자연어 질문의 정답률 증거가 아니다.

추론창 GPU 표본 coverage는 세 host 모두 100%, 평균 utilization은 Spark 35.08%, Mac20 4.77%, Mac21 3.56%였다. Spark 평균 power는 33.03 W이고 Mac의 power/temperature는 관측 불가 사유와 함께 null이다. prefill 물리 batch 평균 폭은 63.23/64.00/63.96행, full ubatch 비율은 97.7%/100%/99.9%였다. 단일 in-flight라 decode 폭은 1, mixed batch는 0이다. 이 실행은 성능 개선률을 증명하지 않는다.

peak와 drain에서 각 host node/native child 1, UNLOAD 뒤 0이었다. 최종 task agent/proxy/터널 회수 뒤 작업 소유 native child·listener·GPU compute process 0, transport failure 0, 보호 agent 3개 생존을 확인했다. `cleanup.json`과 `route-snapshots.json`에 소유 자원 및 왕복 경로를 함께 보존했다.

## 판정과 다음 단계

**H0 v7의 I0 단일 요청 서비스는 GREEN**이다. 전체 `judge-integrity.py`는 I1–I4 arm이 아직 없어서 예상대로 `arm_coverage` exit 1, `integrity_baseline=false`다. 전체 서비스 정합성이나 성능 개선을 선언하지 않는다. [결정론적 장부 L084](../../../docs/deterministic-execution-register.md)는 반환 터널 생존 검사로, L085는 폐기된 모델 token 분자 제거로 차단했다. H0 v6 보고에서 중·장문의 `0.884/0.249`를 유효 TPS로 쓴 값은 잘못되었으며 이 재실기의 `0/0`이 현재 기준이다.

다음 I1은 64건 closed-loop 전체를 같은 적재에서 증명해야 한다. 정적 corpus 64/64·총 2,201,802 input token·7,159,035 prompt byte와 요청 예산을 대조했다. I1 생성기에 관측 장벽이 없는 계약 결함을 모델 LOAD 전에 찾아 별도 체크아웃에서 보강 중이다. I1의 최종 source/seal이 달라지면 로드맵 규칙에 따라 새 H0와 I0를 다시 검증한다.

재현 명령은 `python test/benchmarks/cluster-inference/release-a/run-integrity-i0.py --manifest target/release-a-i0-v7-20260916/control/manifest.json --output target/release-a-i0-v7-20260916/execution-01`이다. 그 디렉터리의 `artifact.json` SHA-256은 `22d4f3cfbfc118a6e2be05546a6bb6ec3cec8014bf06e8edf5809eead032f8ea`, `raw.json`은 `bd37a498cc1498c9eb294492af15e072267b9e28eb390ca503b2793ba2a97b79`, `external.json`은 `cffd9148e1976c0416bcb6f6358e95a46f7d836e3a7233c19d0db9cfba6494a7`, `bundle.json`은 `27d9f20ee34bd8292750efcffbdf7d8da8b2b2b972ea18de9464b43587719f42`다. 원자료는 ignore된 `target/`에 보존한다.
