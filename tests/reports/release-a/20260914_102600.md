# Release A — Installed-app execution environment check and the remaining native PLAN

2026-09-14 10:26–10:46 KST. P4 `debbe0c0f78815c22bcb37ea931771a805323a5e`, HF
`bc656a2a09422e452a69735eb411c12899caa57e`. [Plan](../../plans/release-a-20260914.md) ·
[previous fleet check](20260914_062025.md). **The model-access-blocked verdict is withdrawn. This is not yet acceptance of A0 as a whole.**

## Execution environment correction

The contract the user stated repeatedly is that SSH and the installed app have different file access environments, and that the model path is `S:/models`,
the same as on this PC. The facts that S: was not visible over SSH and that opening the UNC path directly failed with the Windows1326 authentication error
were wrongly extended into an access failure of the installed app. Concluding from `Win32_ComputerSystem.UserName:null` and the quser output alone
that no interactive session existed was also wrong. The app process's SessionId/owner and the actual API must be checked.

The installed app on `.29` is the Linker supervisor `:18082` of user 42mob/session1. `/health` was ok,
`/api/models/availability` reported all 10 shards of 550B as available=true/missing=[],
and `/api/models/inspect` read nemotron_h_moe/108layer/8192embedding/512experts from the real GGUF.
The existing installed app, its settings and processes were not changed. The 400 response to the first metadata request is kept on record as a preparation failure;
the subsequent identical request for the 10 shards returned 200. The path, model and limits were not changed.

The existing test Rust agent `:52005` runs as 42mob/session0/S4U, an execution environment different from the installed app.
The child of a diagnostic node created on that agent could not read S: either. This diagnostic child did not provide the staged HELLO, so
LOAD was ReadyFailed as expected, and it is not counted as a successful real model LOAD. The first DELETE was rejected because the node was in the failed state,
and UNLOAD rejected a generation that was not yet bound. Afterwards, this diagnostic node alone was reclaimed through a normal LOAD→UNLOAD→DELETE with a separate local Qwen file,
and INSPECT nodes=[] was confirmed. The existing agent was not restarted.
The initial reclaim rejection and the final reclaim success are separate raw records. Retention in the receipt index is not described as 0 resource leakage.

## Remaining PLAN

The currently sealed native was run as a separate hidden one-off task in 42mob/session1, the same session as the installed app.
It read the S: model as is, and native complete/fits were both true for stage0[0,24) and stage1[24,48).
CUDA_DEVICE_ORDER=PCI_BUS_ID, CUDA0/1 RTX3090, seq8/ctx819200/batch128/ubatch64/F16/FA on,
and the CPU expert placement were kept. The raw byte values are below.

| stage | device required | host required | Result |
| --- | ---: | ---: | --- |
| 0 / CUDA0 | 14,813,148,288 | 75,105,305,888 | exit0, session1, complete/fits |
| 1 / CUDA1 | 14,059,241,344 | 81,308,681,248 | exit0, session1, complete/fits |

This secures the preflight for 8/8 stages of the current native. The two host entries use the same `.29` RAM and are not treated as separate
physical pools. This is not acceptance of every limit of the public OUTER PLAN→LOAD, of the actual 550B allocation, or of the service SLO.
Each `layer_device_expectations_checked:false` is also left as is.
Only the one-off task `P4-Release-A-102259-Plans`, whose check had finished, was deleted.

## Occupancy check right before the real-hardware run

After the installed-app access correction and the 8/8 PLAN were committed and pushed in `b457e32aa`, preparation for the 550B run continued.
INSPECT on Spark's existing agent showed that `studio-qwen122b-1789349924354-0`, belonging to another task, the **browser Qwen 122B inference demo**,
was loading. Native PID39318 is a child of that task's existing
agent PID3662540 and is not owned by this check. Neither this process nor the installed app was terminated.

The last Spark available RAM reading was 44,197,605,376 bytes. The estimated shared-pool requirement of this 550B stage2 is
device 83,944,759,424 + host 106,955,808 = 84,051,715,232 bytes, so there is no headroom for loading concurrently.
The current 7-host integrated real-hardware run is on hold because of the resources held by this separate demo. This is distinct from SSH/NAS access blocking.
550B was not run with a different placement or a reduced context, and no new LOAD/SESSION/inference was started.

In the final cleanup, the local diagnostic Qwen copy was removed, and 0 tasks and 0 native children from this work were confirmed.
The executable paths and start times of the existing test agent PID5532 and the installed app PID12376 were unchanged.

## Raw data and next action

raw root `target/release-a-20260914/retry-102259/`:
`m42-{access,paths-corrected,direct-open,app-context,app-startup,installed-services}.json`,
`supervisor-{health,processes,availability}.json`, `installed-model-{availability,inspect}.json`,
`installed-app-access{,-events}.json`, `diagnostic-node-{unload,recovery}.json`,
`stage-{0,1}-plan-102259/`, `m42-plans-interactive.ps1`, `interactive-{plans-start,task-cleanup}.json`.
Additional occupancy/cleanup data: `pre-smoke-fleet.json`, `spark-{existing-inspect,final-occupancy}.json`, `final-cleanup.json`.
`m42-paths.json`, the result of the first, wrongly joined PowerShell path array, is excluded from the per-path evidence.

Next: run the sealed native and the current agent for the same 7-host/8-stage layout in the user app's execution environment,
and verify the real 550B LOAD, normal response, cost observation and reclaim. SSH access failure is not used again as an app-blocking condition.
First confirm with a real INSPECT that the separate demo's Spark/Ubuntu nodes have been reclaimed, and re-check the current capacity.
