# Release A / A-RED cause separation and past trace audit

2026-09-14 04:03 KST, Windows PowerShell. [Stage plan](../../plans/release-a-20260914.md).
Starting P4 `07e9455ede20a2e1869476ceb6f054ec0ebd81e2`, HF `bc656a2a09422e452a69735eb411c12899caa57e`.
This report is an A0 intermediate checkpoint. It is not acceptance of all of A0, A1–A5 or H0–H7.

## A-RED

The baseline tests all passed: timer-only 1, scheduler 33, bounded 10, service 2, model-loading 34, composer 4.
There is no claim that the earlier RED always reproduces today. The original test compared the global state as if it must be identical
even when a normal RELEASE is processed between `decode_coalescing_wait` and `before_native_issue`.
Holding/forwarding the real Worker's tail RELEASED separated the following two cases.

- Timer expiry with no input: the existing request/flight/slot/input authority and batch ordinal must be exactly identical.
- Normal RELEASE interleave: only one slot returned for the actually completed peer and one event for the OUTER receipt are allowed.
  At the RELEASE commit point the remaining authority must be identical. A later timer expiry also creates no additional authority.

Both paths check actual publication of an underfull decode, no republication while outstanding, and final reclaim.
The runtime timer and default policy were not changed. This is a real Worker loop test using a fake native, kept distinct from a remote native real-hardware run.
One compile error on the Option type of `sequence_id` during writing was fixed before verification.

Recompiled in copies made from an independent source archive plus an explicit overlay, each with a new Cargo target.
`target/release-a-20260914/timer-mutations/sources.json` preserves the source/overlay hashes.

| Mutation | Result | test executable SHA256 |
| --- | --- | --- |
| baseline | 3 passed, exit0 | `943d3e8337d181ae062b604129bc252fbea904fc6de4566db38674e3b90163e9` |
| Remove the linearization of a normal RELEASE | Expected assertion failed, exit101 | `b13fe30f887bfdda2367eb50c9671fcdaa55de97f6ebc8d0917a650430810f40` |
| recv_timeout expiry increments next_event | Authority preservation assertion failed, exit101 | `c442ad2b62026b402ff555c67e386aa1adbc79065d8da4ecdfa9d792b33c4afc` |

Final `cargo test --workspace --no-fail-fast` exit0, total over 58 summaries **1451 passed / 0 failed / 7 ignored**.
Log `target/release-a-20260914/workspace-final.log`, SHA256
`2feb4583f1a82a65fcec5d13fcd12720d93dceff543e2e2e96d612b90143f419`.
A separate HF feature build/real-hardware rerun is not part of this checkpoint. The previous HF acceptance evidence stands.

## A-COST / original preservation

The analyzer checks execution/load/session and rejects duplicate, backward or missing timestamps.
Within the same worker clock it separates ingress→start, start→end and end→forward.
start→end includes the native RPC and capsule decode, so it is not called pure kernel.
It passed 3 unit counterexamples and was applied to the original smoke/long artifacts.

The original `target/nemotron550-all-fleet/long-artifact.json`, 27,191,113 bytes, has SHA256
`286879542d4c58aa42b7e1025bc21e9a4c62c16091bc6b3a879ed2038d060828`.
The original native pin is `434ddbbc0e30522e897670681e503b797c12b7c1`, distinct from the current pin.
At the 7,200-second deadline, completed/released 0/0. The published prefill is contiguous, 7,864 positions 0–7863 per request, and
983 physical executions were observed. `requests.prefill_rows=0` does not mean no computation happened at all.
The last execution of each of stages 3–7 is unobserved. The original first error, cleanup error and evidence_missing are preserved.

| stage | Observed spans | RPC total ms | RPC p50 ms | forward total ms | Unobserved executions |
| --- | --- | --- | --- | --- | --- |
| 0 | 983 | 1694782 | 1711 | 2722 | 0 |
| 1 | 983 | 1869038 | 1900 | 8165 | 0 |
| 2 | 983 | 229616 | 236 | 464 | 0 |
| 3 | 982 | 271991 | 280 | 1355 | 1 |
| 4 | 982 | 393182 | 406 | 1116 | 1 |
| 5 | 982 | 805666 | 819 | 2054 | 1 |
| 6 | 982 | 1001076 | 1039 | 776 | 1 |
| 7 | 982 | 772686 | 795 | 693 | 1 |

The RPC totals are not converted into a share of total wall time or into cross-host overlap. A complete breakdown into kernel/n_kv/copy/network
and the SLO floor for the current pin are not judged yet. The earlier UNLOAD busy left active owner/frontier 8,
and the forced stop of about 4.67 s is not a graceful reclaim success. The mixed arm was not started because of old native work.
The 6 remote hosts checked read-only are reachable, and only existing agents were observed. Those processes are not owned by this test.

## Reproduction and next inputs

```powershell
cargo test -p p4-llamacpp-staged-adapter --lib v2::node::worker::loop_tests::bounded_strategy::phase_pacing_actual_loop -- --nocapture
python test/benchmarks/cluster-inference/release-a/timer-mutations.py target/release-a-fresh-mutations
cargo test --workspace --no-fail-fast
node --test test/benchmarks/cluster-inference/release-a/*.test.mjs
node test/benchmarks/cluster-inference/release-a/analyze-trace.mjs target/nemotron550-all-fleet/long-artifact.json target/release-a-fresh-long-cost.json
```

The 3 counterexamples for the new corpus generator and the source-derived exact JSON oracle also passed. They use a code equality counterexample and
evidence combined from the start, middle and end of a document, and do not approve on a substring match alone.
They are not yet wired to tokenization, semantic review, the current runtime manifest or the run runner, so this is not product fixture approval.
The first next task is to finish this A0 wiring and the native cost instrumentation, then proceed to A1 public PLAN/LOAD verification.
